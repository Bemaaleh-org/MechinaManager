# CLAUDE.md — מערכת מכינת ניר עוז

הנחיות מחייבות לעבודה על המאגר הזה. העקרונות כאן אינם סגנון — כל אחד מהם
נובע מאילוץ תשתית או מהתחייבות שניתנה למשתמשים. חלקם נראים שרירותיים ויש
מאחוריהם סיפור; לפני שסוטים מאחד מהם — לשאול.

---

## מה זו המערכת

ניהול מלאי למטבח מכינת ניר עוז. רצה בייצור ומשמשת את המטבח מדי יום.

- **לקוח:** React + Vite
- **שרת:** פונקציות serverless על Vercel, בתיקיית `api/`
- **נתונים:** monday.com בלבד. אין בסיס נתונים אחר.
- **ייצור:** https://mechina-kitchen.vercel.app

**בפיתוח:** ענף `lessons`. הענף `main` הוא מה שרץ במטבח — לא נוגעים בו.
שילוב נעשה רק דרך Pull Request שרועי מאשר.

---

## ⚠ מגבלת 12 הפונקציות — לקרוא לפני הוספת קובץ ל-`api/`

תוכנית Vercel הנוכחית מגבילה ל-**12 פונקציות**. כל קובץ `.js` ישירות בתוך
`api/` ששמו **אינו** מתחיל בקו תחתון נספר.

**תפוסות כרגע 6:** `kitchen.js` (ציוד המטבח), `auth.js`, `students.js`,
`attendance.js`, `lessons.js`, `container.js`. **נותרו 6.**

חריגה מהמגבלה **שוברת את הדיפלוי של כל המערכת** — לא רק את התכונה החדשה.
האפליקציה במטבח לא תעלה.

### הדפוס המחייב

קובץ נספר אחד לכל תחום, שמנתב פנימית לפי `?action=`. הלוגיקה יושבת במודולי
`_` שאינם נספרים.

```
api/lessons.js          ← נספר. נתב דק בלבד.
api/_lessons-list.js    ← לא נספר. לוגיקה.
api/_lessons-create.js  ← לא נספר. לוגיקה.
```

הדוגמה לחיקוי היא `api/attendance.js` — נתב דק שמייבא מודולי `_`
וממפה אותם לפי `?action=`:

```js
import { router } from "./_router.js";
import day from "./_attendance-day.js";
// ...
export default router({ day, mark, requests, decide, train });
```

כתובת נקודת קצה משתקפת בשלושה מקומות בלבד: הנתב, `src/api.js`
(הדלת היחידה), ו-`tools/api-snapshot.mjs`.

### הנתב לא נוגע באימות

`api/_router.js` בוחר handler לפי `?action=` ומחזיר 404 לפעולה לא מוכרת. הוא
**אינו** עוטף באימות. כל מודול מביא את ההגנה שלו:

- `_students-list.js` עטוף ב-`withAuth(handler, { manager: true })` ולכן מחזיר
  403 לתורן גם דרך הנתב.
- `_login.js` ו-`_logout.js` אינם עטופים כלל — logout חייב להצליח גם למי
  שהעוגייה שלו פגה, אחרת הוא נשאר תקוע עם עוגייה מתה.

**כשמוסיפים action חדש — לוודא שהמודול שלו עטוף.** שכחה כאן פותחת נקודת קצה
לא מוגנת, והנתב לא יתפוס את זה.

---

## העקרונות

### 1. monday היא מקור האמת

אין בסיס נתונים אחר. הכול חי בלוחות monday ונקרא בזמן ריצה.

**המשמעות המעשית:** מה שאפשר להגדיר בלוח — מוגדר בלוח, לא בקוד. מנהל המכינה
צריך לשנות משימה, ניסוח, שיבוץ או תוכן שיעור **בלי לכתוב קוד ובלי דיפלוי**.

זה מעצב ארכיטקטורה, לא רק אחסון. דוגמה: **המפתח** — כמה צריך להיות מכל פריט
ציוד — נשמר בלוח ולא בקוד, כי המכינה משנה אותו מדי חודש. שינוי שם או כמות
בלוח משפיע מיד, בלי דיפלוי.

**לפני שמקבעים ערך בקוד — לשאול אם מקומו בלוח.**

### 2. סודות רק בסביבה

`MONDAY_TOKEN` ו-`SESSION_SECRET` נקראים מ-`process.env` בלבד. לעולם לא בקוד,
לא בהערה, לא בקומיט. מקומית ב-`.env` (חסום ב-`.gitignore`), ב-Vercel
ב-Environment Variables.

מזהי הלוחות **כן** מקובעים בקוד, ב-`shared/`. הם אינם סוד — בלי טוקן אי אפשר
לגשת אליהם.

**שני קבצים אסור לייבא מ-`src/` לעולם:**

| קובץ | למה |
|---|---|
| `api/_monday.js` | קורא את `MONDAY_TOKEN` |
| `shared/auth-board.js` | מוביל לעמודת קודי הכניסה |

ייבוא מהם לקוד לקוח מכניס את הנתון לחבילה שנשלחת לדפדפן. שני הקבצים נושאים
אזהרה בראשם — לא להסיר אותה.

אם סוד נכנס להיסטוריית גיט, לא מספיק למחוק אותו בקומיט הבא — צריך להחליף את
כל המפתחות של האפליקציה.

### 3. האימות בשרת, לא בלקוח

כל נקודת קצה עטופה ב-`withAuth` מ-`api/_session.js`. השרת קובע מי המשתמש לפי
העוגייה החתומה — **לא לפי מה שהדפדפן מצהיר בגוף הבקשה**.

```js
export default withAuth(handler);                      // כל מחובר
export default withAuth(handler, { manager: true });   // מנהל בלבד
```

`handler` מקבל `(req, res, session)`, ו-`session` הוא מקור האמת על הזהות:
`{ kind, itemId, name, isManager }`.

**בדיקת תפקיד בצד הלקוח היא נוחות תצוגה בלבד, לא הגנה.** מי שקורא ל-endpoint
ישירות עוקף אותה.

נקודות עדינות בסשן, לא לשבור:
- הקוד עצמו לעולם לא נכנס לעוגייה — רק טביעת אצבע (HMAC) שאי אפשר לשחזר ממנה.
- בכל בקשה מושווית הטביעה לקוד הנוכחי בלוח. הוחלף הקוד, השורה כובתה או
  נמחקה — הסשן נפסל מיד. **סיבוב קוד חייב להמשיך לנתק.**
- שם החניך (`session.name`) הוא **מוצהר ולא מאומת** — הוא נבחר מרשימה ומשמש
  לתפעול בלבד. אין להשתמש בו כבסיס להרשאה. רק `isManager` מאומת.

### 4. נתון רגיש מסונן בשרת, לא בתצוגה

**אין כרגע מחירים במערכת** — הם נמחקו יחד עם קטלוג המלאי. העיקרון נשאר, והוא
יחזור לרלוונטיות ברגע שתיכנס עמודת עלות ללוח הציוד.

הסינון **בשרת** — הנתון לא יוצא החוצה, לא רק מוסתר בתצוגה. מי שיפתח כלי פיתוח
או יקרא ל-endpoint ישירות לא ימצא אותו.

**הדפוס המועדף הוא מיפוי מפורש ולא השמטה**, כדי שעמודה חדשה בלוח לא תדלוף
בטעות:

```js
rows.map((r) => ({ id: r.id, name: r.name, kind: r.kind, active: r.active }))
```

הרף הגבוה ביותר הוא קוד הכניסה: **הוא לעולם לא יוצא מהשרת — גם לא למנהל.**

**כל נקודת קצה חדשה שמחזירה עלויות או שורות משתמשים — לשמור על הדפוס.**

### 4א. בקשת יציאה עוברת שני שלבים

המדריך של הקבוצה **ממליץ**, וראש המכינה **מכריע**. שני השלבים באותה נקודת
קצה (`?action=decide`), ומי המחליט נקבע **מהשלב שהבקשה נמצאת בו** — לא ממה
שהדפדפן שולח.

השרשרת עוברת שלושה לוחות, וכולה נתון:

```
שיבוצים   החניך משובץ ל"קבוצת נעם"
הגדרות    לקבוצה יש עמודת "מוביל" ובה "נעם"
משתמשים   למשתמש "נעם — מדריך" יש תפקיד "מדריך"
```

**אין שם מקובע בקוד.** המכינה מחליפה מדריך, מעבירה חניך בין קבוצות או ממנה
ראש מכינה אחר — הכול בלוחות, בלי דיפלוי. גם ראש המכינה עצמו הוא עמודה
(`AUTH_COLS.role`) ולא זהות בקוד: היוצא לחופשה מסמן את הסגנית ואיש לא נתקע.

נקודות שלא לשבור:

- **השלב נגזר ולא נשמר** (`requestStage` ב-`shared/mechina-boards.js`). אין
  עמודת "שלב" שיכולה לסתור את הנתונים.
- **גם דחיית מדריך עוברת הלאה.** היא המלצה, לא הכרעה — ראש המכינה רואה את כל
  התמונה ורשאי להפוך אותה.
- **ראש המכינה אינו כפוף לשלב.** הוא מכריע גם לפני שהמדריך המליץ, וההכרעה
  שלו סוגרת. השלב הראשון הוא סדר עבודה, לא שער. ההמלצה שדולגה נשארת ריקה —
  ולא מומצאת בדיעבד.
- **שורת ההיעדרות נוצרת בהכרעה הסופית בלבד.** המלצה אינה נוגעת בלוח השנה.
- **חניך בלי שיבוץ לקבוצה מתחיל אצל ראש המכינה.** שיבוץ חסר לא יתקע בקשה.
- **שאר אנשי הצוות רואים כל בקשה ואת השלב שלה, ואינם מכריעים.** מנהל אינו
  ראש מכינה. הפעמון וכרטיס תשומת הלב הולכים לפי `canDecide` בלבד — התראה על
  משהו שאי אפשר לגעת בו היא רעש.
- **⚠ החניך אינו רואה את השלבים בכלל.** יוצאת לו תשובה אחת — ממתין, מאושר או
  נדחה. לא מי המדריך שלו, לא מה הוא המליץ, ולא היכן הבקשה עומדת. שני מיפויים
  מפורשים ונפרדים ב-`_requests.js`, ולא השמטה, כדי ששדה חדש לא ידלוף לצד
  החניך מעצמו. הסיבה אינה טכנית: המלצה היא שלב פנימי, וחניך שיראה "המדריך
  המליץ לדחות" ינהל משא ומתן על החלטה שאינה סופית.

### 4ב. לדירוג מרצה יש שני מקורות, והם לא מתערבבים

```
students  ממוצע חי של הצבעות החניכים למפגש
manual    ציון שאיש צוות הזין ביד
```

31 מחוות הדעת של מחזור א׳ נכתבו לפני שהיה מנגנון דירוג, והציון קיים רק בראש
של מי שהיה שם. `LESSON_COLS.evals.manual` נותן להזין אותו בדיעבד.

**⚠ עמודה נפרדת ולא כתיבה לתוך `avg`.** ממוצע של 23 חניכים וציון שמדריך אחד
זכר הם שני דברים שונים, וברגע שהם יושבים באותו שדה אי אפשר עוד להבדיל.
נקודת הקצה מחזירה `score` (המספר להצגה) לצד `source` — **מסך שיציג את `score`
בלי `source` מוחק בדיוק את ההבחנה שבגללה יש שתי עמודות.** בממשק המקור מסומן
בכוכב: להצבעות יש ★, לדירוג הידני אין.

**הצבע הוא לפי הציון, לא לפי המקור** (`scoreTone`): 9+ ירוק · 8 כחול · 6 ענבר ·
מתחת ל-6 חימר. ברשימה של 32 מרצים המספר לבדו אינו נקרא.

30 מהדירוגים חולצו מטקסט חוות הדעת עצמו (`scratchpad/extract-ratings.mjs`),
שנכתב בשני ניסוחים — `דירוג:10` ו-`8/10`. אור הלר קיבל `11/10` ונשאר ריק:
זו מחמאה ולא ציון, ואין לנחש עבורה.

**הצבעות גוברות תמיד.** חוות דעת עם שניהם מציגה את החניכים, והידני נשאר
לצידה ומסומן — לא נמחק ולא מנצח.

### 4ג. שפת העיצוב — עומק, לא קווים

`src/styles.js` נגמר בבלוק **שכבת ההרמה** שדורס את מה שמעליו. הוא יושב בסוף
בכוונה: אפשר להסיר אותו בחתיכה אחת אם משהו נשבר.

העיקרון: אותה זהות חמה — קרם, נייבי, חימר — אבל **משטח מוגדר על ידי צל ולא
על ידי מסגרת**. מסגרת מפרידה, צל מרים.

| טוקן | מה |
|---|---|
| `--r-lg` `--r-md` `--r-sm` | 20 · 14 · 10 |
| `--sh-1` `--sh-2` | צל דו-שכבתי: קו צמוד שמגדיר קצה + פיזור רך שמרים |
| `--ease` | `cubic-bezier(.22,1,.36,1)`, 120ms על כל דבר לחיץ |
| `--t1`..`--t8` + `-s` | שמונה גוונים, כל אחד בדיו וברמת מילוי רכה |

**⚠ הצל בשתי שכבות ולא באחת.** שכבה אחת נותנת או קצה חד או ערפל, לא את
שתיהן. והמסגרת הוחלשה ולא הוסרה — בלעדיה משטח לבן על קרם בהיר מאבד את הקצה
באור שמש ובמסכים חיוורים.

**הגוון נגזר מהשם ואינו נשמר** (`tone()` ב-`Placements.jsx`): גיבוב יציב של
שם הפריט מודולו 8. ענף חדש שיתווסף בלוח מקבל צבע מעצמו — **בלי דיפלוי ובלי
עמודת צבע לתחזק**. אותו שם מקבל תמיד אותו צבע, ולכן הרשימה לא מתחלפת בכל
טעינה.

`.tile` הוא אריח האייקון — ריבוע מעוגל בגוון עם האייקון בדיו שלו. `--t`
ו-`--t-s` נקבעים על ההורה (`.tone-N`), כדי שגם הכותרת, הפס והתגית באותו
כרטיס יירשו את אותו צבע.

**⚠ גוון התחום וצבע המצב הם שני דברים.** אדום וירוק שמורים למצב — חוסר,
חריגה, תקין. אילו גם האריח היה מאדים, מסך עם שתי בעיות היה נראה כמו אזעקה.

**רכיבים משותפים שנוספו:**

| רכיב | מתי |
|---|---|
| `.band` | רצועת סיכום כהה. שלושה מספרים שמסכמים מסך, לפני הרשימה |
| `.tile` | אריח אייקון בגוון |
| `.mini-bar` | כמות מול יעד כפס — מלאי מול מפתח, משובצים מול מכסה |
| `.empty .e-ico` | מצב ריק עם אייקון רגוע |
| `.skel` | שלד טעינה |

**⚠ רצועה ואריחים באותו מסך הם כפילות, לא הדגשה.** במסך התקלות הוסרו ארבעת
אריחי המספרים אחרי שהרצועה נכנסה — הם הציגו בדיוק את אותם מספרים ולא היו
לחיצים. במסך החניכים הרצועה מדברת על השנה והאריחים על היום, ולכן שניהם
נשארו.

**⚠ אחוז מוצג רק כשיש מספיק ימים מאחוריו.** בתחילת שנה, כשסומן יום אחד,
"0% נוכחות" הוא מספר נכון חשבונית ושקרי במשמעותו — והוא הדבר הראשון שהחניך
רואה על עצמו. הסף הוא **ימי לימוד שסומנו**, לא סכום ימי-חניך: עם 33 חניכים
סכום ימי-החניך עובר כל סף כבר אחרי יום אחד.

**⚠ `.kx:not(.kx-login)` ולא `.kx`.** מסך הכניסה מגדיר לעצמו רקע תמונה באותה
דרגת ספציפיות, ושכבת ההרמה יושבת אחריו בקובץ — כלל רקע על `.kx` גובר עליו
ומוחק את התמונה. **כל כלל חדש על `.kx` שנוגע ברקע חייב את החריגה הזו.**

**⚠ הגוון בגאנט נגזר משם האירוע, אבל שבת וחג לא.** להם משמעות קבועה ולכן
צבע קבוע. בלי זה לוח חודש שלם נראה מונוכרומטי — אי אפשר להבחין בין סמינר,
מסע ויום חופש בלי לקרוא כל תווית.

**⚠ הנפשת כניסה רק במסך הבית** (`.stat-grid`, `.navgrid`). רשימה של שלושים
ימי תקציב שנכנסת בהנפשה נראית איטית, לא חיה. `prefers-reduced-motion` מבטל
הכול.

### 4ד. כמות ציוד נשלחת כהפרש, לא כסך הכול

`PUT ?action=equip { itemId, delta }` מוסיף לכמות; `{ qty }` דורס אותה.
**ביום-יום משתמשים ב-`delta`.**

זו אינה סתירה לעיקרון 5 ("לשלוח את המצב הרצוי, לא הפוך") אלא בדיוק ההפך
שלו: שני אנשים שמכניסים סחורה באותה דקה, האחד 12 והשני 8, צריכים לקבל 20.
אילו כל אחד שלח את הסך הכול, השני היה מוחק את הראשון. **"הפוך" מסוכן כי
שתי כוונות זהות מבטלות זו את זו; "הוסף" בטוח כי שתי כוונות זהות מצטברות.**

**⚠ הכמות היא טקסט חופשי** ("40 חבילות של 10"), ולכן חיבור עובר דרך
`qtyAdd` ב-`shared/par.js`, שמחליף **רק את המספר הראשון** ומשאיר את תיאור
האריזה. מי שיחליף את שני המספרים יהפוך פריט אחד לשני פריטים שונים.
הכמות לא יורדת מתחת לאפס — מינוס שתי חבילות אינו מצב שקיים במחסן, והוא
היה מחלחל למפתח ולרשימת הקניות.

### 4ה. ליום בתקציב יכולים להיות שני סוגים

`{ date, type, type2 }` — "שגרה + אחר" הוא יום שגרה שקרה בו עוד משהו, ושני
הסכומים **מתחברים** (`dayCost` ב-`shared/budget-boards.js`).

לפני כן הייתה רק הברירה בין סוג יחיד לבין מחיר ידני שדורס הכול — ואז נעלמה
הסיבה שבגללה היום יקר. עכשיו נשמר גם מה היום היה וגם מה נוסף לו.

**⚠ הסוג הנוסף הוא חריגה בלבד ואינו נגזר מהגאנט.** ⚠ המחיר הידני עדיין
דורס את שניהם, למקרה שבו שום צירוף אינו מתאר את היום. ⚠ אותו סוג פעמיים
נדחה — זו הכפלה שקטה של אותו יום.

### 4ו. גרפים — הצבע מחושב, לא נבחר בעין

**⚠ כל פלטה קטגורית עוברת ולידציה לפני שהיא נשלחת.** צמד הענבר והחימר של
המערכת (`#8A5A1E` ו-`#9E3626`) נראה שונה על המסך ונכשל בבדיקה: הפרש **2.6**
בלבד בעיוורון צבעים, ו-**9.0** אפילו בראייה מלאה. שני מצבים שונים שנראים
אותו דבר.

הפלטה שעוברת, מול שני הרקעים של האפליקציה:

```
בתקציב      #177A45
קרוב לתקרה  #B08400
חריגה       #B02A1F
```

**⚠ צבע לעולם לא לבדו.** לכל מצב יש מילה, ולחריגה יש גם סימן `!` על העמודה
— כדי שמי שאינו מבחין בין ירוק לאדום, וגם מי שמדפיס בשחור-לבן, יידע מה
קורה.

**⚠ ציר אחד.** תקציב ונקנה הם שניהם ₪ ולכן על אותו סולם, והנקנה מצויר
**בתוך** מסלול התקציב. פס שגולש מהמסלול אינו קריא — החריגה מסומנת בנפרד.

**⚠ הניצול נמדד מול הקניות ולא מול הסך הכול.** הקייטרינג הוא חוזה — הוא לא
"מנוצל", הוא פשוט עולה. תקציב הקניות הוא הסכום שיש מולו שיקול דעת.

### 4ז. נוכחות אימונים היא אמת נפרדת מהנוכחות היומית

`api/_training-summary.js`. חניך יכול להיות נוכח במכינה ולהיעדר מהאימון,
ולהפך — ולכן `training` הוא שדה נפרד ולא חלק מ-`summary`.

- **⚠ מטבח אינו היעדרות.** תורן מטבח לא היה באימון כי המכינה שלחה אותו
  למטבח; לספור אותו כנעדר היה מעניש אותו על תורנות. הוא נספר בנפרד ואינו
  במכנה של האחוז.
- **⚠ "לא סומן" הוא מצב שלישי.** מפגש שהחניך אינו באף אחת משלוש הרשימות שלו
  אינו נספר לו — אחרת מפגש שאיש לא סימן היה נראה כמו מפגש שכולם נעדרו ממנו.
- אחוז מוצג מארבעה אימונים ומעלה, כמו בנוכחות היומית.

### 4ח. לוח השיעורים הוא תצוגה, לא מאגר

`?action=board` מחזיר את השיעורים בשבועיים לכל כיוון. **הכול נשלף מהגיליונות
ומהמפגשים שלהם, וכל סימון חוזר לאותו מקום.** אין כאן שום נתון משלו, וגיליון
שישתנה משנה גם כאן.

**⚠ הרשימה השנייה היא "טרם דווח" ולא "לא התקיים".** `happened === null` הוא
מצב שלישי, וזו כל הסיבה שהמסך קיים. גיליון כבוי אינו מוצג — מפגשיו שנשארו
בלוח אינם מטלה של אף אחד.

### 4ט. ⚠ ברירת מחדל שקטה על קלט ריק היא באג בהמתנה

`areaOf("")` החזיר `"אוכל"` בשקט. זה נשאר נכון כל עוד המסך תמיד ביקש תחום
מפורש — וברגע שנוסף המסך המאוחד, שקורא בלי `area`, ההתנהגות הזו **הסתירה
את כל 16 פריטי החד״פ**. הם היו בלוח כל הזמן ופשוט לא הוחזרו.

היום יש שלוש תשובות ולא שתיים: שם תחום · `ALL` (ריק) · `null` (לא מוכר).
**קלט ריק שמשמעותו "הכול" חייב להיות ערך משלו, לא ברירת מחדל של אחד
הערכים.**

### 4י. סימון קנייה ותפקיד הוא אופטימי

הסימון מוצג מיד והבקשה נשלחת ברקע — לחיצה שממתינה לתשובת monday ואז טוענת
מחדש את כל הרשימה לקחה שנייה וחצי, וזה מרגיש כאילו לא נקלטה.

**⚠ ובכישלון חוזרים אחורה ואומרים.** סימון אופטימי שנשאר על המסך אחרי
שהשרת דחה אותו הוא שקר, לא נוחות.

### 4יא. תיאורי התפקידים הם טקסט של המכינה

`src/roles-info.js`. הועתק כלשונו ולא נערך, קוצר או "שופר" — מי שיערוך,
לוודא מול מי שכתב.

**⚠ `desc` ו-`perms` הם שני דברים.** התיאור הוא מה שהאדם עושה; `perms` הוא
מה שהמערכת פותחת לו בפועל. תפקיד יכול לשאת אחריות שאין לה ביטוי במסך,
וההבחנה חייבת להישאר ברורה.

המפתח הוא שם התפקיד **בדיוק** כמו בעמודת התפקידים בלוח. תפקיד שיתווסף בלוח
ואין לו תיאור יוצג בלי תיאור — ולא ייעלם.

### 4יב. מנה נשמרת לכמות אנשים אחת

`shared/dishes.js`. אחראי המטבח כותב "מצרכים עבור 35", וכל כמות אחרת היא
הכפלה. אחסון של כל וריאציה היה מייצר עותקים שמתיישנים בנפרד.

- **⚠ `qty === null` הוא "לפי הטעם", לא אפס.** מצרך כזה אינו מוכפל ואינו
  נבדק מול המלאי — אין מה להכפיל בו.
- **⚠ האיחוד לפי שם *ויחידה* ביחד.** "עגבניות 3 קילו" ו"עגבניות 5 יחידות"
  אינם אותו דבר, וחיבור שלהם היה מייצר מספר חסר פשר.
- **⚠ הבדיקה מול המלאי היא התראה, לא חסימה.** ההתאמה בין שם מצרך לשם פריט
  היא חלקית ובשני הכיוונים, ולכן יש **שלושה** מצבים: יש · חסר · **לא נמצא**.
  "לא נמצא" אינו "חסר" — ייתכן שהוא במלאי בשם אחר, וזו בדיקה של אדם.
- עיגול כלפי מעלה: 6 קילו ל-35 הם 2.91 ל-17 ולא 2.9. במטבח עדיף קצת יותר.

### 4יג. ⚠ הקובץ של משרד החינוך לא נכנס למערכת

הרשימה שהמכינה שלחה מכילה **תעודות זהות, מיילים פרטיים, קופת חולים ומידע
רפואי** של 24 בוגרים. לוח הבוגרים נועד לסטטיסטיקה על שירות צבאי, ואין לאף
אחד מהשדות האלה מקום בו.

הבוגרים נזרעו מהרשימה שנמסרה בשיחה — שם, תפקיד, תאריך גיוס ותאריך לידה.
**מי שיוסיף שדות ללוח הזה — לשאול קודם למה הם נחוצים.**

### 4יד. השיחה האישית שייכת למדריך של החניך

שלושת תאריכי השיחה נקבעים על ידי **המדריך של הקבוצה** ולא על ידי כל איש
צוות. השיחה האישית היא בין החניך למדריך שלו, ומי שקובע לה תאריך הוא מי
שיושב בה.

**⚠ ראש המכינה אינו נחסם** — אותו כלל כמו בבקשות היציאה. חניך שאינו משובץ
לקבוצה, או מדריך שיצא לחופשה, לא יכולים להשאיר את השיחה תקועה בלי שאיש
יוכל לקבוע לה תאריך.

**⚠ `canEditTalks` מוחזר מהשרת ומשקף את הכלל הזה.** מסך שיציע עריכה למי
שאינו רשאי מקבל 403 אחרי שהמשתמש כבר הקליד — הכפתור חייב לדעת מראש.

### 4טו. ⚠ `withAuth(handler)` חוסם חניכים

בלי אף אפשרות, `withAuth` מרשה לכל **מחובר** פרט לחניכים. תפריט הארוחות
נכתב כך בטעות ו"נפתח לכולם" חסם בדיוק את הקבוצה שהוא נועד לה.

`{ student: true }` הוא מה שפותח אותו לחניכים. ההבחנה בין קריאה לכתיבה
נעשית **בתוך** ה-handler לפי `req.method`, כי `withAuth` שומר על הזהות ולא
על השיטה.

### 5. אין מעקב אישי על עבודת התורנים

**לוח המשימות השבועי הוסר**, אבל ההתחייבות שמאחוריו לא בוטלה — היא ניתנה
לחניכים ולא הייתה החלטה טכנית. **אין להוסיף שדה שמזהה מי ביצע מה**, גם אם זה
נראה שימושי ואפילו אם מבקשים. מי שיבנה מחדש לוח משימות — הכלל חל עליו.

שני לקחים מהמנגנון שהיה, ששווים לכל פעולה משותפת שתיבנה:

- **לשלוח את המצב הרצוי, לא "הפוך".** שני תורנים שלוחצים כמעט יחד שולחים אותה
  כוונה ומקבלים אותה תוצאה; בקשת "הפוך" הייתה גורמת לשנייה לבטל את הראשונה.
- **עדיף פשטות על העמסת שדות ופעולות.**

### 6. כשל טעינה נראה אחרת מ"אין נתונים"

באג שחי יומיים בייצור: הקטלוג חזר ריק, והמסך נראה בדיוק כמו מטבח ריק.

`src/storage.js` מסמן `loadFailed` ומוצג באנר אדום. **לשמור על העיקרון בכל קוד
חדש** — משתמש חייב לדעת שהחיבור נכשל, ולא לחשוב שהמלאי אזל או שאין שיעורים.

מצב ריק אמיתי ומצב כשל הם שני מסכים שונים. תמיד.

### 7. כל קריאה לשרת דרך `src/api.js`

זו הדלת היחידה. **אין לכתוב `fetch` ישירות בשום מקום אחר ב-`src/`.**

הבאג מסעיף 6 נגרם בדיוק מזה: `storage.js` החזיק ארבע כתובות מקובעות, והן לא
עודכנו כשנקודות הקצה אוחדו. מקום אחד שמכיר כתובות הוא מה שמונע חזרה של זה.

`src/api.js` מטפל מרכזית ב-401 (`setUnauthorizedHandler`) ומחזיר את המשתמש
למסך הכניסה עם הסבר. עקיפה של השכבה מאבדת גם את זה.

**תכונה חדשה → מוסיפים מתודה ל-`api` ב-`src/api.js`, וקוראים לה מהמסך.**

---

## מבנה הפרויקט

```
api/            פונקציות השרת
  kitchen.js      ציוד המטבח: אוכל וחד״פ    ← נספר
  auth.js         כניסה, יציאה, זהות        ← נספר
  students.js     חניכים, פרופיל, תפקידים   ← נספר
  attendance.js   נוכחות ובקשות יציאה       ← נספר
  lessons.js      שיעורים, גאנט, דירוגים    ← נספר
  container.js    ציוד מכינה: מכולה, ניקיון ← נספר
  _router.js      נתב פנימי לפי ?action=
  _session.js     withAuth, עוגייה חתומה, השהיה גוברת
  _monday.js      לקוח GraphQL. קורא את הטוקן — שרת בלבד.
  _items.js       ארבע פעולות פריט גנריות, משותפות לשני תחומי הציוד
  _cache.js       מטמון קצר משותף
  _test-date.js   שער ?date=
  _*.js           שאר הלוגיקה. לא נספרים.

shared/         מזהי לוחות וקוד משותף ללקוח ולשרת
  kitchen-boards.js    ציוד המטבח: תחומים ותוויות
  kitchen-ids.js       ⚠ מחולל — נכתב על ידי tools/seed-kitchen.mjs
  placements.js        שיבוצי חניכים: קטגוריות ותקופות
  placements-ids.js    ⚠ מחולל — נכתב על ידי tools/seed-placements.mjs
  safety-board.js      בטיחות ותקלות
  safety-ids.js        ⚠ מחולל — נכתב מכפתור ההקמה שבמסך
  faults-board.js      תקלות ובעיות
  faults-ids.js        ⚠ מחולל — נכתב מכפתור ההקמה שבמסך
  container-boards.js  ציוד המכינה
  par.js               חישוב המפתח, משותף לשני תחומי הציוד
  auth-board.js        הרשאות ⚠ שרת בלבד
  mechina-boards.js    חניכים, נוכחות, בקשות
  lessons-boards.js    שיעורים
  week.js              חישוב השבוע בשעון ישראל

src/
  App.jsx         שלד בלבד: כניסה, ניווט, מסגרת
  Equipment.jsx   מסך הציוד — משותף למטבח ולמכולה
  Kitchen.jsx     הגדרת תחום המטבח (אוכל · חד״פ)
  Container.jsx   הגדרת תחום המכינה (מכולה · ניקיון)
  Mechina.jsx     נוכחות, חניכים, תפקידים
  Placements.jsx  שיבוצי חניכים — מסך המנהל
  Safety.jsx      בטיחות ותקלות — מנהל ואחראי בטיחות
  Faults.jsx      תקלות ובעיות — מנהל ואב בית
  Lessons.jsx     שיעורים
  api.js          כל הקריאות לשרת — הדלת היחידה
  Login.jsx       מסך הכניסה
  styles.js       כל העיצוב
  excel.js        ייצוא לאקסל
  testDate.js     קריאת ?date= בצד הלקוח

tools/          סקריפטים לתחזוקה ובדיקה
  api-snapshot.mjs       חוזה נקודות הקצה
  setup-local.mjs        הקמת עמדת פיתוח
  seed-kitchen.mjs       הקמת לוחות המטבח וזריעת המלאי
  kitchen-seed-items.mjs המלאי הפותח
```


---

## לוחות monday

| לוח | תפקיד | מזהים |
|---|---|---|
| מטבח – ציוד | אוכל וחד״פ: כמות, סוג, מפתח | `shared/kitchen-ids.js` |
| מטבח – קניות | רשימת הקניות של המטבח | `shared/kitchen-ids.js` |
| ציוד מכולה | מכולה וניקיון | `shared/container-boards.js` |
| קניות מכולה | רשימת הקניות של המכולה | `shared/container-boards.js` |
| מצבת חניכים | החניכים, מגדר, מוביל | `shared/mechina-boards.js` |
| לוח שנה, היעדרויות, בקשות | נוכחות ויציאות | `shared/mechina-boards.js` |
| גיליונות, מפגשים, דירוגים | שיעורים | `shared/lessons-boards.js` |
| שיבוצים – הגדרות | ענפים, סדרות, ועדות, קבוצות, מכסות | `shared/placements-ids.js` |
| שיבוצים – שיבוץ | מי משובץ לאן ובאיזה סמסטר | `shared/placements-ids.js` |
| בטיחות ותקלות | דיווחי אירועי בטיחות ⚠ מנהל ואחראי בטיחות | `shared/safety-ids.js` |
| תקלות ובעיות | דיווח פתוח לכל חניך · מעקב ⚠ מנהל ואב בית | `shared/faults-ids.js` |
| משתמשים והרשאות | קודי כניסה, רשימת חניכים ותפקידי צוות | `shared/auth-board.js` |
| בוגרים | לאן התגייסו ומתי ⚠ צוות בלבד | `shared/extras-ids.js` |
| אירוח קבוצות | תיאום, תדריך והחזרת מבנים ⚠ אחראי בטיחות | `shared/extras-ids.js` |
| השאלת ציוד | מה יצא, מתי חוזר, והאם חזר ⚠ אחראי מכולה | `shared/extras-ids.js` |
| מנות · תפריטים | מצרכים לכמות אנשים אחת ⚠ אחראי מטבח | `shared/extras-ids.js` |

לכל תחום קובץ `shared/` משלו, והוא היחיד שמכיר את מזהי העמודות שלו. עמודה
שהוחלפה ב-monday → משנים שורה אחת שם. **לא לפזר מזהי עמודות בקוד.**

⚠ `shared/kitchen-ids.js` ו-`shared/placements-ids.js` הם **קבצים
מחוללים** — נכתבים על ידי סקריפטי ה-seed שיוצרים את הלוחות
(`npm run seed:kitchen`, `npm run seed:placements`). לא לערוך ביד.

התוויות בקוד חייבות להיות זהות **בתו** לתוויות שבלוחות — שימו לב לגרשיים
עבריים (`״`, `׳`) מול רגילים. `"חד״פ"` ו-`"חד\"פ"` הן שתי תוויות שונות ב-monday,
ותווית שלא נמצאה גורמת לקריאה להיכשל.

⚠ `create_labels_if_missing` נשאר `false` בכל הקריאות, בכוונה: תווית חסרה
פירושה טעות בקוד או שינוי בלוח, ועדיף שהקריאה תיכשל ברעש מאשר שייווצרו
תוויות כפולות בשקט.

**⚠ חריג יחיד: עמודת "סוג היום" בלוח ימי התקציב** (`setColsOpen` /
`createItemOpen` ב-`_kitchen-budget.js`). שם התוויות אינן נתון של הקוד אלא
של המכינה — סוגי היום חיים בלוח "סוגי יום" והמכינה מוסיפה שם סוג בלי
דיפלוי, בעוד עמודת הסטטוס היא רשימה שנייה של אותם שמות שאין שום דבר
שמסנכרן אליה. התוצאה הייתה שהעמודה נשארה עם `Working on it / Done / Stuck`
וכל ניסיון לכפות סוג יום נפל ב-502 עם ההודעה חסרת התוכן "פעולת התקציב
נכשלה".

אין כאן סכנת זבל: הערך נבדק מול לוח סוגי היום **לפני** הכתיבה, ולכן היחיד
שיכול להיווצר הוא שם של סוג שקיים באמת.

**לוח חדש:** קובץ `shared/` נפרד משלו, באותו דפוס. לא להרחיב קובץ של תחום אחר.

---

## זמן

השבוע מתחיל ביום ראשון 00:00 ומסתיים בשבת 23:59, **בשעון `Asia/Jerusalem`** —
לא בשעון השרת. Vercel רצה ב-UTC, ובלי ההמרה מוצאי שבת בישראל נחשב לשבוע הקודם.

כל חישוב תאריך עובר דרך `shared/week.js`, ו-`israelToday()` שב-`_attendance-data.js`
הוא שמחזיר את תאריך היום — כך שרשימת קניות שנוצרת בערב לא נרשמת ליום הבא.

**אין להשתמש ב-`new Date()` גולמי לחישוב יום או שבוע.**

---

## פרמטר `?date=`

בפיתוח מקומי בלבד: `localhost:5173/?date=2026-08-13` גורם למסך להתנהג כאילו זה
התאריך הזה. שימושי לבדיקת ימים אחרים ושבועות עתידיים.

**ההגבלה יושבת בשרת** (`api/_test-date.js`), לא בלקוח — בדיקה בצד הלקוח ניתנת
לעקיפה בקריאה ישירה. בכל דיפלוי (production **וגם** preview) הפרמטר מתעלם
לחלוטין: בלי שגיאה, בלי חיווי. באוויר אין אפילו אימות פורמט, כדי שההבדל בין
הסביבות לא יהיה ניתן לזיהוי מבחוץ.

**לא להסיר את ההגבלה ולא להרחיב אותה ל-preview.**

נקודת קצה חדשה שתלויה בתאריך — לקרוא `parseTestDate` מאותו מודול, לא לממש שער
משלה.

---

## בדיקות

### `tools/api-snapshot.mjs`

מצלם את התשובות של כל נקודות הקצה — כתורן, כמנהל, ובלי התחברות.

```bash
node --env-file=.env tools/api-snapshot.mjs /tmp/after.txt
diff tools/snapshot-before-dev.txt /tmp/after.txt
```

**להריץ לפני ואחרי כל שינוי בשכבת השרת.** תפס תקלות שבדיקה ידנית לא גילתה.

הוא מתעד **מבנה**, לא ערכים. "אפס הפרשים" אומר שלא נשבר כלום — לא שהשינוי
עובד. את זה לבדוק בנפרד.

### `tools/seed-kitchen.mjs`

מקים את לוחות המטבח ב-monday וזורע את המלאי הפותח. **רץ פעם אחת**, ומסרב
לרוץ שוב כדי שלא ייווצרו לוחות כפולים.

```bash
node --env-file=.env tools/seed-kitchen.mjs
```

הוא כותב את המזהים ל-`shared/kitchen-ids.js` — **הקובץ הזה חייב להיכנס
לקומיט**, אחרת הדיפלוי ב-Vercel לא ימצא את הלוחות.

עד שהוא רץ, נקודות הקצה של המטבח מחזירות **503 מפורש** ולא רשימה ריקה. זה
עיקרון 6: כשל הקמה נראה אחרת מ"אין ציוד".

### ⚠ בדיקה יוצרת את הנתונים שלה ומוחקת אותם

`scratchpad/rating-test.mjs` בחר פעם "חוות הדעת הראשונה בלי דירוג", שינה
אותה והחזיר אותה בסוף. פעם אחת ההחזרה לא תפסה, ו-7.5 נשאר על שורה של מרצה
אמיתי. גרוע מזה: כל כתיבה לחוות דעת חותמת עליה את שם הכותב, ולכן הבדיקה
השאירה חתימות על שורות מיובאות שאיש לא נגע בהן.

בדיקה שנוגעת בנתוני המכינה תלויה בכך ש**כל** שלב בניקוי יצליח. בדיקה שיוצרת
שורה משלה, עובדת עליה ומוחקת אותה — לא תלויה בכלום, וגם `uncaughtException`
מוחק אחריה.

`scratchpad/audit-ratings.mjs` נכתב בעקבות זה: הוא משווה כל דירוג בעמודה מול
הדירוג שכתוב בטקסט של אותה חוות דעת, ומדפיס כל סתירה. **להריץ אותו אחרי כל
נגיעה בלוח חוות הדעת.**

### גלישה בסתר

`localStorage` מסתיר באגים — זה מה שהסתיר את הבאג מסעיף 6 במשך יומיים.
**תמיד לבדוק בחלון פרטי** לפני שמסיקים שמשהו עובד.

---

## תקציר לפני קומיט

1. עובדים בענף `lessons`. `main` לא שלנו.
2. לא הוספנו קובץ נספר ל-`api/` בלי לספור. המגבלה 12, תפוסות 6.
3. כל נקודת קצה חדשה עטופה ב-`withAuth`.
4. נתון רגיש מסונן בשרת, במיפוי מפורש.
5. אין שדה שמזהה מי ביצע מה.
6. כשל טעינה או כשל הקמה מוצג אחרת ממצב ריק.
7. כל קריאה לשרת דרך `src/api.js`. אין `fetch` ב-`src/`.
8. מה שאפשר להגדיר ב-monday — הוגדר ב-monday.
9. אין סוד בקוד, בהערה או בקומיט.
10. `shared/kitchen-ids.js` נכנס לקומיט אחרי הקמת הלוחות.
11. נבדק בגלישה בסתר.

### 4טז · רשימות שהצוות מרחיב, ולמה זה חריג

`create_labels_if_missing` הוא **false בכל האפליקציה**. תווית שאינה קיימת
פירושה טעות בקוד, וכישלון רועש עדיף על תוויות כפולות שנוצרות בשקט.

יש שני חריגים מתועדים, ורק שניים:

1. **"סוג היום" בתקציב** — נזרע פעם אחת מול לוח שהגיע עם תוויות ברירת המחדל
   של monday בלבד.
2. **"זרוע" בבוגרים** (`api/_alumni.js`) — הרשימה נקראת מהלוח, והצוות מוסיף
   לה מהמסך.

החריג השני הוא החלטה ולא נוחות. מכינה לא מגייסת לפי טבלה שכתובה בקוד: כל
מחזור מביא מסלול שלא היה, ורשימה סגורה הייתה דוחפת את כולם ל"אחר" תוך
שנתיים — ואז הסטטיסטיקה שהמסך כולו קיים בשבילה מפסיקה לומר משהו.

הכללים שמחזיקים את החריג:

* התווית מגיעה **משדה נפרד ומכוון** ("הוספת סוג תפקיד"), לא מהקלדה חופשית
  בשדה הזרוע. החיכוך הוא העניין: מי שמקליד "סיירת גבעתי" במקום לבחור
  "סיירות חי״ר וקומנדו" מפצל שורה אחת בסטטיסטיקה לשתיים שנראות שונות ואינן.
* הדגל מועבר במפורש — `createItem(..., { labels: true })` — ורק במסלול הזה.
  אסור להעביר אותו בייבוא, בזריעה או בטופס רגיל.
* ⚠ **monday אינה מאפשרת למחוק תווית דרך ה-API.** תווית שנוצרה בטעות
  נמחקת ביד בלוח. זו הסיבה שהחיכוך נמצא לפני היצירה ולא אחריה.

**ההרשאה כאן היא `isManager`, ופירושה כל כניסת צוות** — לא ראש המכינה בלבד
(`_session.js`: `isManager = kind === "manager"`). חניך אינו מגיע למסך
הבוגרים כלל. בחרנו צוות ולא `isHead` מאותו טעם שחוזר בכל האפליקציה: רשימה
שרק אדם אחד יכול להרחיב נתקעת ברגע שהוא בחופשה, וכולם נדחפים ל"אחר".

### 4יז · החזרה היא חלקית מטבעה

ציוד מושאל אינו תיבת טקסט אחת אלא **שורה לכל פריט** (`shared/loan-items.js`),
עם כמות שיצאה וכמות שחזרה. הושאלו 20 כיסאות וחזרו 15 — זה המצב הרגיל, לא
החריג, ועל גוש טקסט אי אפשר היה לענות "מה עוד לא חזר".

* מצב ההשאלה (`בחוץ` / `חזר חלקית` / `הוחזר`) **נגזר מסכום השורות ואינו
  נשמר**. שדה שמור היה מתיישן ברגע שמישהו יעדכן שורה בלוח עצמו.
* הפורמט בלוח קריא לאדם — `כיסאות פלסטיק × 20 · חזרו 15` — כי הלוח הוא מסד
  הנתונים ומישהו יסתכל בו. JSON היה נוח יותר ולא היה נקרא על ידי איש.
* **תאריך הסגירה נחתם מעצמו** כשהפריט האחרון מסומן כחוזר, ונמחק אם התברר
  שלא הכול חזר. אבל **תאריך שאדם הקליד לא נדרס לעולם**.
* החזרה גדולה מהכמות שיצאה היא **שגיאה 400 ולא קיצוץ שקט** — הקלדה כזו היא
  כמעט תמיד טעות שכדאי לראות.
* העמודה הישנה ("הציוד") נשארת קריאה בלבד, לשורות שנכתבו לפני הפיצול.

### 4יח · כסף שנכנס מול כסף שהובטח

בסיכומי האירוח (`api/_hosting.js`) **"התקבל" ו"צפוי" הם שני מספרים ולא אחד**.
אירוח שהתקיים הוא כסף שנכנס; אירוח עתידי הוא הבטחה. מספר אחד שמאחד אותם
מנפח את התמונה בדיוק ברגע שמסתכלים עליה כדי להחליט.

* הסכימה לפי **תאריך תחילת האירוח** ולא תאריך הרישום — זו השאלה שנשאלת.
* אירוח שבוטל אינו נספר כלל, לא בכסף ולא בראשים.
* "תשלום" ו"סכום" הם שני שדות: אירוח בתשלום שהסכום בו טרם סוכם הוא מצב רגיל,
  וסכום 0 היה נראה בדיוק כמו חינם.
* מוחזר גם `unmarked` — כמה אירוחים טרם סומנו. **המספר שאומר כמה מהתמונה חסר
  שייך למסך**, לא רק ללוג.

באותה רוח בבוגרים: אחוזי הפיקוד והקצונה מחושבים **מתוך מי שנשאל**, ולידם
כתוב מכמה. בוגר שטרם נשאל אינו "לא יצא לקצונה", ושיוך שלו למכנה היה מוריד
את האחוז בכל פעם שנוסף בוגר ולא נשאל.

### 4יט · מסך של בעל תפקיד זהה למסך של המנהל

חניך שנושא תפקיד — אחראי מטבח, מכולה, בטיחות, אב בית, לו״ז — רואה את
**אותם מסכים ובאותה צורה** כמו המנהל. לא גרסה מקוצצת.

זה נשבר פעם אחת ובאופן שקט: המנהל קיבל "אוכל וחד״פ" מאוחד, ואצל החניך
נשארו "ציוד אוכל" ו"ציוד חד״פ" נפרדים. אף בדיקה לא תפסה את זה, כי שני
המסכים עבדו — הם פשוט לא היו אותו מסך. באותה הזדמנות התגלה שאחראי מכולה
לא ראה את השאלות הציוד ואחראי בטיחות לא ראה את האירוחים.

הכלל: **`src/App.jsx` ו-`src/Mechina.jsx` הם שתי מעטפות לאותם רכיבים.**
תוספת לניווט של אחת חייבת להיבדק מול השנייה. `scratchpad/parity-test.mjs`
עושה בדיוק את זה — נכנס כחניך עם שלושה תפקידים ומשווה כל מסך למנהל, גם
בסטטוס וגם במבנה התשובה.

⚠ שני הבדלים שנשארים בכוונה: **בוגרים** הם `{manager:true}` (הלוח נושא
תאריכי לידה ומקומות מגורים של מי שכבר אינו במכינה), ותפקידים נקראים
**טרי מהלוח בכל בקשה** — ולכן אינם בתשובת הכניסה. הסרת תפקיד סוגרת את
המסך מיד ולא בכניסה הבאה.

### 4כ · הגאנט הוא המקור, וכל מי ששואל שואל באותו קוד

`shared/gantt-days.js` הוא המקום היחיד שיודע מה יש בגאנט ביום נתון.

הוא נולד מתקלה: תקציב המטבח גזר את סוג היום מהגאנט, ולוח השיעורים לא שאל
את הגאנט **בכלל**. התוצאה — 142 מפגשים שהוצגו כמתקיימים בימים שהגאנט אומר
עליהם יום כיפור, ראש השנה או סופ״ש בית. שני המסכים "עבדו", וכל אחד ענה
תשובה אחרת לאותה שאלה.

* `eventsByDate(gantt)` → תאריך לאירועים · `lessonBlock(index, iso)` →
  `{ blocked, reason, kind }`.
* **מחזיר סיבה ולא רק "לא".** מפגש שנעלם בלי הסבר נראה כמו תקלה; "הגאנט
  אומר: יום כיפור" מאפשר לראות מיד מי מהשניים צריך תיקון.
* **מוצג ולא נמחק.** המתנגשים יורדים מ"השיעורים הקרובים" ועולים לרשימה
  משלהם. הגאנט עשוי להיות זה שטועה.
* ⚠ **"טרם דווח" אינו מסונן לפי הגאנט.** `חג ומועד` בגאנט מכסה גם את צום
  גדליה ואת אסרו חג, שבהם המכינה עובדת. סינון היה מוחק מטלה אמיתית בשקט,
  וההכרעה נשארת אצל אחראי הלו״ז.
* ⚠ **"סדרה" אינה חוסמת.** בסדרה יש לימודים, רק לא לפי המערכת הרגילה.

⚠ `HOME_RE` הועתק כפי שהוא מתקציב המטבח ולא הורחב — הרחבה הייתה משנה
בשקט חישוב כסף לימים שכבר חושבו. ידוע ולא תוקן: אירוע בשם
`בית (לא בטוח, תלוי ארועים בקיבוץ)` אינו נתפס, כי `^בית$` דורש התאמה
מדויקת, וימים כאלה מחושבים כשגרה.

### 4כא · תוויות סטטוס ב-monday: מה אפשר ומה לא

* **הוספה** — `create_labels_if_missing:true` בשמירה. עובד.
* **מחיקה** — `update_status_column` נכשל ב-`Unable to delete a label
  already in use`, כולל שימוש בשורות שיושבות ב**סל המיחזור**. ריקון הסל
  כדי למחוק תווית הוא הרסני ואינו נעשה.
* **שינוי צבע** — נכשל ב-`request to change default status label color`
  על שלוש התוויות הראשונות. שמות הצבעים ב-`settings_str` הם הישנים
  (`orange`, `green-shadow`) ואינם נמצאים ב-enum `StatusColumnColors`,
  ולכן גם **סיבוב ריק** של הרשימה נדחה.

המסקנה המעשית: תווית שנוצרה בטעות **נמחקת ביד בלוח**. לכן החיכוך נמצא
לפני היצירה — שדה נפרד ומכוון — ולא אחריה. ובבדיקות: אין ליצור תוויות
חדשות אלא אם הן אמורות להישאר.

### 4כב · הרשאה לפי תחום, לא לפי מסלול

ציוד המכולה וציוד הניקיון חולקים לוח אחד וקוד אחד — ו**לא** אותו אחראי:
המכולה של אחראי המכולה, הניקיון של אב הבית. `withAuth` אינו יכול לבטא
"או־או" בין שני תפקידים (הדגלים שלו הם AND), ולכן:

* השער הוא `{ student: true }`, וההרשאה האמיתית היא `mayArea(session, area)`
  ב-`shared/container-boards.js`, שנקראת בכל פעולה.
* ⚠ **התחום נלקח מהפריט ולא מהבקשה** ב-PUT וב-DELETE. אחרת אפשר היה
  לערוך פריט מכולה בכך שכותבים "ניקיון" בגוף הבקשה.
* השגיאה אומרת **של מי התחום** ("ציוד הניקיון מנוהל על ידי אב הבית") ולא
  "אין הרשאה" — מי שנחסם צריך לדעת למי לפנות.
* הכלל יושב ב-shared כדי שהמסך יסתיר בדיוק את מה שהשרת יחסום.

### 4כג · לוח השיעורים נקרא כמו הגיליון

הלוח הציג את מפגשי היום **בסדר שרירותי** ובלי שעה: אימונים של 7:00 הופיעו
אחרי מליאה של 20:00. מסך שאומר "יש היום חמישה שיעורים" בלי לומר מתי אינו
נראה כמו הגיליון שממנו הוא נבנה, וזה מה שנקרא "לא תואם בכלל".

* המיון: **תאריך, ואז שעה**, ואז שם. שיעור בלי שעה בגיליון יורד לסוף היום.
* ב"טרם דווחו" הסדר הפוך — האחרון ראשון. קל יותר לדווח על אתמול מאשר על
  לפני שבועיים.
* השעה מנותחת מ`dayTime` שבגיליון (`timeOf` / `minutesOf` ב-
  `shared/lessons-boards.js`), שהוא טקסט חופשי: `"שני 10:00"`,
  `"שלישי כל היום"`, `"ראשון 15:30/14:30"`.
* ⚠ **`offDay`** — מפגש שנופל ביום אחר מזה שכתוב בגיליון **מסומן ולא
  מתוקן**. זה קורה באמת בהשלמות ובשיעורים שהוזזו, והלוח לא אמור להכריע
  במקום אחראי הלו״ז.
* ⚠ סתירה לכאורה שאינה סתירה: ל"אימונים" 86 מפגשים והגיליון אומר
  "שני 7:00". השיעור מתקיים פעמיים בשבוע והגיליון מציין רק אחד מהם.
  הגיליון חסר, המפגשים נכונים.

`LessonsBoard` מוצג עכשיו גם במסך הבית של אחראי הלו״ז ב-`Mechina.jsx` —
אותו רכיב בדיוק של מסך הבית של המנהל. ראו סעיף 4יט.

### 4כד · מחיקת תוויות סטטוס — התיקון לסעיף 4כא

סעיף 4כא קבע שאי אפשר למחוק תווית דרך ה-API. **זה לא נכון**, ושתי
השגיאות שהובילו למסקנה הזו היו שתי טעויות שלי:

1. **`id` מול `index`.** `id` הוא מזהה התווית (המספר שנשמר על השורה),
   `index` הוא מיקומה ברשימה. שליחת ה-id בשדה index גורמת ל-monday
   לחשוב שכל התוויות הישנות נמחקו → `Unable to delete a label already
   in use`.
2. **שמות הצבעים.** `settings_str` מחזיר שמות ישנים (`orange`, `grey`,
   `mustered`) וה-API מקבל שמות enum (`working_orange`, `explosive`,
   `saladish`). אין ביניהם כלל המרה — הניחושים שלי היו שגויים ברובם.
   צבע לא נכון על אחת משלוש התוויות הראשונות → `request to change
   default status label color`, גם על סיבוב ריק.

הטבלה נבנתה **בניסוי** ב-`scratchpad/color-map.mjs`: עמודת סטטוס זמנית
עם תווית בכל אחד מ-40 הצבעים, קריאת `settings_str`, ומחיקת העמודה.
התוצאה ב-`scratchpad/color-map.json`.

הכללים שנשארים בתוקף:
* `update_status_column` **דורס את כל הרשימה** — היא נבנית מהקיים.
* `revision` הוא נעילת גרסה, והוא הגנה רצויה כשמישהו עורך את הלוח ביד.
* צבע חייב להיות **ייחודי** לכל תווית.
* ⚠ ולפני הכול: **לא מוחקים תווית שיש מאחוריה שורה.** הבדיקה הזו תפסה
  שני בוגרים שישבו על תווית שנוצרה בטעות.

### 4כה · "מתוכנן" שבגיליון מכריע

לגיליון המרצים יש עמודת **מתוכנן** (כן/לא) ועמודת **סיבה**, ואחראי הלו״ז
כבר ממלא אותן: "שבוע קליטה", "יום כיפור", "יום מיון", "לתאם מחדש".
**269 מתוך 688 המפגשים מסומנים "לא"** — והלוח הציג את כולם.

סדר ההכרעה, מהחזק לחלש:

1. **`planned === "לא"`** → המפגש אינו מתקיים. נקודה. הוא עובר לרשימת
   "לא מתקיימים" עם הסיבה, ואינו נספר ב"טרם דווחו" — אין עליו מה לדווח.
2. **הגאנט** → **מסמן ואינו מכריע**. הוא תפס רק 80 מאותם 269 (הוא יודע
   על חגים, לא על "יום מיון"), ובכיוון השני היה מסלק שיעורים שהגיליון
   אומר במפורש שהם מתקיימים — שיעור בצום גדליה שסומן "מתוכנן: כן" באמת
   מתקיים. ההתנגשות מוצגת על השורה ונספרת, והשיעור נשאר ברשימה.
3. **ריק נחשב מתוכנן** — זו ברירת המחדל של הגיליון.

⚠ הכלל הכללי: **הצהרה מפורשת על הפריט גוברת על גזירה מהקשר.** אותו כלל
חוזר בשיחה האישית (סעיף 4יד) ובחריגת התוויות (4טז).

### 4כו · התראות נגזרות, לא תור הודעות

`api/_notify.js` בונה את ההתראות **מהמצב הנוכחי בכל שליפה**. אין לוח
"התראות" ואין תור.

תור שמור מתיישן ברגע שמישהו מטפל בתקלה דרך monday: ההתראה נשארת פתוחה
ומצביעה על משהו שכבר נסגר, והמשתמש לומד להתעלם ממנה. התראה שנגזרת מהמצב
נעלמת מעצמה כשהמצב משתנה — וזו ההתנהגות שגורמת לאנשים להמשיך להסתכל.

* **נמען לפי תפקיד.** אב בית → תקלות וציוד ניקיון · אחראי מטבח → חוסרים ·
  אחראי מכולה → השאלות באיחור · אחראי בטיחות → אירועים שטרם דווחו להורים
  ואירוחים קרובים · אחראי לו״ז → שיעורים שלא דווחו וחוות דעת חסרות.
  **מנהל מקבל את הכול.**
* **חניך מקבל רק את שלו.** לעולם לא נתונים של חניך אחר.
* ⚠ **דחוף לחוד, השאר כמספר.** תקלה דחופה מקבלת שורה בשמה; עשרים תקלות
  רגילות הן שורה אחת. עשרים שורות "תקלה פתוחה" הן רעש שגורם לסגור את
  הפעמון ולא לפתוח אותו.
* ⚠ **`id` יציב ונגזר מהתוכן** ולא מספר רץ — אחרת "נקרא" היה מתאפס בכל
  רענון.
* ⚠ **"נקרא" הוא חותמת זמן אחת למשתמש**, בעמודה בלוח שממנו הוא מתחבר,
  ולא שורה לכל התראה.
* ⚠ **תחום שנופל אינו מפיל את הפעמון** — כל מקור נתפס בנפרד ונרשם ללוג.
  התראה חסרה עדיפה על מסך שבור.
* ⚠ **נטען גם כשהפעמון סגור.** התג הוא כל התכלית: מי שצריך ללחוץ כדי
  לדעת שיש משהו לא יידע שיש משהו.

⚠ אלה התראות **בתוך האפליקציה**. דחיפה לטלפון, מייל או וואטסאפ דורשת
שירות חיצוני (וסודות נוספים ב-.env), ולא נבנתה.

### 4כז · שכבת ההרמה השנייה — מה נלקח מ-busly ומה לא

**נלקח:** פס גוון בקצה העליון של כרטיס טופס (`.card.lift`) · כפתור ראשי
עם שיפוע **וזוהר בצבעו שלו** מתחתיו · שדות גבוהים (52px) עם מילוי רך
וטבעת מיקוד · מסילת שלבים ממוספרת עם קו מקשר (`.steps`) · קשת מנוקדת עם
נקודת אור ברקע הרצועה · מפריד עם מילה (`.or`).

**לא נלקח:** הפלטה הכהה. לאפליקציה יש זהות קרם־נייבי שעובדת, והחלפה שלה
הייתה עיצוב **אחר**, לא עיצוב טוב יותר.

⚠ הזוהר מתחת לכפתור הוא **בצבע הכפתור** ולא באפור. צל אפור מתחת לכפתור
כחול נראה כמו שכבה זרה; זוהר בגוונו נראה כמו שהכפתור מאיר.

⚠ שדה במילוי רך ולא לבן: שדה לבן על כרטיס לבן נשען על המסגרת בלבד,
ומסגרת דקה נעלמת בשמש.

### 4כח · שמות משתמש וסיסמאות

**הסיסמה אינה נשמרת בשום מקום.** מה שיושב בלוח הוא תוצאת `scrypt` עם מלח
אקראי לכל משתמש (`api/_credentials.js`). מי שיפתח את הלוח — כולל מנהל
המכינה, כולל מפתח — רואה מחרוזת חסרת פשר.

זה שינוי מהותי מהקודים: קוד הכניסה של הצוות שמור כטקסט גלוי, כי הוא קוד
תפעולי שמישהו צריך למסור בטלפון. **סיסמה שאדם בוחר בעצמו היא דבר אחר
לגמרי** — הוא כמעט בוודאות משתמש בה במקום נוסף, ודליפה שלה אינה עניין של
המכינה בלבד.

* `scrypt` ולא SHA — גיבוב מהיר נשבר במיליארד ניחושים לשנייה.
* ההשוואה ב-`timingSafeEqual`.
* הפרמטרים נשמרים בתוך המחרוזת, כדי שאפשר יהיה לחזק אותם בלי לפסול
  סיסמאות קיימות.
* **בלי חבילות חיצוניות.** `crypto` מובנה.

**כניסה ראשונה של חניך:** שם המשתמש והסיסמה הם שניהם תעודת הזהות. הדלת
נפתחת פעם אחת עם משהו שאינו סוד — ת"ז מופיעה בכל טופס ומוכרת לחברים
לכיתה — **ונסגרת מיד**: הסשן מסומן `setup`, ו-`withAuth` חוסם כל נקודת
קצה חוץ מ-`account`. ⚠ החסימה **בשרת ולא במסך**: מסך שמונע ניווט הוא
הצעה, שרת שחוסם הוא הבטחה.

⚠ **הכניסה מקבלת שם משתמש או אימייל, באותו שדה.** אדם שזה עתה איפס סיסמה
דרך המייל שלו מנסה להיכנס עם אותה כתובת — זה מה שהיה מול העיניים שלו לפני
שנייה. דרישה שיזכור דווקא את שם המשתמש היא בדיוק הרגע שבו הוא נתקע שוב.

אין התנגשות אפשרית: שם משתמש אינו יכול להכיל `@` (`USER_RE`) ואימייל חייב
להכיל אותו, ואימייל ייחודי לחשבון (`_account.js` דוחה כתובת תפוסה).

⚠ **אותה הודעה לכל כישלון כניסה.** "שם משתמש לא קיים" ו"סיסמה שגויה" הם
שני מסלולים שהופכים את הטופס למנוע בדיקה של מי רשום במכינה — וזה נכון
כפליים כשהזיהוי הוא לפי אימייל, שאחרת הטופס היה מגלה אילו כתובות רשומות.
"אינו פעיל" כן מקבל הודעה נפרדת — זו תקלה תפעולית — ורק **אחרי** שהסיסמה
אומתה.

⚠ **טביעת האצבע בעוגייה היא של הסוד שבלוח** (ת"ז/קוד) ולא של גיבוב
הסיסמה, כי זה מה ש-`requireAuth` בודק מול הלוח בכל בקשה. המשמעות: החלפת
סיסמה אינה מנתקת סשנים אחרים. פער מוכר ומקובל כאן — כיבוי "פעיל" בלוח
מנתק מיד, וזו הפעולה שעושים כשחוששים.

⚠ **מדיניות סיסמה: אורך בלבד.** שמונה תווים, בלי דרישות מורכבות. כללים
כמו "אות גדולה, ספרה, תו מיוחד" מייצרים `Aa123456!` אצל כולם ומעודדים
לכתוב על פתק. מה שכן נחסם: הסיסמאות שמנחשים ראשונות, ותעודת הזהות של
המשתמש עצמו.

### 4כט · איפוס סיסמה, ולמה הוא עובד גם בלי מייל

`api/_recover.js`. **התשובה לבקשת איפוס זהה תמיד** — קיים או לא קיים,
אותו טקסט ואותו סטטוס. ⚠ ואין `penalize` על "לא נמצא": ההשהיה עצמה
הייתה הופכת את זמן התשובה לאינדיקציה.

* האסימון נשלח למשתמש; בלוח נשמר **גיבוב שלו** ותאריך תפוגה (שעה).
* **שימוש אחד בלבד** — האסימון נשרף בקביעת הסיסמה, וגם קביעת סיסמה
  מהמסך הרגיל מוחקת אסימון פתוח.
* SHA-256 ולא scrypt לאסימון: 32 בתים אקראיים אינם ניתנים לניחוש
  במתקפת מילון, וההאטה הייתה מיותרת.

**⚠ קוד בן שש ספרות שנשלח במייל — לא קישור.** קישור נראה כמו כתובת
אקראית של Vercel, נשבר כשפותחים אותו מאפליקציית מייל שמריצה דפדפן פנימי,
ומלמד את מי שקיבל אותו איפה המערכת מתארחת. קוד מוקלד **במסך שממנו יצאה
הבקשה** ועובד מכל מכשיר.

הזרימה: שם משתמש → קוד למייל → הקלדת הקוד → סיסמה חדשה **ואישורה**.

⚠ **הקוד נבדק תמיד מול שם המשתמש, ולעולם לא לבדו.** שש ספרות הן מיליון
אפשרויות — הרבה פחות מאסימון של 32 בתים שהיה כאן קודם. בלי הקישור לשם,
ניחוש מוצלח היה פותח את החשבון של **מי שבמקרה** מחזיק באותו קוד, וכל קוד
פעיל במערכת היה מטרה. עם הקישור, תוקף חייב לדעת את השם **וגם** לנחש את
הקוד בתוך שעה, מול השהיה גוברת.

⚠ **הקוד נבדק לפני שמציגים שדות סיסמה.** מי שהקליד קוד שגוי יודע מיד ולא
אחרי שכבר בחר סיסמה.

⚠ **הקוד נשמר לפני השליחה ולא אחריה.** כשל באמצע משאיר קוד קיים בלוח
ולא בקשה בלי שום מפתח.

⚠ **הקוד אינו מוצג לאיש במסכים.** הצגתו לצוות הוסרה: מדריך שרואה קוד
איפוס של חניך אחר מחזיק מפתח לחשבון שאינו שלו, וזה אינו דבר שצריך להיות
מונח בפעמון ההתראות. הוא נשמר בעמודת "איפוס סיסמה" בלוח כשסתום חירום —
משתמש בלי אימייל רשום, או תקלת שליחה, משאירים אותו נעול לגמרי, ולמצב הזה
צריכה להיות דרך יציאה.

### שני מסלולי שליחה, ו-SMTP גובר

**המכינה כבר על Google Workspace.** בדיקת ה-DNS של `bemaaleh.com` מראה
`MX → smtp.google.com` ו-`SPF include:_spf.google.com`. כלומר יש כבר
שירות דואר משולם, מאומת, עם כתובת אמיתית בדומיין — ואין שום סיבה לאמת
דומיין אצל ספק אחר.

| מסלול | מה צריך | מתי |
|---|---|---|
| **SMTP** (מומלץ) | `SMTP_USER` + `SMTP_PASS` | Google Workspace |
| Resend | `RESEND_API_KEY` + `MAIL_FROM` | בלי Workspace |

⚠ **Resend חסום כאן ולא בגלל Resend.** האימות שלו דורש רשומת **MX על
תת-דומיין**, ו-Wix — שמנהלת את ה-DNS — אינה תומכת בזה בכלל. הדומיין
נשאר `Pending` לנצח. זו מגבלת Wix, ולא משהו שאפשר לעקוף בהגדרות.

⚠ `SMTP_PASS` הוא **סיסמת אפליקציה** בת 16 תווים ולא הסיסמה של החשבון.
גוגל חוסמת SMTP עם הסיסמה הרגילה. רווחים בהעתקה מוסרים אוטומטית — זו
תקלה שנראית כמו "סיסמה שגויה" ואינה.

⚠ `MAIL_FROM` חייב להיות **אותה כתובת של `SMTP_USER`** או כינוי מאושר
שלה. גוגל דוחה שליחה בשם כתובת שאינה של החשבון.

⚠ **nodemailer היא החבילה החיצונית היחידה במערכת**, ורק בגללה SMTP
אפשרי. כתיבת SMTP-over-TLS ביד היא כמאתיים שורות לתחזק.

⚠ פורט **465 עם TLS** ולא 587 עם STARTTLS: חלק מסביבות serverless
חוסמות את המשא ומתן של STARTTLS.

⚠ `מסך "בדיקת מייל"` מתרגם את שגיאות גוגל לפעולה — "סיסמה רגילה במקום
סיסמת אפליקציה", "המנהל חסם SMTP", "כתובת שולח לא מורשית". בלי זה
`535` הוא כל מה שיש.

⚠ כל המשתנים ב-.env ובמשתני Vercel בלבד. בלעדיהם המערכת עובדת במלואה.

⚠ **האימייל חובה בכניסה הראשונה**, ורק בה. מי שכבר בפנים ורוצה לשנות
סיסמה אינו נחסם בגלל שדה שלא היה קיים כשנכנס. ⚠ והוא עדיין **מוקלד על ידי
המשתמש ולא מיובא** — בפרט לא מקובץ משרד החינוך, שנשאר מחוץ למערכת.

⚠ **בדיקה שנוגעת בחשבון אמיתי מחזירה את מה שהיה, לא מנקה.**
`scratchpad/demo-test.mjs` ניקה את עמודות הזהות בסופו, ובהרצה אחת מחק
למנהל את שם המשתמש והסיסמה שהוא הרגע הגדיר — בלי לומר מילה. אותו לקח
של סעיף "בדיקה יוצרת את הנתונים שלה".

### 4ל · מחזורים — המשכיות בלי מפתח

המטרה: ראש המכינה פותח מחזור חדש **מתוך האפליקציה**, ממלא אותו, מפעיל —
והמערכת עוברת אליו. בלי שינוי בקוד ובלי דיפלוי.

**איך זה עובד.** כל מודול מייבא מזהי לוחות מ-`shared/*-ids.js`. אלה
**אובייקטים ולא מחרוזות**, ולכן `api/_cycle.js` מחליף את תוכנם בזמן ריצה
ואף מודול לא יודע. ⚠ `Object.assign` על האובייקט הקיים ולא החלפה שלו:
מודולים שכתבו `const A = EXTRA.alumni` מחזיקים הפניה לאובייקט הפנימי.

⚠ בגלל זה `FAULTS_BOARD` ו-`SAFETY_BOARD` הומרו ל-`FAULTS.board` /
`SAFETY.board`. **מחרוזת מיוצאת נקבעת פעם אחת בטעינת המודול ואי אפשר
להחליף אותה** — כל מזהה לוח חדש חייב לשבת בתוך אובייקט.

⚠ `ensureCycle()` נקרא מ-`withAuth` בכל בקשה. מטמון של חמש דקות, ולעולם
אינו זורק — כישלון משאיר את המערכת על המזהים שכבר נטענו, כי עדיף לעבוד
על המחזור הקודם מאשר ליפול.

**מחזור פעיל אחד, גלובלי** — לא לכל משתמש ולא לכל בקשה. מכינה מריצה מחזור
אחד, והקודם נשאר לקריאה ב-monday. לכן אין מרוץ בין בקשות.

**השכפול** הוא `duplicate_board_with_structure`: אותן עמודות, אותן תוויות,
**אף שורה**. 19 לוחות בכ-35 שניות, אחד־אחד ולא במקביל — monday מגבילה
קצב, ו-20 בקשות במקביל משאירות מחזור חצי בנוי. לוח שנכשל נרשם ב-`failed`
ואינו מפיל את השאר.

⚠ **פתיחה והפעלה הן שתי פעולות נפרדות.** בין השתיים יש חודשיים של הזנת
נתונים; מחזור שנכנס לתוקף ברגע שנפתח היה מחליף את המערכת ללוחות ריקים
באמצע שנה פעילה. הפעלה דורשת שהשלבים המסומנים כחובה יושלמו.

⚠ **ראש המכינה בלבד** (`session.isHead`), ולא כל איש צוות. זו הפעולה הכי
משמעותית במערכת.

⚠ **המחזור היוצא עובר לארכיון ולא נמחק**, ומחיקת רישום מחזור מסירה את
הרישום בלבד — **הלוחות ב-monday נשארים**.

⚠ **מה לא שייך למחזור:** לוח ההרשאות (הצוות ממשיך), לוח המנות (מתכונים
אינם משתנים), ולוח הבוגרים — שהוא בדיוק המקום שבו מחזור שהסתיים ממשיך
לחיות. `shared/cycles.js` הוא **החוזה**: תחום חדש שנוסף למערכת דורש שורה
שם, אחרת מחזור חדש ייווצר בלעדיו — ותקלה כזו מתגלה רק בעוד שנה.

⚠ **מה שעדיין ידני:** סימון שלב ההקמה כ"הושלם". המערכת יכולה לדעת שיש
שורות בגאנט, אבל לא שהגאנט **נכון** — וזה לא אותו דבר. ראש המכינה יודע.

### 4לא · חשבון בדיקה

חניך שנכנס למערכת ככל חניך, ו**אינו נספר בשום מקום** — לא בנוכחות, לא
בשיבוצים, לא במובילי שבוע, לא באימונים ולא ברשימת החניכים.

הסימון הוא עמודת `demo` בלוח המצבה (`MECHINA_COLS.roster.demo`), והסינון
נעשה **בשורה אחת**, ב-`activeStudents()` שב-`api/_student-rows.js`.

⚠ **הכיוון הזה הוא העניין.** כל מסך שמונה חניכים עובר דרך `activeStudents`,
ולכן מסך חדש שייכתב מחר יסנן אותו מעצמו. מי שירצה דווקא לכלול אותו יצטרך
לקרוא ל-`studentRows` במפורש — ולכתוב למה.

⚠ **הכניסה עוברת ב-`studentRows` ולא ב-`activeStudents`**, ולכן היא ממשיכה
לעבוד. זו כל התכלית של החשבון.

⚠ **`active` לבדו לא היה מספיק.** כיבוי "פעיל" אמנם מוציא אותו מכל ספירה,
אבל גם **חוסם לו את הכניסה** — וזה בדיוק ההפך ממה שנדרש. שני הדגלים
נפרדים כי הם עונים על שתי שאלות שונות.

### 4לב · הקמת מחזור — הדבקה, תצוגה מקדימה, תיקון

`shared/import-parse.js` + `api/_cycle-import.js` + `src/CycleImport.jsx`.
המסילה של המחזור אינה עוד הסבר — לכל שלב יש **"מילוי עכשיו"** שעושה
את העבודה.

**הקלט הוא הדבקה ולא קובץ.** ראש מכינה מחזיק את הנתונים בגיליון,
בוואטסאפ או במסמך, וההדבקה עובדת מכולם. העלאת קובץ דורשת לשמור, למצוא
ולבחור — שלושה שלבים שבכל אחד אפשר להיתקע. המפריד: טאב, פסיק, או שני
רווחים ומעלה.

⚠ **הפרסור משותף למסך ולשרת.** שתי גרסאות היו נפרדות זו מזו בתיקון
הראשון, והמנהל היה מאשר דבר אחד ומקבל אחר.

⚠ **תמיד תצוגה מקדימה לפני כתיבה.** פרסור שגוי שנכתב ישר ללוח הוא
33 שורות למחוק ביד.

⚠ **שורה שלא נקלטה מוצגת עם הסיבה ועם מספר השורה** — היא לא נזרקת
בשקט. שורה שנעלמת היא חניך שלא קיים, ואיש לא יידע עד ספטמבר.

⚠ **ושתי רשימות דחייה נפרדות:**
* `bad` — הפרסור נכשל ("לא נמצאה תעודת זהות").
* `failed` — הפרסור הצליח ו-**monday סירבה**. זה קרה באמת: המגדר נכתב
  כ-`בן`/`בת` והתוויות בלוח הן `זכר`/`נקבה`, אז monday דחתה את **כל
  השורה**. בלי הצגה, המנהל היה רואה "נוספו 32" אחרי שהדביק 33.

  זה בדיוק הכלל של "התוויות בקוד חייבות להיות זהות בתו לתוויות שבלוח".

⚠ **הדבקה חוזרת אינה מכפילה.** שורה שכבר קיימת אינה נכתבת שוב ואינה
נחשבת שגיאה — היא נספרת כ"כבר קיים". `keyOf`/`readKey` לכל שלב.

⚠ **הכול ניתן לתיקון ומחיקה מהמסך.** נתונים אמיתיים תמיד מגיעים עם
טעות קטנה — רווח כפול, ספרה חסרה, תאריך הפוך. בלי תיקון מהמסך כל טעות
שולחת את המנהל ל-monday, וזה בדיוק מה שהאפליקציה נועדה למנוע.

⚠ **נכתב ללוחות של המחזור שנבחר, לא לפעילים.** המזהים מהרישום שלו ולא
מהמצב הגלובלי. הבדיקה מוודאת שמחזור ב׳ נשאר ב-33 חניכים.

⚠ **תיקון במחזור פעיל מסומן במסך** (`live` בתשובת ה-GET) — שם הוא נוגע
בנתונים שהמערכת עובדת איתם עכשיו.

⚠ **שלב בלי מסלול ייבוא אינו מקבל כפתור** — "פתיחת הלוחות" ו"תפקידים"
נעשים במקום אחר, וכפתור שלא עושה כלום גרוע מהיעדרו.

---

## 4לג · מחיר, משקל והמרת ירקות (`shared/produce.js`)

⚠ **המספרים הם הערכה מוצהרת.** עגבנייה שוקלת בין 80 ל-180 גרם. הטבלה
עונה על "כמה ק״ג להזמין לשלושים וחמישה איש", ולא על "כמה זה עלה".
לכן כל מספר במסך מוצג עם **≈** ועם ההסתייגות במילים, לפני הכלי ולא
אחריו. טבלה שנראית מדויקת ואינה, גרועה מטבלה שמצהירה על עצמה.

⚠ **ערך בלוח גובר על הטבלה.** `ק״ג ליחידה` בעמודת הפריט מנצח תמיד —
עיקרון 1. הטבלה היא ברירת מחדל כדי שלא ימלאו 60 שורות ביד, לא מקור
אמת. השרת מחזיר `kgSource` (`board` / `table` / `null`) כדי שהמסך
יוכל לומר מאיפה המספר הגיע. מספר בלי מקור נראה כמו מדידה.

### ⚠ שתי מלכודות עברית שנתפסו כאן, ויחזרו בכל התאמת טקסט

1. **אות סופית מתחלפת ברבים.** `"מלפפונים".includes("מלפפון")` הוא
   **false** — ן סופית ו-נ רגילה הם תווים שונים. `includes` פשוט על
   שם פריט מהלוח פשוט לא ימצא אותו.
2. **`includes` דו-כיווני רחב מדי.** "עגבניות" מצא את "עגבניות שרי",
   כי הכיוון השני התאים. שרי שוקלת 12 גרם ועגבנייה 120 — פי עשרה
   בהזמנת ירקות.

הפתרון: **התאמה מילה־מילה על שורשים**. מפתח מתאים רק אם *כל* מילותיו
נמצאות בשאילתה. הסדר בתוך `stem` קובע — קודם מסירים סיומת ריבוי מהמילה
המקורית, ורק אז ממירים אות סופית.

### מחיר

⚠ **ריק אינו אפס.** מחיר 0 פירושו "בחינם"; מחיר חסר פירושו "לא
יודעים". שלושה מצבים ב-`numPatch`: `null` מנקה, מספר קובע, `false`
נדחה. מחרוזת ריקה היא הדרך להחזיר פריט ל"לא יודעים".

⚠ **שווי המלאי מחושב בשרת ולא בדפדפן**, כדי שמסך האוכל, מסך החד״פ
והמסך המאוחד יראו את אותו מספר.

⚠ **ותמיד עם `unpriced`.** "₪1,240" לבד נקרא כשווי המחסן כשהוא שווי של
שליש ממנו. המסך חייב להראות כמה פריטים אין להם מחיר.

⚠ **מחיר ומשקל קיימים במטבח בלבד** (`d.money` ב-`Kitchen.jsx`). למכולה
אין עמודות כאלה בלוח, ושדה שנשלח לשם היה נדחה בשקט.

⚠ **`src/api.js` מפרק אובייקט במפורש.** שדה שלא נכתב שם נשמט בשקט —
זו הדרך שבה שדה חדש נראה עובד במסך ואינו מגיע לשרת.

⚠ **מספרים קטנים דורשים שלוש ספרות.** שתי ספרות עיגלו את עגבניית השרי
מ-0.012 ל-0.01, והשורה סתרה את עצמה מול "83 יחידות בק״ג".

⚠ **"0.2 אבטיחים בק״ג" נכון וחסר תועלת.** פריט ששוקל יותר מק״ג נמדד
ביחידות; העמודה מציגה `—`.

---

## 4לד · הרשמת הצוות

⚠ **הקוד כבר אינו כניסה — הוא הרשמה.** איש צוות עם `isFresh` (בלי שם
משתמש **ובלי** גיבוב) מקבל `setup: true`, ו-`withAuth` חוסם אותו בכל
נקודת קצה חוץ ממסך ההקמה. אותו מסלול בדיוק כמו חניך עם ת.ז.

⚠ **הקוד ממשיך לעבוד אחרי ההרשמה, אבל כבר לא כהרשמה.** אחרת מי שיודע
את הקוד היה יכול לאפס את הזהות של מנהל רשום.

⚠ **האימייל חובה גם לצוות.** בלעדיו איפוס סיסמה דורש שאדם יהיה זמין
למסור קוד ביד — וזה בדיוק הרגע שבו אף אחד לא זמין.

### ⚠ מה זה עושה לבדיקות

הבדיקות נכנסות עם קוד, ולכן כולן נחסמו ביום שההרשמה נוספה.
`scratchpad/_auth.mjs` רושם איש צוות **זמנית**, והגיבוב נבנה מ-32 בתים
אקראיים שנזרקים מיד — **אין מחרוזת שתפתח את החשבון**. סיסמת בדיקה
ידועה על שורת מנהל היא פרצה, גם אם זמנית.

`reg.restore()` ב-`finally` הוא חובה. `scratchpad/unregister.mjs` מנקה
שאריות מהרצה שנפלה באמצע.

---

## 4לה · "הפרופיל שלי" — מסך אחד

⚠ **היו כאן שתי לשוניות באותו שם.** אחת לזהות ואחת לצבא ולמיונים,
והחניך היה צריך לנחש איזו מהן מחזיקה את מה שהוא מחפש. הכול על עצמי
יושב עכשיו במסך אחד.

⚠ **ת.ז ומגדר לקריאה בלבד.** הם מזהים את האדם מול המכינה, ושינוי שלהם
מהמסך הזה היה מנתק את החניך מהשיבוצים, מהנוכחות ומהבקשות. המסך אומר
למי לפנות, ולא רק "אי אפשר".

⚠ **החלפת אימייל דורשת את הסיסמה הנוכחית** — היא הדרך להשתלט על חשבון
דרך "שכחתי סיסמה".

⚠ **הנתונים האישיים מגיעים מהשורה של הסשן עצמו בלבד** (`api/_account.js`),
לא מרשימה שמסוננת בדפדפן.

---

## 4לו · טבלת ההמרה הנערכת (`מטבח – המרות`)

⚠ **שלוש שכבות, מהכללי לספציפי, והספציפי גובר:**
1. `PRODUCE_KG` ב-`shared/produce.js` — 46 ברירות מחדל בקוד.
2. לוח "מטבח – המרות" — מה שאחראי המטבח והמנהלים קובעים מהמסך.
3. עמודת "ק״ג ליחידה" של הפריט עצמו בלוח הציוד.

⚠ **הלוח מחזיק דריסות בלבד, והוא נוצר ריק.** הזרעה של כל 46
השורות נוסתה ובוטלה: כשכל שורה מסומנת "מהלוח", התג "נערך"
מופיע על כל הטבלה ומאבד את משמעותו, ואי אפשר לדעת מה באמת שונה.

⚠ **מחיקה של דריסה היא "איפוס" ולא "מחיקה"**, והשרת מחזיר
`fallback` כדי שהמסך יוכל לומר לאן חוזרים. פריט שנוסף מאפס
ואין לו ערך מובנה — שם הכפתור "מחיקה".

⚠ **`buildTable` מתעלם מערך לא חוקי בשקט** ונופל לברירת המחדל.
שורה שמישהו מחק לה את המספר ב-monday לא אמורה למחוק את הידע
של המערכת על מה זו עגבנייה.

⚠ **הלוח אינו חלק מ-`boardsReady`.** לוח שלא הוקם מחזיר רשימה
ריקה והמערכת נופלת לברירת המחדל; 503 היה מוריד את כל מסך המטבח
בגלל טבלת עזר. כתיבה, לעומת זאת, נכשלת במפורש.

⚠ **המדד מחושב פעם אחת לטבלה** (`WeakMap`). `kgPerUnit` נקראת
פעם לכל פריט ברשימה של 105, ובניית שורשים בכל קריאה הייתה
מורגשת במסך.

---

## 4לז · שווי רשימת הקניות

⚠ **התאמה לפי שם מדויק ובאותו תחום.** שורת הקנייה נוצרת מהפריט
עצמו ולכן השם זהה. התאמה חלקית הייתה מדביקה מחיר של פריט אחר
על שורה שהוקלדה ביד — בדיוק המקום שבו מספר שגוי נראה סביר.

⚠ **`openValue` נפרד מ-`value`.** "כמה שווה מה שיש" ו"כמה תעלה
ההזמנה" הן שתי שאלות, ומספר אחד לשתיהן היה מטעה בשתיהן.

---

## 4לח · האנימציה של ה-✓

⚠ **אלמנט שמתפרק אינו מבצע מעבר.** סימון "נקנה" העביר את השורה
מיד לרשימת "נקנו", כלומר האלמנט נבנה מחדש כשהוא כבר מסומן —
ולמשתמש זה נראה כאילו הלחיצה לא נקלטה בכלל.

הפתרון: `flash` — השורה **נשארת במקומה** ל-`FLASH_MS` בזמן
שה-✓ נצבע ומצטייר, ורק אז עוברת. הבקשה לשרת יוצאת מיד ואינה
מחכה לאנימציה; זו תצוגה, לא עיכוב אמיתי.

⚠ גם ל"סימון הכול" — בלי זה ארבעים שורות נעלמות בבת אחת.

⚠ ו-`prefers-reduced-motion` מקבל את הצבע בלי הקפיצה.

---

## 4לט · דלת אחת לכניסה

⚠ **מסך "כניסת צוות עם קוד" הנפרד נמחק.** הוא יצר בעיה אמיתית:
מי שנכנס פעם אחת עם קוד לא ידע מה להקליד בפעם הבאה, כי המסך
שראה בכניסה הראשונה כלל לא היה המסך הרגיל. מסך כניסה שמלמד
דבר אחד ודורש דבר אחר הוא מסך שבור.

⚠ **הסוד שבלוח בשני השדות.** חניך — תעודת הזהות; צוות — קוד
הכניסה. `_signin.js` מזהה **רק שורות `isFresh`** בדרך הזו, ורק
כששני השדות תואמים. מי שכבר בחר שם וסיסמה אינו נגיש כך —
אחרת מי שיודע את הקוד היה עוקף את הסיסמה של מנהל רשום.

⚠ **הראשון שנכנס עם הקוד תופס את החשבון.** זה היה נכון גם
במסלול הישן ואינו רגרסיה, אבל זו הסיבה שהקודים אינם מסתובבים.

---

## ⚠ ניקוי אחרי בדיקה — לפי מזהה, לא לפי סינון

בסשן הזה נמחקה שורה אמיתית מלוח הקניות, כי הניקוי חיפש
`name === "אורז" && qty === "12"` במקום להשתמש במזהים שהוחזרו
ביצירה. סינון לפי ערכים תופס גם שורות שלא יצרנו.

**הכלל: לשמור את המזהה שחזר מה-POST, ולמחוק רק אותו.**

---

## 4מ · ראש מכינה — הרשאה בעמודה, לא בקוד

⚠ `isHead` נגזר מ**עמודת "תפקיד במכינה"** בלוח המשתמשים, בכל בקשה
מחדש. לתת למישהו את כל הרשאות ראש המכינה — פתיחת מחזורים, ייבוא
נתונים, הכרעה בבקשות יציאה — זה **שינוי תווית בלוח**, לא דיפלוי.

⚠ **אין הנחת "ראש יחיד" בשום מקום.** כל שימוש הוא `session.isHead`
לכל סשן בנפרד; חיפושי רשימה קיימים רק למדריכים (`_guides.js`).
לכן שני ראשי מכינה במקביל תקינים, וזה מה שמאפשר לסגנית לקבל
את ההכרעה כשראש המכינה בחופשה.

---

## 4מא · התיק המלא של החניך (`staff` בפרופיל)

⚠ **הבלוק נבנה רק אחרי `session.isManager`, ואינו קיים בתשובה
לחניך** — לא ריק ולא null: פשוט אינו שם. שדה שאינו נשלח אינו
יכול לדלוף מטעות בתצוגה.

⚠ **ת.ז ותאריך לידה נכללים.** הם כבר גלויים לכל הצוות בלוח
החניכים ב-monday, ולכן המסך אינו מרחיב חשיפה — הוא חוסך פתיחה
של הלוח. אם יוחלט לצמצם, `staffView` הוא המקום היחיד.

⚠ **הסיסמה לעולם אינה יוצאת** — רק `registered: true/false`.
"טרם נרשם" הוא מה שמדריך צריך כשחניך אומר שאינו מצליח להיכנס.

⚠ **`partial` מבדיל בין "נכשלה טעינה" לבין "אין נתונים".** כל
מקור נטען בנפרד ואינו מפיל את המסך, אבל מה שנכשל **מוצג** —
אחרת מדריך רואה "אין שיבוצים" על חניך שיש לו חמישה.

⚠ **מה שריק אינו מוצג** (כותרת מעל ריק נראית כמו תקלה), אבל
`partial` דווקא כן. שני הכללים הפוכים בכוונה.

⚠ `placementsFor` מחזירה את ההגדרה המלאה ולא את `toStudentDef` —
היא נועדה לצוות בלבד, והקוראת היחידה כבר אכפה `isManager`.

---

## ⚠ בדיקה שנשענת על סדר שורות בלוח

`perm-test` בחר `roster[0]` והניח בשקט ש"נעם אינו המדריך שלו".
שיבוץ אחד השתנה, והבדיקה נכשלה על התנהגות **נכונה**.

**הכלל: לבחור את נתוני הבדיקה לפי התכונה שנבדקת, לא לפי מיקום.**
עכשיו הבדיקה גוזרת את המדריך של החניך ואת מדריך אחר מהנתונים,
בודקת את שני הכיוונים (המדריך שלו כן, אחר לא), ואם אין שני
מדריכים היא אומרת זאת ונכשלת במקום לטעון טענה ריקה.

---

## 4מב · ⚠ אין קשר בין הגאנט לשיעורים

**היה קשר, הוא היה שגוי, והוא הוסר.** `_lessons-board.js` קרא את הגאנט
וסימן כל מפגש כ"מתנגש", `gantt-days.js` ייצא `lessonBlock`, והמסך הציג
מונה "דורשים בדיקה".

זה נבנה בתגובה לשאלה למה מוצג שיעור שאינו קיים — אבל **התיקון האמיתי
היה עמודת "מתוכנן" שבגיליון**, והחיבור לגאנט נשאר כשריד. הוא הוסר
במלואו: השרת, המסך, `lessonBlock`, והבדיקות.

**מה שקובע אם מפגש מתקיים הוא עמודת "מתוכנן" בגיליון, ורק היא.**
אחראי הלו״ז ממלא אותה ידנית, והגאנט אינו מעיד עליה דבר.

⚠ `shared/gantt-days.js` **נשאר**, ומשרת את **תקציב המטבח בלבד** — שם
הקשר אמיתי: סופ״ש בית וחג משנים כמה סועדים יש.

⚠ `parity-test` נועל עכשיו את **ההיעדר**: אין `conflict` על אף שורה,
אין `counts.clashing`, ולוח השיעורים אינו מייבא את הגאנט. מי שירצה
להחזיר — לשאול קודם. זה נוסה ונדחה.

---

## 4מג · חוברות מעוצבות ל-Google Sheets

⚠ **למה הייצוא הקודם היה שטוח.** `src/excel.js` בונה בדפדפן עם גרסת
ה-CDN החינמית של SheetJS, ו**היא אינה כותבת עיצוב בכלל** — מקבלת
צבעים ומשליכה אותם בשקט. שום שיפוץ שם לא היה עוזר; זו תקרה של הכלי.

⚠ **הפלטה והמבנה הועתקו מהחוברת שהמכינה בנתה ביד**, לא הומצאו:
`#4472C4` לכותרות ולעמודות תכנון, `#ED7D31` לעמודות "בפועל",
`#C6EFCE`/`#FFC7CE` לתאי כן/לא, `#F2F2F2` לפסים. המטרה אינה "יפה"
אלא **מוכר** — מי שפותח צריך למצוא את מה שהוא כבר יודע איפה נמצא.

⚠ **הדאשבורד מחשב ואינו מעתיק.** כל תא בו הוא `COUNTIF` שמצביע על
לשונית הנושא. מספר מועתק היה מתיישן ברגע שמישהו עורך שורה ביד —
והחוברת כולה נועדה להיערך ביד.

⚠ **`formulaValue` ולא `stringValue`.** נוסחה שנשלחה כטקסט מוצגת
כטקסט, והדאשבורד מת. הבדיקה נועלת את זה.

⚠ **הגיליון נוצר על ידי המנהל.** חשבון שירות אינו אדם ואין לו דרייב;
קובץ שהוא ייצור לא יופיע לאיש. המנהל פותח גיליון, משתף עם כתובת
החשבון, ומדביק קישור. יתרון שלא תוכנן: הקישור **יציב**, והדוח מתרענן
לתוך אותו קובץ.

⚠ **לשונית שאינה בדוח אינה נמחקת** — היא מדווחת כ"עודפת". מי שהוסיף
לעצמו לשונית לא אמור לגלות שהיא נעלמה.

⚠ **הבקשות מפוצלות ל-400.** חוברת השיעורים מייצרת כ-1,170 בקשות, וכולן
יחד חוצות את גבול הגודל של גוגל ונדחות בשגיאה שאינה מסבירה כלום.

⚠ **בלי `googleapis`.** החתימה (JWT RS256) נעשית עם `node:crypto`.
nodemailer נשארת התלות החיצונית היחידה.

### ⚠ שני מספרים שנראים אותו דבר ואינם

בדוח הנוכחות הכותרת אמרה "298 ימי לימוד" מעל עמודה שכתוב בה 1.
`summarize` סופר **ימים שסומנה בהם נוכחות**, ולוח השנה סופר **ימי
לימוד בשנה**. שניהם נכונים ואינם אותו דבר. עכשיו הכותרת אומרת את
שניהם במפורש, והאחוז מוצג רק מחמישה ימים מסומנים ומעלה — אותו סף
כמו במסכים (4ג).

---

## 4מד · אחריות — אוצר מילים אחד (`shared/duties.js`)

⚠ **"אילו אחריות נושא החניך" מגיעה משלושה מקורות**: עמודת התפקידים
בלוח החניכים, שיבוץ מוביל שבוע, ויו״ר ועדה/סדרה. `dutiesOf()` הוא
המקום היחיד שמאחד אותם. שלוש הגדרות מקבילות מתפצלות — זה כבר קרה
כאן עם סדר הקטגוריות, ו-`KNOWN_ROLES` יושב ב-lessons-boards.js
כשאף אחד אינו מייבא אותו.

⚠ **`DUTIES[x].tabs` מזין גם את המגירה וגם את הקיצורים במרכז
התפקיד.** עיקרון 4יט נשבר פעם אחת בגלל שתי רשימות שאמורות היו
להיות זהות; רשימה שלישית הייתה מבטיחה שזה יקרה שוב.

⚠ **ליו״ר יש מופע לכל ועדה** (`scope`), ולא אחריות אחת כללית.
`dutyKey` כולל אותו — אחרת שתי ועדות של אותו יו״ר היו חולקות
רשימת משימות אחת.

⚠ **תפקיד שאינו מוכר אינו נעלם.** `dutyInfo` נותן לו גוון נגזר‑שם
ואייקון ברירת מחדל, כמו `roleTone` במסך התפקידים.

---

## 4מה · ⚠ הגבול: לצוות אין מעקב אחרי המשימות

`api/_duty-tasks.js` היא **נקודת הקצה היחידה במאגר שבה `isManager`
אינו מרחיב גישה**, ובמכוון. בכל שאר המערכת מנהל רואה יותר; כאן הוא
רואה כלום.

זו אותה הבטחה שבגללה אין שדה "מי ביצע" בתורנויות (עיקרון 5).

**ההבחנה: לצוות יש תיבת יוצא, לא מראה.**
* כל מה שהצוות עושה זורם החוצה — כותב הצפה.
* הדבר היחיד שזורם פנימה הוא תשובה שהחניך **בחר** לשלוח.
* אין מצב "טופל", אין חותמת קריאה ואין מונה. מספר הוא תחילתו של
  דירוג, ודירוג הוא מעקב.

⚠ **ההצפה מופנית לתפקיד ולא לאדם.** אין במסך הצוות בורר חניך, ולכן
אין מאיפה שתצמח עמודה "לפי אדם". ההצפה גם נשארת נכונה אם התפקיד
עבר לחניך אחר.

⚠ **המחיר מוצהר במסך**: איש צוות לא יידע אם ההצפה טופלה ויצטרך
לשאול. זה המחיר הנכון — הוא מחזיר את הבדיקה לשיחה.

⚠ **ומה שאי אפשר להסתיר**: הלוח יושב ב-monday, ולאיש צוות עם גישה
ללוח יש גישה אליו. אין טריק שסוגר את זה. מה שהמערכת כן מבטיחה: היא
אינה בונה כלי מעקב. עמודת הבעלים מחזיקה **מזהה בלבד ולא שם**, כדי
שהלוח ייקרא כרשימה של תפקיד ולא כיומן של אדם.

⚠ **404 ולא 403** על משימה של חניך אחר — 403 מאשר שהשורה קיימת.

⚠ `scratchpad/duty-test.mjs` נועל את שלושת הכיוונים. בלעדיו הקורא
הבא יראה בסינון באג ו"יתקן" אותו.

---

## 4מו · יו״ר ועדה — עמודה משלו

⚠ **`lead` היא המדריך המלווה; היו״ר הוא חניך.** נכון ש-`lead`
פנויה טכנית (`api/_guides.js` זורק כל שורה שאינה "קבוצה"), אבל
עמודה אחת לשניהם נגמרת ביום שבו ועדה תרצה גם מדריך וגם יו״ר —
וזה בדיוק מה ש-`src/placement-guides.js` כבר מתאר.

⚠ **מזהה ושם**, בדפוס לוח השיבוצים. שם לבדו אינו מתעדכן כשהחניך
משנה שם, ואי אפשר לשאול עליו "האם אני היו״ר" בלי התאמת שמות.

### ⚠ מוקש שנמצא ותוקן

`tools/seed-placements.mjs` ו-`api/_placements-setup.js` כתבו את
`shared/placements-ids.js` עם **שלוש עמודות בלבד**, והיו מוחקים
בהרצה הבאה את `desc/hours/needs/lead`. `lead` ריקה פירושה
`val(i, undefined) === ""` ו-`guideMap()` מחזירה מפה ריקה **בשקט** —
כלומר כל בקשת יציאה מדלגת על המדריך ומגיעה ישר לראש המכינה, בלי
שגיאה ובלי שאיש ישים לב. שניהם יוצרים וכותבים עכשיו את כל העמודות.

---

## 4מז · מסמכי חפיפה

⚠ **מחוץ למחזור.** חפיפה היא ידע מוסדי שעובר בין מחזורים — אותו
נימוק שבגללו לוח המנות ולוח הבוגרים נשארים מחוץ ל-`CYCLE_BOARDS`.
המשימות וההצפות **כן** במחזור.

⚠ **פריט אחד לכל סוג תפקיד**, לא לכל ועדה. "יו״ר ועדה" הוא סוג אחד.

⚠ **אישור הקריאה נגזר מהמצב ואינו דגל**: המפתח הוא
`תפקיד@תאריך-עדכון`, ולכן מסמך שנכתב מחדש חוזר להתריע מעצמו.

⚠ **מסמך בלי תוכן אינו מסמך** — הוא מסונן ולא ייצור התראה.

---

## 4מח · לוח נוכחות שנתי — רשת חודשים

⚠ **מצב חמישי חדש: `missing`.** `days` מכיל רק תאריכים שיש להם
שורה ב-monday. הרשת הישנה הדביקה את הימים ב-flex-wrap והחור היה
בלתי נראה; רשת חודש חושפת כל תאריך חסר. בלי מצב חזותי משלו הוא
נראה כמו "יום ללא פעילות" — טענה שגויה על הנתונים, וזה עיקרון 6
בגרסה חזותית.

⚠ **סינון המחצית מעמעם ולא מסיר.** ימים מחוץ למחצית שנבחרה היו
הופכים לחורים באמצע החודש.

⚠ **`title` אינו עובד במגע**, וזה היה כל המידע שהיה בלוח. התא הוא
עכשיו כפתור שפותח שורת פירוט.

⚠ **`.kx .yr2-c` ולא `.yr2-c`** — התא הוא `<button>`, ו-`.kx button`
מאפסת background ו-border בסגוליות גבוהה יותר. אותה מלכודת תפסה
גם את `.task-box` במרכז התפקיד.

⚠ **`direction:rtl` במפורש** על הרשת, כמו ב-`.cal` של הגאנט.

---

## ⚠ בקטיק בתוך `styles.js`

הקובץ כולו הוא template literal אחד. **בקטיק בהערת CSS סוגר את
המחרוזת** והשארית הופכת לקוד. זה קרה פעמיים באותו סשן, ובפעם
השנייה `vite build` **דיווח הצלחה** בעוד הדפדפן נכשל.

**הכלל: אין בקטיקים ב-`src/styles.js` מלבד השניים שעוטפים אותו.**
בדיקה: `grep -c '\`' src/styles.js` חייב להחזיר 2.

---

## ⚠ מטמון השרת יושב בתהליך אחר

`invalidate()` בסקריפט בדיקה **אינו** נוגע במטמון של שרת הפיתוח.
בדיקה ששינתה נתון ב-monday וקראה מיד קיבלה תשובה ישנה — ובשני
מקרים בסשן הזה היא **עברה מסיבה שגויה** (400 שהגיע מ"חניך לא מוכר"
ולא מהמכסה שנבדקה).

**הכלל: להמתין על תנאי — לשאול את השרת עד שהוא רואה את השינוי —
ולא על זמן. ולבדוק את תוכן ההודעה ולא רק את הסטטוס.**

---

## 4מט · "לא התקיימה שגרת מכינה"

סוג יום חדש, ושונה מ**חופשה**: חופשה מתוכננת מראש, וזה מתגלה
בדיעבד — יום שסומן כרגיל ואז שגרת המכינה לא התקיימה בו.

⚠ **המכנה מסונן לפי סוג היום ולא רק לפי "סומן".** קודם
`summarize` ספר כל תאריך שיש לו שורה בלוח הסימונים. יום שסומן
ואז השתנה סוגו נשאר במכנה **לנצח**, כי השורה עדיין קיימת —
והחניך הופיע על 0/1 ביום שלא הייתה בו מכינה.

עכשיו הסינון בזמן החישוב, ולכן שינוי סוג היום בלוח מתקן את
האחוז **מיד ולמפרע**, בלי לגעת בשורות. יום ראשון כזה משאיר
את החניך על **0/0**.

⚠ וגם ההיעדרות והנוכחות נספרות רק בימים שנספרים — אחרת
`absent` היה יכול לעלות על `schoolDays`.

---

## ⚠ בדיקה שנוגעת ב-`?action=mark` חייבת למחוק גם ימי סימון

`quota-test` ניקה היעדרויות בלבד, ו-`stampMarked` יוצר **שורה
נוספת** בלוח הסימונים לכל תאריך. התוצאה: שלושה ימים נשארו
מסומנים בלוח האמיתי — כולל אחד בפברואר 2027 — והמכינה ראתה
ארבעה ימי נוכחות שמעולם לא היו.

**הכלל: לספור מה נוצר בכל לוח שהפעולה נוגעת בו, לא רק בלוח
הראשי.** `?action=mark` נוגע בשניים: היעדרויות **וסימונים**.

---

## 4נ · ניהול צוותים — ההיפוך המכוון של 4מה

ועדות וסדרות מנהלות את המשימות שלהן במסך משלהן
(`src/Teams.jsx`, `api/_team.js`, `api/_team-task.js`), ולא במרכז
התפקיד. זה מחליף את הגיליון ואת monday עבור הצורך הזה.

### ⚠⚠ שני לוחות שנראים דומים והם הפוכים

| | `duty.tasks` (4מה) | `team.tasks` |
|---|---|---|
| **בעלות** | של החניך בלבד | של הוועדה |
| **עמודת הבעלים** | מזהה בלבד | מזהה **ושם** |
| **מי קורא** | החניך. `isManager` **אינו** מרחיב | צוות · יו״ר · מדריך · חברי הצוות |
| **הפסקה במסך** | "המשימות כאן שלך בלבד" | "המשימות כאן משותפות" |

שניהם נכונים, ומי שיראה אותם זה לצד זה עלול **"לתקן" אחד לפי
השני**. שלוש הגנות: אזהרות הדדיות בראש `shared/duty-ids.js`
ו-`shared/team-ids.js`, הסעיף הזה, ו-`scratchpad/team-test.mjs`
שנועל את **שני** הכיוונים באותה הרצה — הצוות כן כותב בלוח
החדש, והצוות עדיין מקבל 403 על `?action=duty-tasks`.

**הגבול חי במסלול הקריאה ולא בשורה.** לכן שני לוחות, שתי נקודות
קצה ושני מסכים — ולא סינון אחד על לוח משותף, שמישהו "יתקן"
בעוד שנה בלי לדעת שהוא שובר הבטחה.

### ⚠ אוצר המילים הוא הדינמיות

לוח `צוותים – אוצר מילים` מחזיק את הסטטוסים והשלבים. **`closes`
("נחשב סגור") הוא כל הסמנטיקה של התקדמות, והוא בלוח** — מנהל
המכינה שמוסיף "ממתין לאישור" מחליט בעצמו אם הוא סוגר משימה,
בלי דיפלוי. זה עיקרון 1 במלוא היקפו.

* **אין מחיקה, יש `archived`.** משימות נושאות את המזהה, ומחיקה
  הייתה משאירה אותן עם ערך שאינו נפתר לכלום. שורה מארכבת
  **עדיין נפתרת לתצוגה** ומוסתרת מהבורר בלבד.
* **סטטוס שאינו נפתר נספר כפתוח, לעולם לא כסגור, ומדווח
  ב-`warnings`.** ברירת מחדל של "סגור" הייתה מקפיצה את אחוז
  ההתקדמות בלי שאיש יידע — 4ט בגרסה מסוכנת.
* **בלי אף סטטוס סוגר `pct` הוא `null` ולא 0**, והמסך אומר
  שההגדרה חסרה. 0% נראה כמו נתון (עיקרון 6).
* **`kind` לא מוכר נספר ומדווח ולא מושמט** — בדיוק כמו
  `CATEGORIES.includes` ב-`_placements.js`, ששם שורה עם קטגוריה
  לא-מוכרת אינה מופיעה בשום מסך ואינה יוצרת שום שגיאה.

### ⚠ `owner ≠ הכותב` — לראשונה במאגר

בלוח האישי הבעלים הוא תמיד `session.itemId` ואין מה לאמת. כאן
משייכים לאדם אחר, ולכן **אימות כפול**: `activeStudents()` (קיים
ופעיל) **וגם** `membersOf(placementId)` (בצוות הזה). אימות אחד
בלבד משאיר משימה משויכת למי שאינו בצוות, או למזהה שרירותי.

**וחבר צוות אינו משייך לאחרים** — רק היו״ר והמדריך המלווה.

### ⚠ 403 על הצוות, 404 על מזהה משימה

הוועדה עצמה אינה סוד — היא מופיעה בפרופיל של כל מי שמשובץ
אליה, ולכן `?action=team` מחזיר 403 ואומר "אינו שלך". מה שיש
**בתוכה** כן: `?action=team-task` על מזהה של צוות אחר מחזיר
**404**, כי 403 מאשר שהשורה קיימת. הבדיקה תפסה כאן 403 בגרסה
הראשונה.

### ⚠ `mayTeam` ב-shared, לא `withAuth`

הדגלים של `withAuth` הם AND, והשאלה כאן היא איחוד: איש צוות
**או** המדריך המלווה **או** היו״ר **או** חבר הצוות. אותו דפוס
בדיוק כמו `mayArea` (4כב), ומאותה סיבה הוא יושב ב-`shared/team.js`
— כדי שהמסך יסתיר בדיוק את מה שהשרת יחסום.

**חבר צוות רואה את כל המשימות ולא רק את שלו.** ועדה שכל אחד
בה רואה רק את שלו אינה ועדה, היא רשימת מטלות.

### ⚠ ההצפות — בלי מרחב מפתחות שני

ההצפה מהוועדה ממשיכה להיות מופנית ל-`יו״ר#<מזהה>` — מפתח שכבר
קיים, כבר נישא ב-`dutiesForStudent`, ושורד החלפת יו״ר. מפתח
בשם `צוות#<מזהה>` היה מכפיל את הערוץ ומייצר שתי תיבות דואר
לאותה שאלה. **התיבה הנכנסת יושבת במרכז התפקיד, ומהמסך הזה יש
אליה קישור אחד בכיוון אחד** — לא לשונית שמשכפלת אותה.

### ⚠ שלושה באגים שנמצאו בדרך ותוקנו

1. **יו״ר שהוסר מהוועדה שמר על ההרשאה.** `?action=placements`
   מחק את שורת השיבוץ ולא נגע ב-`chair`; `chairMap()` המשיכה
   להעניק לו אחריות. עד היום זו הייתה אחריות-רפאים, ומרגע שיש
   לוח צוות זו **הרשאת ניהול ששורדת את ההסרה**. הניקוי נעשה
   באותה בקשה, ו-`chairCleared` מוחזר כדי שהמסך יאמר זאת.
2. **חניך לא-פעיל הקפיא את עורך השיבוצים.** `picked` אותחל מכל
   המשובצים בעוד הרשימה מסוננת ל-`activeStudents`, וחניך שכובה
   נשאר **מסומן בלי תיבה לבטל** — שמירה נכשלה ב-400 לנצח.
   עכשיו הוא מוסר מהבחירה **ומדווח** מעל הרשימה.
3. **`Number(cap)` על תוכן לא-מספרי נותן NaN**, ו-`NaN != null`
   הוא `true` אבל `length > NaN` תמיד `false` — האכיפה
   **מתבטלת בשקט** והמסך מציג "X/NaN". `Number.isFinite` בשרת
   ובמסך, ו-400 במסלול הכתיבה.

### ⚠ ומה שנחסם בכוונה

**שינוי תקופה שמותיר שורות יתומות.** מסך השיבוצים מסונן לפי
`placement + semester`; ועדה שנוצרה בייבוא נכנסה בלי `period`,
נחשבת "שנתי", ומרגע שתשונה ל"לפי סמסטר" — השורות הישנות לא
יופיעו עוד באף מסך **ולא יהיה מקום למחוק אותן**. העורך חוסם
ואומר איפה הן יושבות.

---

## 4ס · הצפה בתוך ההקשר, וקטגוריה חמישית

### ⚠ מסך ההצפות הנפרד נמחק

`src/DutyNotes.jsx` היה דף שבו איש צוות **בוחר תפקיד מרשימה**
ואז כותב. הבחירה ההיא הייתה כל הבעיה: מי שעומד מול כרטיס
"אחראי מטבח" ורוצה להעיר לו משהו לא צריך לעזוב את המסך, למצוא
דף אחר, ולבחור שם שוב את מה שכבר היה מול העיניים שלו.

`src/Escalate.jsx` הוא רכיב אחד שיושב **בתוך** ההקשר, בשלושה
עוגנים: כרטיס התפקיד ב-`RoleHolders`, כרטיס "מהות התפקיד"
ב-`WeekLeaders`, ולשונית "הצפות" בכל ועדה, סדרה וצוות.

* **המנגנון לא השתנה** — `api/_duty-notes.js` נשאר כפי שהוא,
  וכך גם הגבול של 4מה: תיבת יוצא, לא מראה.
* ⚠ **נטען רק כשההקשר פתוח.** `rl-body` אינו מרונדר כשהכרטיס
  סגור, ולכן רשימה של עשרה תפקידים אינה מייצרת ולו בקשה אחת.
  **אין להטמיע את `Escalate` מחוץ ל-`isOpen`.**
* ⚠ **מפתח האחריות מיובא ולא מוקלד** (`dutyKey`, `DUTY_CHAIR`,
  `DUTY_LEADER`). מפתח שגוי בתו אחד נשמר, מופיע לשולח בתיבת
  היוצא, **ולעולם לא מגיע** — כי הפעמון משווה מול `dutyKey`.
* ⚠ **למוביל שבוע לא היה נתיב הצפה בכלל.** `shared/duties.js`
  מדלג עליו ברשימת התפקידים והוא אינו בעמודת התפקידים — הוא
  נגזר משיבוץ. הכרטיס ב-`WeekLeaders` הוא ההקשר היחיד שלו.

### ⚠⚠ החור ב-DELETE של ההצפות

הבדיקה היחידה הייתה `!session.isStudent`, ו-`deleteItem`
ב-`api/_items.js` שולחת `delete_item(item_id:…)` **בלי
`board_id`** — כלומר כל איש צוות יכול היה למחוק **כל שורה בכל
לוח במערכת** בכך שישלח מזהה שרירותי. חניך, שיעור, יום נוכחות,
בקשת יציאה.

כשזה ישב במסך אחד זה היה חבוי; מרגע שכפתור המחיקה יושב בתוך כל
כרטיס תפקיד ובכל ועדה, זה מסלול פתוח.

עכשיו המזהה מאומת מול **לוח ההצפות** ומול **מי ששלח**, ומחזיר
**404** ולא 403. `duty-test` שולח מזהה מלוח אחר ומוודא גם את
הסטטוס **וגם שהשורה שרדה** — בלי הטענה השנייה, 404 שמגיע אחרי
מחיקה מוצלחת נראה בדיוק כמו הצלחה.

⚠ **וההערה ב-GET תוקנה לפי הקוד, לא ההפך.** היא אמרה "הצוות
רואה מה **הוא** שלח" והקוד החזיר את הכול. ההצפה מופנית לתפקיד,
ואיש צוות שלא יראה שמישהו כבר הציף את אותו דבר יציף אותו שוב —
וזה בדיוק מה שהערוץ נועד למנוע. מה שכן נוסף: `mine` **נגזר
בשרת** ונשלח, כדי שכפתור המחיקה יופיע רק על מה שאני שלחתי.
השוואת שמות בלקוח הייתה נשברת ביום שאיש צוות משנה את שמו.

### ⚠ הקטגוריה החמישית: "צוות מזדמן"

צוות שקם באמצע שנה — יום הזיכרון, טקס. אינו ועדה קבועה ואינו
סדרה, ומנהל המכינה מקים אותו **מהמסך** (`?action=team-admin`,
לשונית "צוותים מזדמנים"). מתנהג כמו ועדה: שיבוץ, יו״ר, משימות.

תו בתו: `צוות מזדמן` — יחיד, רווח יחיד, בלי גרשיים. שם הרבים
`צוותים מזדמנים` חי ב-`CATEGORY_PLURAL` בלבד ולא בלוח.

⚠ **היו חמש רשימות סדר קטגוריות מקובעות בארבעה קבצים, ובשני
סדרים סותרים.** ההערה בראש `shared/duties.js` הזהירה מ"שלושה
מקומות"; בפועל היו חמישה, ושניים מהם הפוכים מ-`CATEGORIES`.
כל אחד מהם `indexOf` על מערך סגור — כלומר קטגוריה חדשה מקבלת
**-1 וקופצת לראש המיון**, בלי שגיאה. עכשיו `categoryRank` /
`byCategory` ב-`shared/placements.js` הם המקור היחיד, וערך
לא-מוכר יורד **לסוף**.

⚠ **וגם שם הרבים היה שרשרת טרנרי שנפלה ל-else** — במסך
השיבוצים הופיעו שתי לשוניות בשם "קבוצות", זו לצד זו.
`plural()` הוא המקור, והוא מחזיר על ערך לא-מוכר **את עצמו**.

⚠ **`CHAIRABLE` ו-`TEAM_CATEGORIES` היו שתי רשימות מקבילות.**
קטגוריה שנוספת לאחת ולא לשנייה נותנת צוות שאפשר לנהל בו
משימות אבל אי אפשר לקבוע לו יו״ר. `CHAIRABLE = TEAM_CATEGORIES`.

### ⚠ הוספת תווית לעמודת status — מה באמת עובד

`update_status_column` נדחה כאן ב-`argument: id is required`:
החתימה שעובדת היא של `scratchpad/fix-branches.mjs` —
`update_status_column(board_id:$b, id:$c, settings:$s, revision:$r)`
עם `$s:UpdateStatusColumnSettingsInput!` ו-`$r:String!`, ושדות
`{id,label,index,color,is_deactivated}`. `revision` אינו
ב-`settings_str` וצריך לשאול אותו כשדה נפרד.

המסלול שהצליח בפועל היה **הנפילה לאחור**: כתיבה זמנית עם
`create_labels_if_missing:true` על שורה קיימת ומיד שחזור הערך
הקודם. ⚠ ואחריה **אימות בקריאה חוזרת שכל תווית ישנה שרדה** —
`update_status_column` דורס את כל הרשימה, וקריאה שגויה מוחקת
בשקט ארבע קטגוריות ואיתן כל שורה בלוח.

### ⚠ שלושה באגים שנתפסו בגלל השינוי הזה

1. **`renameItem(board, itemId, name)` — שלושה ארגומנטים.**
   `_team-admin.js` ו-`_team-task.js` קראו לה בשניים, `name`
   נשאר `undefined`, `$n:String!` הגיע חסר, וכל שינוי שם נפל
   ב-502 גנרי. `team-test` היה ירוק כי כל POST-עם-`id` בו נחסם
   ב-400 **לפני** שורת ה-rename. הטענה החדשה עושה שינוי **שם
   בלבד**.
2. **`byCategory` ו-`CATEGORIES` נקראו בלי ייבוא** ב-
   `api/_placements.js` וב-`src/Mechina.jsx` — שני `ReferenceError`
   חיים ש-`vite build` **לא** תופס, כי הם זמן ריצה.
3. **`teamCategories` מקובע בלי `adhoc`** ב-`?action=team-admin`,
   בעוד `isTeam` באותה תשובה כן כלל אותו — תשובה אחת שסותרת
   את עצמה.

### ⚠ אחרי יצירה — כרטיס הצלחה, לא קפיצה

`invalidatePlacements()` מנקה את המטמון **במופע שטיפל בבקשה
בלבד**, והוא בן 30 שניות. בייצור הבקשה הבאה עשויה לנחות על
מופע אחר ולקבל "הצוות אינו נמצא" על צוות שזה עתה נוצר — יצירה
מוצלחת שנראית בדיוק כמו כישלון. ולחיצה שנייה על "צוות חדש"
הייתה מחזירה «כבר קיים בלוח ההגדרות», שני מסרים סותרים ואף
אחד מהם לא נכון.

---

## 4ע · שלוש רמות הרשאה, ושער אחד לכל אחת

### ⚠ צפייה בלבד — עמודה בלוח, אכיפה ב-`withAuth`

`AUTH_COLS.viewOnly` בלוח ההרשאות. מנכ״ל העמותה רואה את כל
המערכת ואינו משנה בה דבר.

**האכיפה במקום אחד**: `withAuth` דוחה כל בקשה שאינה `GET`.
לא בכל נקודת קצה בנפרד, משתי סיבות — יש עשרות מסלולי כתיבה
ומי שיוסיף את הבא לא יזכור להוסיף בדיקה, וההבחנה לפי
`req.method` מדויקת ממילא. **מסלול שייכתב מחר מוגן מעצמו.**

* ⚠ **ריק = הרשאה מלאה.** הקוטביות ההפוכה היא כל העניין: כל
  שורה שקיימת נכתבה לפני שהעמודה נוספה, ותיבה ריקה שמשמעותה
  "צפייה בלבד" הייתה נועלת את **כל** הצוות בשקט.
* ⚠ **`logout` הוא POST ואינו עטוף ב-`withAuth`** — מי שנחסם
  חייב להיות מסוגל לצאת.
* ⚠ **והחיווי במסך** (`.ro-bar`), כי מי שכל כפתור מחזיר לו 403
  יסיק שהמערכת שבורה.

### ⚠ קריאה לכל הצוות, כתיבה לבעל התחום

`{ kitchen: true }` ו-`{ scheduler: true }` מרשים לכל כניסת
צוות (`isManager === kind === "manager"`) — ולכן **מדריך יכול
היה לערוך את תקציב המטבח ואת גיליונות המרצים**. לא כי מישהו
החליט, אלא כי `isManager` הוא הדגל הרחב ביותר במערכת.

`withAuth(h, { …, edit: "kitchen" })` מצמצם את **הכתיבה בלבד**
ל-`mayEdit` שב-`shared/edit-rights.js`: ראש המכינה (וזה כולל
את הסגנית — תווית בלוח ולא זהות בקוד, 4מ) ובעל התחום.
הקריאה נשארת פתוחה, וזו הכוונה.

* ⚠ **`canEdit` בשלושה מסכים חישב את הכלל מחדש.** עכשיו
  שלושתם קוראים ל-`mayEdit` — אחרת הכפתור מופיע והשמירה נופלת
  ב-403 אחרי שהמשתמש כבר הקליד (4יד).
* ⚠ **ההודעה אומרת מי כן רשאי** ולא "אין הרשאה".
* ⚠ **`_lesson-mark.js` נשאר פתוח** — מוביל שבוע מדווח קיום
  מפגשים, וזו החלטת המכינה.

### ⚠⚠ עקיפת סיסמה שנסגרה

`/api/students?action=login` נתן **סשן מלא** לכל מי שמחזיק
תעודת זהות של חניך — **גם אחרי שהחניך בחר שם משתמש וסיסמה**.
תעודת זהות מופיעה בכל טופס ומוכרת לחברים לכיתה.

`api/_signin.js` כבר סגר את זה במסלול שלו ("רק שורות
`isFresh`"), והנקודה הזו נשארה פתוחה — **היא אינה נקראת מאף
מסך**, ולכן איש לא הבחין. עכשיו חניך רשום מקבל 409.

⚠ **ומה שנשאר פתוח, במודע:** חניך שטרם נרשם עדיין מקבל שם
סשן מלא. אין לו סיסמה לעקוף, וזו התנהגות מהיום הראשון — אבל
היא אינה מה ש-`_signin.js` עושה. הפער נשאר כי שש חבילות בדיקה
נכנסות דרך הנקודה הזו כחניכים שטרם נרשמו. **הדרך לסגור אותה
היא להעביר את הבדיקות לחשבון עם סיסמה.**

### ⚠ "כבר נרשמת" — החריג היחיד לכלל ההודעה האחידה

מי שנרשם פעם אחת חוזר כעבור חודש, זוכר שנכנס עם תעודת הזהות,
מקליד אותה בשני השדות, ומקבל "שם משתמש או סיסמה שגויים" —
הודעה **נכונה ולא עוזרת**. הוא מסיק שהחשבון נמחק.

ההודעה יוצאת **רק כששני השדות זהים זה לזה** ושניהם תואמים את
הסוד שבלוח — כלומר רק למי שכבר מחזיק בת.ז או בקוד ועשה בדיוק
את מחוות הכניסה-הראשונה. זה אינו מנוע בדיקה: אי אפשר לשאול
אותו "האם X רשום" בלי לדעת כבר את הסוד של X.

⚠ **ובלי `penalize`.** ההודעה יוצאת למי שהוכיח שהוא מחזיק
בסוד — והוא בדיוק האדם שהיא נועדה לו. השהיה גוברת עליו
פירושה שמי שחוזר אחרי חודש ננעל לשש דקות. והיא גם אינה
מוסיפה הגנה: המסלול דורש ידיעת הסוד מראש.

### ⚠ "נספר" ו"ניתן לשיבוץ" הן שתי שאלות

`demo` נבנה כדי שחשבון הבדיקה לא יזייף נוכחות ומכסות (4לא),
ואותו סינון הוציא אותו גם מ**רשימת הבחירה** של עורך השיבוצים
— כלומר החשבון שנועד לבדיקות לא יכול היה להשתתף בדבר שצריך
לבדוק.

`assignableStudents()` היא `activeStudents({ withDemo: true })`,
והיא נקראת **רק ברשימות בחירה**: עורך השיבוצים, בורר היו״ר,
וחברי הצוות. ⚠ **כל שימוש בה שנוגע במונה, בממוצע או במכסה
הוא באג.** הכיוון נשאר הנכון — מסך חדש שיקרא `activeStudents()`
בלי ארגומנט יסנן אותו מעצמו.

---

## 4פ · פרטי החניך, ולוח החודש

### ⚠ שבעה שדות שכבר היו בלוח

טלפון, אימייל פרטי, עיר מגורים, אלרגיה/רגישות, הגדרה דתית, מין
ומידת חולצה מופיעים עכשיו בפרופיל של החניך **ובתיק שהצוות רואה**.

**לא הרצנו שום ייבוא.** לוח המצבה נבנה מרשימת החניכים של המכינה
והעמודות כבר מלאות (33 מתוך 34). כל מה שנעשה הוא לחבר אותן.

* ⚠ **`shirt` היא עמודת ה-status ולא עמודת המספר.** בלוח יש
  **שתי** עמודות בשם "מידת חולצה": אחת `numbers` וריקה לגמרי,
  ואחת `status` ובה 33 ערכים. קריאה מהמספרית מחזירה ריק — בלי
  שגיאה, ובלי שאיש יבין למה המידה לא מופיעה.
* ⚠ **`mail` היא הכתובת הפרטית ואינה כתובת הכניסה.** בלוח יש
  גם עמודת "אימייל" (`text_mm6kdeb6`) שהיא כתובת ההתחברות
  והאיפוס. ערבוב ביניהן משנה למישהו את חשבון הכניסה בלי שהתכוון.
* ⚠ **טלפון עובר `phoneHe`.** monday מחזירה `972527043037` —
  נכון טכנית, בלתי קריא, ואי אפשר להקליד אותו לטלפון כמות שהוא.
* ⚠ **אלרגיה מובלטת ואינה שורה ברשימה** (`.pf-alert`). זה הנתון
  היחיד כאן שיש לו משמעות מיידית, ומי שמבשל לא סורק שמונה שורות
  אפורות כדי למצוא אותו.

**⚠⚠ ומה שנשאר בלוח ואינו נקרא לעולם:** כתובת מגורים מלאה, שמות
ההורים, טלפוניהם, תעסוקתם, כתובות המייל שלהם, **קופת חולים
ובעיה רפואית**. הם בלוח כי המכינה שמה אותם שם — והמערכת אינה
קוראת אותם, אינה מחזירה אותם ואינה מציגה אותם. זה 4יג, והפעם
לא כהחלטה על ייבוא אלא כהחלטה על **קריאה**.

`shared/mechina-boards.js` מייצא שישה מזהים בלבד, ו-`_student-rows.js`
שולף שישה שדות בלבד. מי שיוסיף שדה שם — לשאול קודם למה.

### ⚠ לוח הנוכחות — חודש אחד, לא עשרה

רשת עשרת החודשים הפכה כל תא לשישה פיקסלים: קריאה כרשת, בלתי
קריאה כתאריך. עכשיו חודש אחד בגדול (`.mv`), ודפדוף בין החודשים.

* ⚠ **נפתח על החודש הנוכחי, ורק בפעם הראשונה.** `mi === null`
  פירושו "עוד לא נבחר", והבחירה נעשית ב-`useEffect` **אחרי**
  שהחודשים מחושבים. ברירת מחדל של `0` הייתה פותחת תמיד בספטמבר
  גם בפברואר, ובחירה בכל רינדור הייתה מחזירה את המשתמש לחודש
  הנוכחי אחרי שדפדף.
* ⚠ **`key2` מרופד.** `${y}-${m}` נותן `"2026-9"` ולעולם לא
  יתאים ל-`"2026-09"` — התאמה שנכשלת בשקט ותמיד פותחת בחודש
  הראשון.
* ⚠ **שעון ישראל ו-`?date=`.** `new Date()` גולמי היה פותח על
  החודש הלא-נכון בערב וב-UTC.
* ⚠ **החץ מושבת ולא מוסתר** בקצוות — כפתור שנעלם מזיז את
  הכותרת בכל דפדוף, וזה נראה כמו קפיצה.

---

## 4צ · תורניות

חמישה לוחות, נתב שביעי (`api/chores.js` — **7 מתוך 12**), ומסך
אחד לארבעה קהלים: אב הבית משבץ, אחראי המטבח עורך את היומי,
תורן היום מסמן צ׳ק ליסט, וכל חניך רואה איפה הוא ואיפה כולם.

### ⚠ שני סוגי תורנות, ולא אחד עם דגל

**סוף יום** — גזרת ניקיון, חצי שעה עד שעה, שיבוץ **לשבוע**.
**יומי** — מטבח וחד״א, יום שלם, שיבוץ **לתאריך**, ולו צ׳ק ליסט.

הם נראים דומים ואינם: אחד שבועי והשני יומי, לאחד יש צ׳ק ליסט
ולשני אין, **ואחד מפקיע את השני** — תורן מטבח מופרש מתורנות
הערב שלו. דגל בוליאני אחד היה מסתיר את כל זה.

⚠ **שורת שיבוץ נושאת שבוע או תאריך, לא את שניהם.** שורה עם
שניהם או בלי אף אחד לא הייתה מופיעה בשום מסך — היא **מדווחת**
ב-`warnings` ואינה מושמטת בשקט (4ט).

⚠ **התקופה היא שבוע הובלה ולא שבוע קלנדרי.** אב הבית מסובב
תורניות כשקדנציה מתחלפת, ושתי מערכות שבועות מקבילות היו
נפרדות זו מזו כבר בחג הראשון.

### ⚠⚠ אדום כאן אינו "בעיה" אלא "תורו"

בכל שאר המערכת אדום הוא חוסר או חריגה. בטבלת המעקב הוא אומר
שהחניך שובץ **פחות** מהממוצע ולכן הוא המועמד הבא. זה הפוך
לאינטואיציה, ולכן המקרא אומר את זה **במילים**: "הרבה מתחת —
תורו הבא".

* ירוק — מעל הממוצע · צהוב — בערך בממוצע או תורנות אחת מתחת ·
  אדום — יותר מכך.
* ⚠ **בתחילת שנה כולם על אפס והממוצע אפס**, כלומר כולם בדיוק
  בממוצע וכולם צהובים. זה נכון, ו-`hasData` הוא מה שמאפשר
  למסך לומר "אין עדיין מה להשוות" במקום לצייר טבלה שנראית
  כמו מסקנה — אותו כלל כמו אחוז נוכחות מחמישה ימים (4ג).
* ⚠ **הממוצע מחולק בכל החניכים ולא במי ששובץ.** אחרת חניך
  שטרם שובץ לא היה נכנס למכנה, והממוצע היה עולה עם כל שיבוץ.

### ⚠ ההמלצה — שתי רמות, ושתיהן הצעה

אב הבית יודע דברים שהמערכת אינה יודעת: מי חולה, מי במיונים,
מי כבר עשה טובה. ההמלצה מנוסחת כשאלה ואינה בוחרת.

**שתי רמות ולא אחת:** יש אדומים → "שווה לשקול", חזק. אין → מי
שמתחת לממוצע, "הכי מאחור כרגע". בלי הרמה השנייה אב הבית היה
מקבל מסך ריק בדיוק בתחילת שנה, כשהוא הכי צריך עזרה. שתיהן
באותו מראה היו הופכות את החזקה לרעש.

⚠ **וההמלצה מחושבת בלי מובילי השבוע** — הצעה לשבץ את מי שאי
אפשר לשבץ מלמדת להתעלם מההמלצות.

### ⚠ מוביל שבוע — חסום, ומסומן

מוביל אינו עושה תורנות בשבוע שהוא מוביל. זו **חסימה בשרת**,
לא אזהרה, והיא חלה גם על גזרת הערב וגם על תורנות יומית שנופלת
בתוך אותו שבוע.

⚠ **ובמסך הוא מסומן ולא מוסתר.** חניך שלא יראה את עצמו ברשימה
יחשוב שנשכח, ולא שהוא פטור. הוא גם מקבל התראה משלו בפעמון,
מאותה סיבה בדיוק.

### ⚠ פיזור התורנים היומיים — מתריע ואינו חוסם

שלושה תורנים ליום, והם מופרשים מתורנות הערב שלהם — ולכן לוקחים
אחד או שניים מכל גזרה. יותר מ-`SAME_SECTOR_WARN` מאותה גזרה
**מתריע**: "הגזרה תישאר חסרה באותו ערב".

חסימה כאן הייתה שולחת את אב הבית לעקוף את המערכת. יום שבו אין
ברירה הוא מצב אמיתי, והמסך מראה לצד כל מועמד באיזו גזרה הוא
יושב באותו שבוע.

### ⚠ הצ׳ק ליסט — רק תורן היום מסמן

לא אב הבית, לא אחראי המטבח ולא ראש המכינה. הם **עוקבים**.
סימון שמישהו אחר עשה במקום התורן הופך את הצ׳ק ליסט מכלי
לרישום. `chores-test` נועל את זה משני הכיוונים.

* ⚠ **קיום שורה = בוצע.** אין עמודת "בוצע", וביטול הוא מחיקת
  שורה — ולכן אין מצב שלישי שקוף שבו השורה קיימת ומסומנת "לא".
* ⚠ **אידמפוטנטי.** שני תורנים שלוחצים כמעט יחד שולחים אותה
  כוונה ומקבלים אותה תוצאה (עיקרון 5).
* ⚠ **מטלה נמחקת מארכבת ולא נמחקת** — שורות ביצוע נושאות את
  המזהה, ומחיקה הייתה משאירה מעקב היסטורי בלי שם.

### ⚠ מה נחסם בכוונה

* **גזרה יומית שנייה** — `dailySector()` מחזירה את הראשונה,
  והשנייה הייתה קיימת בלוח ובלתי נראית בכל מסך.
* **שינוי סוג גזרה שיש לה שיבוצים** — שורות ערב נושאות `week`
  ושורות יומיות `date`; החלפת הסוג הופכת את כולן לשורות
  שבורות שאף מסך לא יציג, **ולא יימחקו לעולם כי אין להן מסך**.
* **מכסה לא-מספרית** — `Number("שלושה")` הוא NaN, וכל השוואה
  מולו `false`, כלומר האכיפה מתבטלת בשקט. אותו באג כמו במכסת
  השיבוצים (4נ).

### ⚠ תיקון ספירה הוא שורה, לא מספר שנדרס

"היה כצופר" ו"התחלף עם מישהו" הם שני אירועים, וכל אחד ראוי
לסיבה משלו. לוח `התאמות` מחזיק שורה לכל תיקון עם `delta` וסיבה;
מספר אחד שנדרס היה מוחק את ההיסטוריה של מה שקרה.

### ⚠ הנהלים כטקסט נערך

`src/TextBlock.jsx` + לוח `טקסטים באפליקציה`. **המפתח הוא שם
השורה** ולא מזהה — בלוק שנמחק בטעות חוזר לחיים ביצירת שורה
חדשה עם אותו שם, בלי לתקן קוד. **ראש המכינה בלבד** עורך: אלה
נהלים של המכינה, לא טקסט של מסך.

⚠ `white-space: pre-wrap` — הטקסט נשמר עם שבירות שורה שמישהו
כתב בכוונה, ורינדור שמוחק אותן הופך רשימה של תשעה סעיפים
לפסקה אחת.

⚠ **המבנה של הצ׳ק ליסט חולץ מהעיצוב שבמסמך ולא נוחש.** כותרת
אזור מסומנת שם ב-bold+underline והמטלות תחתיה רגילות — אבל שתי
שורות מסומנות bold ובכל זאת הן מטלות. החלוקה נבדקה ידנית;
הסקה אוטומטית הייתה נראית סמכותית והייתה שגויה.

### ⚠ הפעמון משלם על כל בונה חדש

`choreNotes` נבדק ב-`isStudent` **ראשון** ואז ב-`choresReady`.
בלי השער הזול הזה נוספות חמש קריאות לוח לכל חניך מחובר כל שלוש
דקות. והתראת תורנות מטבח מוגבלת לשלושה ימים קדימה — התראה על
תורנות בעוד שנים־עשר יום אינה פעולה, והיא מאמנת להתעלם.

---

## 4ק · מה שנמצא אחרי הסבב הראשון של התורניות

### ⚠⚠ `.kx .rows{overflow:hidden}` — הפעם השלישית

`.ch-pick{max-height:44vh;overflow-y:auto}` הוא (0,1,0), ו-
`.kx .rows{overflow:hidden}` שבשכבת ההרמה הוא (0,2,0) — כלומר
**גובר גם כשהכלל שלנו מאוחר יותר בקובץ**. הרשימה של 33
החניכים נחתכה בשמונה בלי שום רמז שאפשר לגלול.

`.rl-pick` ו-`.scroll-y` כבר נתקלו בזה, ושתיהן נושאות הערה.
**רשימה גוללת חדשה בתוך `.rows` חייבת את הקידומת המלאה
(`.kx .rows.X`), או להשתמש ב-`.scroll-y` הקיימת.**

### ⚠ המגירה נגזרת מ-`DUTIES` ואינה כתובה ביד

בקבוצת "תפקידים" ישבה רשימה ידנית של שישה תנאים
(`auth.isContainer ? [...] : []`), והיא הייתה **רשימה שנייה**
לצד `DUTIES[x].tabs` שמזינה את הקיצורים במרכז התפקיד. ההערה
בראש `shared/duties.js` הזהירה מזה במפורש, ועיקרון 4יט נשבר
בדיוק כך פעם אחת.

עכשיו יש מקור אחד: **מסך שנוסף ל-`DUTIES` מופיע מעצמו גם
במגירה וגם במרכז התפקיד**, בלי לגעת ב-`Mechina.jsx`.

* ⚠ **איחוד לפי `tab`** — מוביל שבוע ואחראי לו״ז חולקים את
  "שיעורים במכינה", וקישור כפול נראה כמו באג.
* ⚠ **`TAB_ICON` נפרד מ-`DUTIES[x].icon`** — האחרון מתאר את
  **התפקיד**, ושני מסכים של אותו תפקיד היו מקבלים אותו אייקון.
  מסך בלי ערך במפה מקבל ברירת מחדל ואינו נעלם (4יא).
* ⚠ **מרכז התפקיד נשאר ראשון ואינו ב-`DUTIES`** — הוא אינו
  מסך של תפקיד מסוים אלא הדלת אל כולם.

### ⚠ הנהלים יושבים עם הצ׳ק ליסט, לא בהגדרות

התורן שמסמן "ניקיון מקררים" צריך לדעת גם מה הכלל על אחסון
מזון — והוא לא יעזוב את המסך כדי לחפש אותו בלשונית שהוא לא
נכנס אליה. הלשונית נקראת **"צ׳ק ליסט ונהלים"**.

⚠ **ועריכת המטלות באותו מסך שבו רואים אותן.** מי שקורא מטלה
ורוצה לנסח אותה מחדש לא צריך לעבור מסך, למצוא אותה שוב
ברשימה של 33 ולזכור מה רצה לשנות — זו בדיוק הטעות של מסך
ההצפות (4ס).

⚠ **`headText` נפרד מ-`assign`.** עריכת נוסח היא ראש המכינה
בלבד; עריכת מטלות היא גם אב הבית וגם אחראי המטבח. אלה שתי
שאלות, ודגל אחד לשתיהן היה פותח נהלים לאב הבית או סוגר
מטלות לאחראי המטבח.

### ⚠ טבלת הגזרה בתוך הגזרה

אב הבית בוחר **בכרטיס הגזרה**, ומספרים שיושבים בלשונית אחרת
דורשים ממנו לזכור אותם בעל פה בזמן שהוא מסמן שמות. זו בדיוק
הנקודה שבה הוא יפסיק להסתכל עליהם.

⚠ **והמקרא מושמט בגרסה הדחוסה** — הוא כבר בלשונית המעקב,
ושכפול שלו בכל אחת מחמש הגזרות הופך אותו לרעש שמפסיקים לקרוא.

### ⚠ מחיקת צוות — רק כשהוא ריק

`?action=team-admin` DELETE מוחק **צוות בלי שיבוצים ובלי
משימות**. צוות עם תוכן נחסם, וההודעה אומרת **כמה יש** ומה
לעשות במקום: `archived` שומר את הכול ומוריד אותו מהרשימה.

מחיקה שקטה של 40 משימות ועדה היא בדיוק סוג הפעולה שאי אפשר
לתקן. ⚠ ושלושה לוחות נבדקים ולא אחד, ⚠ והמזהה מאומת מול לוח
ההגדרות לפני `deleteItem` — שהיא שולחת `delete_item` בלי
`board_id` (4ס).

⚠ **ואישור בתוך המסך ולא `confirm()` של הדפדפן** — הוא נראה
זר, ובחלק מהדפדפנים במובייל הוא נחסם לגמרי, כלומר הכפתור
פשוט לא עושה כלום.

---

## 4ר · לשוניות שנגללות, ומה שנוסף אחריהן

### ⚠ `src/Tabs.jsx` — לשונית שנבלעת אינה קיימת

רצועה עם `overflow-x:auto` **נראית שלמה כשהיא נחתכת**: אין פס
גלילה במגע ואין שום רמז שיש עוד. מי שלא יודע שאפשר להחליק לא
מגיע ללשונית החמישית, והיא פשוט לא קיימת מבחינתו.

`<ScrollTabs className="…">` עוטף כל רצועה — `tm-tabs`, `seg`,
`duty-bar` — ומוסיף חצים **שמופיעים רק כשיש מה לגלול**.
מוחל על: תורניות, ניהול צוותים (שתי רמות), שיבוצי חניכים
(שבע לשוניות), ומרכז התפקיד.

* ⚠ **`scrollLeft` ב-RTL אינו עקבי בין דפדפנים** — שלילי בכרום
  ובפיירפוקס, וספור-מהימין בגרסאות ישנות. הבדיקה משתמשת
  ב**ערך המוחלט** ובמרחק מהקצה, ולכן נכונה בשלוש המוסכמות בלי
  לזהות דפדפן.
* ⚠ **`ResizeObserver` גם על הילדים ולא רק על המכל** — לשונית
  שנוספת אחרי טעינת נתונים אינה משנה את רוחב החלון, והחץ היה
  נשאר מוסתר.
* ⚠ **החצים `aria-hidden` ו-`tabIndex={-1}`** — הרצועה עצמה
  נגישה במקלדת, וקורא מסך שיקריא "כפתור, כפתור" בין כל
  לשונית מייצר רעש ולא עזרה.
* ⚠ **הגלילה 80% מהרוחב הנראה** ולא פיקסלים קבועים, כדי שתמיד
  תישאר לשונית אחת מוכרת על המסך.

### ⚠ דפדוף בין כל שבועות המכינה

`?action=view&week=<id>` פותח כל שבוע, ולא רק את הקרובים.
ברירת המחדל היא ההווה — זה מה שאב הבית עושה ביום שני בבוקר —
אבל תכנון קדימה ובדיקה של מה שהיה דורשים את כולם.

* ⚠ **מזהה שאינו קיים נופל חזרה להווה ואינו זורק.** קישור ישן
  או שבוע שנמחק צריכים להחזיר מסך עובד.
* ⚠ **`weekAt` מוחזר עם מה שנטען בפועל**, והמסך מסמן אותו ולא
  את מה שביקש — אחרת הוא מסמן שבוע שלא נטען.
* ⚠ **רשימת השבועות מוחזרת לכולם**, לא רק לאב הבית: היא כבר
  גלויה בלוח מובילי השבוע, והחניך צריך אותה כדי לראות אחורה.
  מיפוי מפורש — תאריכים ומספר בלבד, בלי המובילים ובלי ההערות.

### ⚠ שעת יציאה ושעת חזרה — חובה

זו השאלה התפעולית האמיתית בבקשת יציאה, ובלעדיה המדריך שואל
בוואטסאפ בדיוק את מה שהטופס אמור היה לתפוס.

* ⚠ **טקסט `"HH:MM"` ולא עמודת `hour` של monday.** היא מחזירה
  `{hour, minute}` ומקבלת פורמט משלה, ושתי המרות בכל כיוון הן
  שתי הזדמנויות לשגות.
* ⚠ **הבדיקה בשרת ולא רק ב-`<input type="time">`** — דפדפן ישן
  מגיש טקסט חופשי, ובקשה ישירה עוקפת את השדה לגמרי.
* ⚠ **ואין השוואה בין השעות.** יציאה ב-20:00 וחזרה ב-08:00 היא
  לינה בבית, לא טעות — והשוואה נאיבית הייתה חוסמת בדיוק את
  הבקשה הנפוצה ביותר.
* ⚠ **`dir="ltr"` על השדה** — בלעדיו 19:30 מוצג 30:19.

### ⚠ מחלה ומוצדקת מחייבות פירוט; יום חופש לא

יום חופש הוא זכות במכסה, ודרישת נימוק עליו הופכת אותו לבקשת
רשות. מחלה והיעדרות מוצדקת הן בדיוק המקרים שבהם המנהל
**מכריע**, ובקשה בלי פירוט אינה ניתנת להכרעה — הוא יחזור לחניך
וישאל, וזה יום נוסף שבו הבקשה תלויה.

⚠ `flow-test` נועל את **רשימת השדות המדויקת** שיוצאת לחניך.
`outAt` ו-`backAt` נוספו לרשימה במודע; שדה חדש שיתווסף למיפוי
ייפול שם ולא ידלוף בשקט.

### ⚠ מובילי שבוע ותורנות מטבח

מוביל שבוע פטור מתורנות בשבוע שהוא מוביל — **וזה כולל במפורש
את תורנות המטבח**, שהיא יום שלם ומפקיעה אותו מהלו״ז בדיוק
כשהוא אמור להוביל אותו. ⚠ והשבוע נגזר **מהתאריך** בתורנות
היומית ולא נשלח מהדפדפן, אחרת אפשר היה לעקוף בשליחת שבוע אחר.

### ⚠ "נהלים במכינה" — מסך שהקוד אינו מכיר את תוכנו

`src/Rules.jsx` מציג בלוק טקסט אחד (`mechina.rules`) שיושב
בלוח. הוא **לא מפצל לסעיפים, לא מזהה כותרות ולא סופר קווים
אדומים** — כל מבנה שהיינו כופים היה נשבר בעריכה הראשונה של
דני, והוא זה שכותב.

חשוף לכל החניכים, ונערך על ידי ראש המכינה בלבד. ⚠ `headText`
נפרד מ-`assign`: עריכת נוסח היא ראש המכינה, ועריכת מטלות היא
גם אב הבית וגם אחראי המטבח.

---

## 4ש · האפליקציה נערכת מתוך עצמה

המטרה: ראש המכינה משנה נוסח, תיאור ונתונים **בלי מפתח ובלי
דיפלוי** — בכל מסך, לא רק בתורנויות. שני מנגנונים כלליים,
ולא עורך נפרד לכל מסך.

### ⚠ מנגנון א׳: בלוק טקסט בכל מקום

`shared/content.js` הוא **הרשימה של איפה אפשר לכתוב**. כל
מפתח שם הוא מקום אחד במסך, והוא גם שם השורה בלוח `טקסטים
באפליקציה` — כך ששורה שנמחקה בטעות חוזרת ביצירתה מחדש עם אותו
שם, בלי לתקן דבר בקוד.

* `src/ScreenNote.jsx` — בלוק בתוך מסך. ⚠ **בלוק שלא נכתב
  אינו מוצג בכלל**: לא ריק, לא מסגרת, ולא "טרם הוזן". מסך
  שמלא בקופסאות ריקות מלמד להתעלם מהן.
* `src/Content.jsx` — **המפה**: מה קיים, מה עוד לא נכתב, ומה
  שונה מהנוסח המקורי. זה המקום היחיד שבו "טרם נכתב" מוצג,
  ודווקא שם הוא חשוב.
* ⚠ **העריכה קיימת בשני מקומות ובכוונה** — במסך שאליו הטקסט
  שייך (שם רואים אותו בהקשר) ובמפה. אותו `TextBlock`, אותה
  נקודת קצה.
* ⚠ **למה רשימה בקוד ולא "כל מה שבלוח":** המסך צריך לדעת
  **איפה** להציג כל בלוק, ומיקום אינו נתון שאפשר להקליד בלוח.
  `where` הוא החוזה בין השניים.

### ⚠ תיאורי התפקידים — שלוש שכבות, והספציפי גובר

1. `src/roles-info.js` — הנוסח שהמכינה מסרה, בקוד (4יא).
2. לוח הטקסטים, במפתח `role.<שם התפקיד>` — מה שנערך מהמסך.

**הלוח מחזיק דריסות בלבד ואינו נזרע.** אילו כל התפקידים היו
בו, התג "נערך" היה מופיע על כולם ומאבד את משמעותו, ואי אפשר
היה לדעת מה באמת שונה — בדיוק הנימוק של טבלת ההמרות (4לו).

* ⚠ **המפתח נגזר משם התפקיד**, שהוא בדיוק המחרוזת שבעמודת
  התפקידים. תפקיד שיתווסף בלוח מקבל מפתח מעצמו.
* ⚠ **המקור מסומן בתג "נערך"** — תיאור שנערך ותיאור שהמכינה
  מסרה הם שני דברים, ומי שקורא צריך לדעת מה מולו.
* ⚠ **"החזרת הנוסח המקורי" ולא "מחיקה"** — התיאור בקוד עדיין
  קיים, והשורה בלוח היא דריסה שלו. המילה צריכה לומר מה קורה.
* ⚠ **כישלון בטעינת הטקסטים אינו מפיל את המסך** — הוא נופל
  לנוסח המקורי, וזה בדיוק מה שהיה קודם.

### ⚠ מנגנון ב׳: נתוני חניך שהצוות מנהל

`?action=edit` — שם, ת.ז, תאריך לידה, מגדר, טלפון, אימייל
אישי, עיר, אלרגיה, הגדרה דתית ומידת חולצה.

⚠⚠ **ומה שאינו שם, במכוון:** `army` ו-`tryouts` ממולאים על ידי
החניך על עצמו, ועריכה שלהם מהמסך של הצוות הופכת את השדה למשהו
אחר לגמרי. `user`, `hash` ו-`email` הם חשבון הכניסה שלו —
וסיסמה לעולם אינה נערכת מבחוץ (4כח).

* ⚠ **ראש המכינה בלבד**, ולא כל איש צוות: ת.ז היא סוד הכניסה.
* ⚠⚠ **שינוי ת.ז מנתק את החניך מיד מכל מכשיר** — `requireAuth`
  משווה את טביעת האצבע שבעוגייה מול הת.ז בכל בקשה. זה נכון
  ורצוי, אבל **נאמר לפני ולא אחרי**: המסך מזהיר, והתשובה
  מחזירה `signedOut`.
* ⚠ **מיפוי מפורש ולא פריסה** — שדה שאינו ברשימה אינו נכתב,
  כדי שעמודה חדשה בלוח לא תיפתח לכתיבה מעצמה.
* ⚠ **`kind` לכל שדה אינו קישוט**: עמודת `status` דורשת
  `{label}`, `phone` דורשת `{phone, countryShortName}`,
  ו-`email` דורשת `{email, text}`. **מחרוזת לבדה נדחית בשקט
  על ידי monday ואינה כותבת דבר.**
* ⚠ **תווית שאינה בלוח מתורגמת להודעה שאפשר לפעול לפיה** —
  "העריכה נכשלה" לבדה אינה עוזרת לאיש.
* ⚠ **התשובה מחזירה מה השתנה בפועל** ולא "נשמר". שדה שנשלח
  זהה לקיים אינו נספר, והמסך אומר אמת.
* ⚠ **שם כפול ות.ז כפולה נחסמים** — שם, כי כל המסכים מזהים
  בעין; ת.ז, כי היא שני אנשים שנכנסים לאותו חשבון. ⚠ והודעת
  הכפילות **אינה אומרת של מי** — שם בהודעת שגיאה הוא מידע על
  אדם אחר.

---

## 4ת · מצב המערכת, והעבודה בלי דפדפן

### הבדיקות עברו למאגר

`tools/tests/` — **20 חבילות, 423 טענות.** עד לסשן הזה הן ישבו
ב-`scratchpad/`, שהוא **ב-`.gitignore`** — כלומר מי שמשך את
המאגר במחשב אחר קיבל אפס בדיקות, ולא ידע שהן קיימות.

```bash
npm test              # הכול
npm test -- chores    # אחת
```

`tools/tests/run.mjs` מריץ אותן **אחת אחרי השנייה ולא במקביל**:
כל אחת כותבת ללוחות האמיתיים ומנקה אחריה, ושתיים במקביל היו
רואות זו את הנתונים של זו ונכשלות מסיבה שאינה קשורה. ⚠ והוא
בודק שהשרת בכלל עונה **לפני** שהוא מריץ עשרים חבילות שכולן
ייכשלו באותה שגיאה חסרת פשר.

### ⚠ אין דפדפן? זה לא חוסם כלום

בסביבה אחת כלי הדפדפן לא נפתחו ואי אפשר היה לראות `localhost`.
**כל האימות כאן ממילא ללא דפדפן:**

1. **חבילות הבדיקה** — `fetch` לשרת אמיתי, תשובות אמיתיות.
2. **`curl` עם עוגייה** — `-c` לשמור, `-b` לשלוח.
3. **`npx vite build`** — תחביר בלבד.
4. **`<meta name="build">` בייצור** — מאמת שהדיפלוי עבר.

⚠ **`vite build` אינו תופס `ReferenceError`.** משתנה שנקרא בלי
ייבוא עובר את הבנייה ונופל בזמן ריצה — זה קרה כאן פעמיים, ורק
הרצה בפועל תפסה את זה.

⚠ **דיפלוי לוקח 40–90 שניות. להמתין על תנאי ולא על זמן:**

```bash
until curl -s https://nirozmanager.vercel.app/ | grep -q "$(git rev-parse --short HEAD)"; do sleep 8; done
```

### ⚠ הסביבה עצמה — שתי מלכודות שבזבזו כאן זמן

**`bash` כאן מעבד את תוכן הפקודה גם בתוך heredoc מצוטט.**
בקטיקים מורצים כתחליף פקודה (הערה בעברית עם `` `auth.teams` ``
נמחקה כך באמצע קובץ), ומרכאות בודדות לא-מאוזנות שוברות את
הפקודה כולה. **לכתיבת קובץ עם עברית או בקטיקים — כלי Write,
או Python.** קרה שלוש פעמים.

**`python` כאן הוא cp1252 כברירת מחדל.** `PYTHONUTF8=1` לפני
כל סקריפט שנוגע בעברית, אחרת `UnicodeEncodeError` באמצע כתיבה.

### מצב הפונקציות

**7 מתוך 12 תפוסות:** `kitchen · auth · students · attendance ·
lessons · container · chores`. נותרו 5.

### חשבונות

| מי | כניסה | מה |
|---|---|---|
| חניך בדיקה | `bdika` / `mechina2026` | חמישה תפקידים, ניתן לשיבוץ, אינו נספר |
| אלון רצון | קוד `12341234` | **צפייה בלבד** |

⚠ **הסיסמה של חשבון הבדיקה יושבת בקוד הבדיקות** ולא בלוח (שם
יש גיבוב scrypt). זה חשבון שאינו של אדם ואינו נספר בשום מונה,
ולכן זה מקובל **כאן ורק כאן** — סיסמה ידועה על שורה של אדם
היא פרצה, גם אם זמנית (4לד).

### ⚠ מה שנשאר פתוח ומי חוסם

* **Google Sheets** — חסום ב-`GOOGLE_API_KEY` ובהמרת שלושה
  קבצי אקסל לגיליונות אמיתיים. הפרסרים בנויים ובדוקים.
* **העלאת קבצים למשימות** — דורש סוד נוסף, והוחלט לשאול לפני.
* **חשבון הבדיקה נושא חמישה תפקידים** — לאפס כשאחים מסיים.
* **"מובילי שבוע לא עושים תורנות מטבח בכלל"** — מומש כ"לא
  בשבוע שהם מובילים". פטור לכל השנה היה סותר את ההוגנות שכל
  טבלת המעקב קיימת בשבילה. נשאל, ולא נענה.

---

## 5א · הפיילוט — מה שהשתנה כשאנשים אמיתיים נכנסו

מספטמבר 2026 המערכת בשימוש של חניכים וצוות. שלושה דברים
שנכונים מהיום הזה והלאה, ולא היו נכונים לפניו.

### ⚠⚠ הבדיקות רצות מול הנתונים האמיתיים של המכינה

אין סביבת בדיקה נפרדת. עד לפיילוט זה היה מטריד; מרגע שיש
נתונים אמיתיים זה מסוכן, וזה כבר עלה בנתון:

**`quota-test` מחק שלושה ימי נוכחות של 33 חניכים.** הוא בוחר
את ימי השגרה **הראשונים** של כל מחצית — שהם בדיוק הימים
הראשונים של שנת המכינה, כלומר בדיוק הימים שהצוות סימן ראשונים.
משם שתי פגיעות נפרדות:

1. הניקוי מחק את שורת יום הסימון **לפי תאריך**, ולא לפי מה
   שהבדיקה יצרה.
2. ואפילו בלי מחיקה — `?action=mark` **דורס** את רשימת
   הנוכחים, ולכן היום נשאר בלוח עם אפס נוכחים.

**הכלל שנוסף: בדיקה בוחרת נתונים שאיש עוד לא נגע בהם.**
`quota-test` מצלם את ימי הסימון הקיימים **לפני** שהוא בוחר
ימים, מדלג עליהם, והניקוי שלו מדלג על כל מה שהיה קיים לפניו.

⚠ **וזה חל על כל בדיקה חדשה.** "מוחקת רק מה שיצרה" אינו מספיק
כשהיא **כותבת** על שורה קיימת.

### ⚠⚠ שמונה חבילות דיווחו הצלחה שלא הרוויחו

`auth · batch · cycle · flow · import · parity · perm · rating`
הדפיסו את שורת הסיכום בסדר מילים ש-`run.mjs` אינו מנתח,
**ולא יצאו בקוד שגיאה**. שתי התקלות יחד:

* הטענות שלהן לא נספרו בסך הכול **בכלל** — "423 טענות" היה
  מספר של שתים־עשרה חבילות מתוך עשרים.
* וטענה שנכשלה בהן הוצגה כ-**V ירוק**, כי המריץ קובע לפי קוד
  היציאה בלבד.

ומיד כשהן התחילו לדווח נפלו שתי טענות שהיו כבויות מזמן, ובשתיהן
**הקוד צדק והבדיקה טעתה**: מדריך שעורך גיליון מרצים (נחסם
ב-4ע), ות.ז של חניך רשום שמחזירה 409 ולא 401 (4ע).

**הכלל: חבילה חייבת `process.exit(fail ? 1 : 0)` ושורת סיכום
בפורמט `${pass} עברו, ${fail} נכשלו`.** המריץ מקבל היום את שני
סדרי המילים **ומתריע במפורש** על חבילה בלי שורת סיכום — "—"
בעמודה נראה בדיוק כמו חבילה שהכול בה עבר.

### ⚠ חשבון הבדיקה יכול להימחק, ואז אין בדיקות

הוא נמחק מהלוח, וכל 20 החבילות נפלו ב"אין חשבון בדיקה" — כלומר
לא הייתה שום דרך לאמת שינוי בשרת. `tools/seed-demo.mjs` יוצר
אותו מחדש, ו-`tools/demo-flag.mjs` מדליק את הדגל אחרי הרצה
שנפלה באמצע (חבילות מכבות אותו זמנית ומחזירות ב-`finally`;
הרצה שנקטעה משאירה אותו כבוי, **וחשבון הבדיקה נספר בנוכחות
של כל המכינה**).

---

## 5ב · מוביל שבוע — טווח, ולא "היום"

### ⚠ שתי שאלות שנראות אחת

```
session.isLeader      מוביל היום        → פותח את המסך בניווט
session.leadsAnyWeek  משובץ לשבוע כלשהו → פותח את השער
```

`marker` בדק `isLeader`, ולכן מוביל של **השבוע הבא** לא יכול
היה אפילו לפתוח את הימים שלו — וההודעה שקיבל אמרה "הפעולה
מותרת למוביל שבוע", כלומר סתרה את עצמה.

היום: השער שואל "האם אתה בכלל מוביל שבוע השנה", ו**איזה תאריך**
מותר נבדק בתוך נקודת הקצה מול `weeksOfStudent`.

### ⚠ ההרשאה עוברת מעצמה, כי היא נגזרת מהתאריך

הכלל הוא **התאריך מול הטווח** ולא דגל שמישהו מדליק. לכן מעבר
חצות מזיז את התאריך מעל הקו וההרשאה עוברת — בלי שאיש יעשה
דבר ובלי דיפלוי. `leader-test` בודק בדיוק את הגבול: היום
האחרון בשבוע מותר, היום שאחריו לא.

⚠ **גרסה ראשונה של הבדיקה בדקה זאת בהסרת השיבוץ, וזו הייתה
בדיקה של המטמון ולא של הכלל** — מטמון השבועות חי חמש דקות
ויושב בתהליך אחר.

### ⚠⚠ הסימון הידני בלוח החניכים אינו פג לעולם

`MECHINA_COLS.roster.leader` הוא עוקף חירום **בלי טווח**.
מי שמסומן בו הוא מוביל שבוע **לנצח**, וההרשאה שלו אינה עוברת
לאיש. הוא נשאר "היום בלבד" ואינו נכנס ל-`weeksOfStudent`,
אחרת חניך שסומן פעם אחת היה מקבל הרשאה על כל ימי השנה.

⚠ **בפיילוט נמצאו שני חניכים שנושאים אותו.** מי שרואה סימון
כזה — לבדוק אם הוא עדיין נחוץ.

### ⚠ תזכורות — בפעמון, ולא בטלפון

`leaderMarkNotes` בונה מהמצב: כל יום לימוד שעבר בשבוע של
המוביל וטרם סומן. היום עצמו נספר **מ-18:00 בשעון ישראל** —
תזכורת שמלווה אותו כל היום היא רעש, כי הוא באמת עוד לא סימן
ובצדק.

לאחראי הלו״ז שתי תזכורות נפרדות ובעדיפות גבוהה — **שיעורי
היום** (מהערב) ו**שיעורי אתמול** (למחרת, כשעוד זוכרים מה
היה) — והרשימה הארוכה של שבועיים ירדה ל"רגיל". "12 שיעורים
טרם דווחו" הוא מספר שמלמדים להתעלם ממנו תוך שבוע.

⚠ **`israelHour()` ולא שעת השרת.** Vercel רצה ב-UTC, ו"ערב"
בשעון השרת מגיע בישראל בשלוש לפנות בוקר.

⚠ ואלה התראות **בתוך האפליקציה**. דחיפה לטלפון או במייל
דורשת שירות חיצוני ולא נבנתה (4כו).

---

## 5ג · מה שנוסף בפיילוט, בקצרה

| מה | היכן | הכלל שאסור לשבור |
|---|---|---|
| תאריך השיעור בחוות דעת | `LESSON_COLS.evals.lessonDate` | נפרד מתאריך הכתיבה; נגזר מהמפגש כשיש |
| מחיקת חוות דעת | `_lesson-evals.js` `remove` | **מחזור ב׳ בלבד** — מחזור א׳ יובא ממקור שאינו קיים |
| פרטי קשר עם מרצה | `LESSON_COLS.sheets.phone/mail/contact` | על הגיליון, **ואינם יוצאים לחניך** |
| קנייה על חודשים שנבחרו | `orderMonths(order)` | מחלקים במספר שנבחר, לא ב-3 קבוע |
| נקודה עשרונית | `inputMode` | כמות → `numeric` · כסף → `decimal` |
| יום ג׳ → יום ו׳ | `fridayAfterTuesday` | **רק כשיום שישי ריק** |
| תמונה אחרי תיקון | `FAULTS_COLS.photoDone` | עמודה נפרדת; `assets` מחזיר את כל הקבצים |
| תורן מטבח באימון | `kitchenDutyOn` | מסמן מראש **רק כשלא דווח כלום** |
| מיונים לצבא | `_tryouts.js` | **הבעלות אצל החניך**; היו״ר קורא |
| ועדת גיוסים | `PLACEMENT_COLS.definitions.army` | תיבה בלוח, לא שם בקוד |

⚠ **`shared/placements-ids.js` הוא קובץ מחולל, ושני מחוללים
כותבים אותו.** העמודה `army` נוספה לשניהם — בלי זה הרצה חוזרת
הייתה מוחקת אותה, בדיוק המוקש של 4מו.

---

## 5ד · מה שנבנה בהמשך הפיילוט

### ⚠⚠ מפת ההרשאות נגזרת, ולא נכתבת

`shared/access-map.js` + `src/Access.jsx`. **זו כל התכלית:**
טבלת הרשאות שנכתבת ביד מתיישנת ביום שמישהו משנה דגל אחד, ואז
היא לא סתם חסרה — היא **משקרת על אבטחה**. מסך שאומר "מדריך
אינו עורך תקציב" בזמן שהוא כן, גרוע ממסך שלא קיים.

| מה מוצג | מאיפה נגזר |
|---|---|
| אילו מסכים תפקיד פותח | `DUTIES[x].tabs` |
| מה מותר לו לערוך | `EDIT_AREA` + `mayEdit` |
| מאיפה התפקיד מגיע | `ROLE_FLAG`, ושני החריגים |

`access-test` משווה כל שורה במפה מול **`mayEdit` עצמה** — לא
מול רשימה שנייה. שינוי בכלל שלא יגיע למפה נופל שם.

⚠ **ומה שאי אפשר לגזור נאמר במפורש.** דגלי `withAuth` פזורים
בעשרות מודולים ואין להם רשימה אחת; מה שמוצג עליהם מסומן
כתמצית ולא כחוזה. הצהרה שגויה גרועה מהיעדר הצהרה.

⚠ **ותיאור המכינה מוצג בנפרד מהמסכים.** `desc` הוא מה שהאדם
עושה, `perms` והמסכים הם מה שהמערכת פותחת (4יא).

### מיונים לצבא — הבעלות אצל החניך

`api/_tryouts.js`, לוח משלו, שורה לכל מיון. **הכתיבה של החניך
בלבד** — גם ראש המכינה וגם היו״ר חסומים — כי `army`/`tryouts`
תמיד מולאו על ידו על עצמו, ועריכה מבחוץ הופכת את הנתון
מ"מה שהחניך מספר" ל"מה שהצוות רשם עליו".

⚠ **מי יו״ר ועדת הגיוסים נקבע מתיבה בלוח** (`definitions.army`)
ולא משם בקוד: בלוח יש "ועדת ידיעת הארץ והכנה לצבא" וגם
"ועדת גיוסים", ומחר המכינה תאחד או תפצל אותן.

⚠ **"טרם ניגש" אינו "לא עבר"** — מצב שלישי, ומוצג במפורש.

### דף המובילויות — רק מה שכבר עבר

`api/_leadership.js`. הגבול הוא `end < today` — **נגמר**, ולא
"התחיל". דף שמראה את השבוע שעומד להגיע הופך מסיכום למטלה.

⚠ **שני שדות ושני קולות:** `feedback` נכתב על ידי הצוות,
`summary` על ידי מי שהוביל. ראש המכינה אינו חריג בסיכום.

⚠ **המשוב הוא של השבוע ולא של החניך** — שבוע מובילים שניים.

⚠ **קריאה טרייה ולא מהמטמון בן חמש הדקות:** משוב שנכתב זה עתה
ואינו מופיע נראה כמו תקלה, ונעלם מעצמו — כלומר אי אפשר לשחזר.

### דוח תשלום למרצים

מחיר **למפגש** על הגיליון, כפול מספר המפגשים שסומנו "התקיים".

⚠⚠ **המספר החשוב אינו הסכום אלא מה שחסר.** מפגש שטרם דווח אינו
כסף שמגיע ואינו כסף שלא מגיע — הוא לא ידוע (4ח); וגיליון בלי
מחיר אינו חינם. שניהם מוצגים מתחת לסכום, במילים ולא רק בצבע.

⚠ **ריק אינו אפס:** 0 הוא מתנדב, ריק הוא "לא סוכם".

### מרצים וסיכום בכל צוות

⚠⚠ **אותו לוח חוות דעת, ולא לוח שני.** מרצה שהגיע לסדרה הוא
אותו מרצה שיכול להגיע לשיעור רגיל; שני לוחות היו מייצרים שתי
היסטוריות על אותו אדם. מה שמבדיל הוא `evals.placement`.

⚠ **כל חברי הצוות כותבים**, ועורכים ומוחקים רק את שלהם (או
היו״ר). חוות דעת היא טקסט שאדם כתב.

### צ׳ק ליסט — ימים מרובים ומתי ביום

`days` (רשימה) גובר על `day` (יחיד), ו-`when` הוא מתי ביום.
**רשימה ריקה = כל יום, ולא אף יום.**

⚠ והמסך מקובץ לפי **סדר היום** ולא לפי אזור: תורן שרואה 33
מטלות ברשימה אחת אינו יודע מה לעשות עכשיו.

⚠ באג שנתפס: `loadChecklist` סינן `r.task && r.day`, ולכן מטלה
שנוצרה עם `days` בלבד **נעלמה בשקט**.

---

## ⚠⚠ `vite build` ו-`ReferenceError` — שלוש פעמים בסשן אחד

הבנייה עוברת, והדפדפן נשבר. שלושת המקרים בסשן הזה:

1. `<MI.flag />` על מפתח שאינו קיים ב-`MI` → *Element type is
   invalid*.
2. `reload={reload}` בעוד הטוען נקרא `load` → *ReferenceError*.
3. (מסשן קודם) `byCategory` ו-`CATEGORIES` בלי ייבוא.

**הבדיקה שנוספה:** סריקה של כל הפניות האייקונים בכל מסך —
`<XX.name />` מול המפתחות של `const XX = {…}`. היא תפסה את
הראשון לפני שהגיע לדפדפן.

**הכלל: אחרי כל הוספת אייקון או prop — לסרוק, לא להסתמך על
הבנייה.**

---

## 5ה · התראות דחיפה, ו-8 מתוך 12

### ⚠ הדחיפה ריקה — וזו החלטה, לא קיצור דרך

`api/_push.js` שולח **נקישה בלי מטען**. ה-Service Worker מקבל
אותה, פונה בעצמו ל-`?action=notify` עם העוגייה של המשתמש,
ומציג את מה שמצא.

* **שום נתון של חניך אינו עובר דרך גוגל או אפל.** בדחיפה עם
  תוכן גוף ההודעה עובר בשרתיהם — מוצפן, אבל עובר.
* **ואין הצפנת מטען**: ECDH + HKDF + aes128gcm הם כמאה וחמישים
  שורות קריפטוגרפיה שכל טעות בהן נכשלת בשקט.

⚠ **ואין כאן ספק חיצוני** — זה ההבדל מהמייל (4כט). זוג מפתחות
שנוצר מקומית עם `node:crypto` הוא כל מה שצריך: אין חשבון, אין
תשלום, אין חבילה, ואין דומיין לאמת.

⚠ **חתימת ES256 של node היא DER, ו-JWT דורש r|s גולמי.**
`dsaEncoding: "ieee-p1363"` — בלעדיו חלק משירותי הדחיפה
מקבלים בשקט ואחרים דוחים, כלומר תקלה שמופיעה רק אצל חלק
מהמשתמשים.

⚠ **`buildNotes` חולצה ומיוצאת מ-`_notify.js`.** הפעמון וסבב
הדחיפה בונים מאותה פונקציה; שני מקומות שבונים התראות בנפרד
מתפצלים ביום הראשון, ואז הדחיפה מודיעה על משהו שהמסך אינו
מראה.

⚠ **"נדחף לאחרונה" נשמר.** בלעדיו כל סבב היה דוחף שוב על אותה
תקלה פתוחה, והמשתמש היה מכבה את ההתראות תוך יום.

⚠ **iOS דורש PWA מותקן** (16.4+). המניפסט כבר היה, ולכן זה
עובד — והמסך אומר זאת **מראש**, כי מי שיאשר בסאפרי ולא יקבל
דבר יסיק שהמערכת שבורה. ארבעה מצבי כשל, ארבע הודעות שונות.

### ⚠⚠ Vercel Cron אינו מקבל query string

`"/api/auth?action=push-run"` **פסול**. כל המערכת מנותבת
ב-`?action=`, ולכן משימה מתוזמנת חייבת נתיב אמיתי — וזו
`api/cron.js`, **הפונקציה הנספרת השמינית מתוך 12**. נותרו 4.

⚠ ההגנה היא `CRON_SECRET` ולא סשן: אין כאן משתמש. הבדיקה
נועלת שגם **מנהל מחובר** מקבל 401.

⚠ ומשימה בשם שגוי מחזירה 404 ואינה רצה כברירת מחדל בשקט.

### ⚠⚠ 403 מ-Vercel אינו בהכרח דיפלוי שבור

בסשן הזה `curl` על הייצור החזיר 403 במשך דקות, ואני הסקתי
שהדיפלוי נשבר והסרתי את `vercel.json`. **זה לא היה זה.**
הכותרת `X-Vercel-Mitigated: challenge` אומרת שזו מערכת
ההגנה מפני בוטים של Vercel, שנכנסה לפעולה אחרי עשרות
בקשות אוטומטיות באותה שעה. דפדפן אמיתי עובר אותה.

**הכלל: לפני שמסיקים שהייצור נפל — לקרוא את הכותרות, ולבדוק
בדפדפן.** `curl` שנחסם נראה בדיוק כמו אתר מת.

---

## 5ו · מיונים ושיבוצים, והוצאת שיעור מדוח התשלום

### ⚠⚠ "שאיפות ומיונים" יצא מהפרופיל — ומה שנכתב בו לא נמחק

שני שדות טקסט חופשי (`roster.army`, `roster.tryouts`) שאי אפשר היה
לשאול עליהם שום שאלה: לא כמה ניגשו, לא לאיזה חיל, לא מה הסטטוס.
הם הוחלפו בשניים שאפשר — **מיון בשורה משלו** (לוח המיונים), ו**שיבוץ
אחד** (חיל + פירוט תפקיד, על שורת החניך במצבה).

* ⚠ **העמודות נשארות בלוח ונקראות, ואינן נכתבות.** מה שהחניכים כבר
  הקלידו מוצג להם ולצוות, מסומן "רישום ישן", ואינו בשימוש. שדה
  שנעלם מהמסך בלי מילה נראה בדיוק כמו נתון שהמערכת מחקה.
* ⚠ **המסלול נסגר בשרת ולא בהסרת הטופס.** `?action=profile` מחזיר
  **410** על `army`/`tryouts` ואומר לאן עברו. מסלול שנשאר פתוח אחרי
  שאיש אינו קורא ממנו כותב נתון שלא יופיע בשום מקום — ולקוח ישן
  יראה "נשמר" על משהו שאבד.
* ⚠ `setProfile` ב-`src/api.js` שולח **`talks` בלבד**.

### ⚠ חמש אפשרויות המצב — בגוף ראשון, ובנוסח המכינה

```
טרם ניגשתי · ניגשתי ועברתי לשלב הבא · ניגשתי והתקבלתי
ניגשתי ולא התקבלתי · לא ניגשתי
```

הרשימה הישנה ("מתוכנן · עבר · לא עבר…") תיארה את המיון מבחוץ; זו
מתארת מה **החניך** עשה — והוא זה שממלא. ההבדל אינו סגנוני: מיון
רב-שלבי נדחס ב"עבר" מוקדם מדי.

* ⚠ **הנוסח חי בארבעה מקומות** — `shared/tryouts-ids.js`,
  `tools/seed-tryouts.mjs`, `tools/seed-army.mjs`, והלוח עצמו.
  `tryout-test` נועל את **הנוסח** ולא רק את המספר.
* ⚠ **התוויות הישנות נשארות במפת הגוונים שב-`Tryouts.jsx`** — מיון
  שנרשם לפני השינוי עדיין נושא אותן.

### ⚠⚠ כתיבה מחדש של רשימת תוויות — מה שומר על הנתונים

`tools/seed-army.mjs` כותב את הרשימה מחדש על **אותה עמודה**, ולא
בעמודה שנייה: עמודה שנייה הייתה משאירה את הישנה בלוח ואת
`TRYOUT_COLS.status` מצביע רק על אחת מהן.

`update_status_column` **דורס את כל הרשימה**, ותווית שנעלמת ממנה
בזמן שמישהו יושב עליה **מוחקת לו את הערך בשקט**. לכן הסדר:

1. לקרוא מי יושב על מה — **לפני** שנוגעים.
2. תווית ישנה בלי שורות → יורדת. תווית ישנה עם שורות → **נשארת,
   מושבתת**, ומדווחת בשמה.
3. תווית חדשה שכבר קיימת → נשמרת עם ה-`id` שלה (ולא נוצרת שוב).
4. ⚠ **ווידוא בקריאה חוזרת** שכל תווית שהייתה בשימוש שרדה. בלעדיו
   מחיקה של רשימה שלמה נראית בדיוק כמו הצלחה.

⚠ ו-`id` מול `index`, ושמות הצבעים — ראו 4כד. שלוש התוויות
הראשונות נשארות על צבעי ברירת המחדל.

### ⚠ השיבוץ הוא חיל, ומאותה רשימה של הבוגרים

`roster.armyCorps` (status) + `roster.armyRole` (text).

* ⚠⚠ **רשימת החילות נזרעת מלוח הבוגרים ואינה מוקלדת בקוד.** "לאיזה
  חיל" היא אותה שאלה בשני המקומות, ושתי רשימות מקבילות מתפצלות ביום
  הראשון — ואז הסטטיסטיקה של הבוגרים מפסיקה להתיישב עם השיבוצים.
  ⚠ ואם הרשימה חוזרת ריקה, הסקריפט **אינו יוצר עמודה עם רשימה
  מומצאת**. עדיף בלי העמודה.
* ⚠ **האימות בזמן ריצה נקרא מהעמודה שאליה כותבים** ולא מלוח הבוגרים:
  מה ש-monday תקבל נקבע על ידי התוויות של אותה עמודה, ואימות מול
  מקור אחר מאשר ערך שהכתיבה תדחה.
* ⚠ **חיל מרשימה, תפקיד חופשי.** רשימת תפקידים סגורה מתיישנת בכל
  מחזור ודוחפת את כולם ל"אחר"; טקסט חופשי לבדו אינו נספר.
* ⚠ **שיבוץ אחד לחניך.** מיון יכול להיות רב — שיבוץ הוא התוצאה.
* ⚠ **אותה בעלות כמו במיונים**: החניך על עצמו, וגם ראש המכינה חסום.
  `PUT` **בלי `id`** הוא מה שמבדיל שיבוץ ממיון בשרת.
* ⚠ **`armyCorps`/`armyRole` מחוץ ל-`profile` ואינם ב-`toPublic`** —
  הם יוצאים רק דרך נקודת הקצה של המיונים.
* ⚠ ורשימת המזהים ב-`studentRows` עוברת `.filter(Boolean)`: **מזהה
  ריק ברשימה גורם ל-monday להחזיר את כל העמודות** — כלומר בדיוק את
  פרטי ההורים והמידע הרפואי שהמיפוי המפורש נועד למנוע (4פ).

### ⚠⚠ הוצאת שיעור מדוח התשלום — תיבה, ומוצגת

`LESSON_COLS.sheets.noPay`. יש שיעורים שמפגשיהם אמיתיים ואינם עולים
כסף, והם ניפחו את "שיעורים בלי מחיר" עד שהמספר הזה הפסיק לומר משהו —
והוא **כל התכלית של הדוח**.

* ⚠ **מחוץ לדוח בלבד.** הגיליון נשאר פעיל, מפגשיו נספרים בלוח
  השיעורים, ואחראי הלו״ז ממשיך לדווח עליהם. כיבוי `active` היה
  מוריד אותו מכל המערכת.
* ⚠ **"לתמיד" פירושו שלא יחזור מעצמו, ולא שאי אפשר להתחרט.** מחיקת
  גיליון אמיתית הייתה מוחקת גם את מפגשיו, חוות הדעת והדירוגים.
* ⚠⚠ **מה שהוצא מוצג בתחתית הדוח, עם כמה מפגשים התקיימו בו וכפתור
  החזרה.** הוצאה שקטה היא בדיוק סוג הדבר שאיש לא יזכור בעוד חודשיים,
  ואז מרצה שכן צריך תשלום פשוט אינו בדוח ואין לכך שום סימן.
* ⚠ **הרשאה: `!session.isStudent` — הצוות, ולא אחראי הלו״ז.** זו
  אינה חזרה על `canEdit`: `mayEdit(session,"scheduler")` פותח את
  **המחיר** גם לאחראי הלו״ז, שהוא חניך, וזה נכון — הוא זה שמסכם עם
  המרצים. הוצאה מהדוח היא החלטה תקציבית ונשארת אצל הצוות.
  `canExclude` מגיע **מהשרת** ואינו נגזר במסך.
* ⚠ **ואישור שאומר מה בדיוק ייצא מהסכום** — כמה מפגשים וכמה ₪.
  "בטוח?" על פעולה שמשנה מספר כסף אינה שאלה שאפשר לענות עליה.

### ⚠ הייצוא נושא את ההסתייגות, ולא רק המסך

קובץ שיוצא מדוח התשלום ייפתח בגיליון וייערך הרחק מהמסך שאמר "הסכום
אינו סופי". לכן **כותרת הקובץ** אומרת כמה מפגשים טרם דווחו וכמה
שיעורים בלי מחיר, ו"טרם דווחו" הוא **עמודה** ולא הערה.

⚠ סכום שאי אפשר לחשב יוצא כ-"—" ולא כ-0. אפס הוא מתנדב (4לג).

### ⚠ שתי עמודות שריקות עד שהסקריפט ירוץ

`roster.armyCorps` · `roster.armyRole` · `sheets.noPay` נכתבו כ-`""`,
ושתי פונקציות שומרות עליהן: `armyPlacementReady()` ו-`payFilterReady()`.

⚠ **הן אינן מפילות דבר.** בלי `noPay` הדוח מציג את כל הגיליונות
ואינו מציע להוציא; בלי עמודות השיבוץ המסך **אומר מה להריץ** במקום
להציג בורר ריק שנראה כמו מכינה בלי חילות (עיקרון 6). והכתיבה נכשלת
ב-**503 מפורש** עם `setupRequired`.

**אחרי `npm run seed:army` — להעתיק את שלושת המזהים שהוא מדפיס
לתוך `shared/mechina-boards.js` ו-`shared/lessons-boards.js`.**

---

## ⚠⚠⚠ 5ז · אינדקס 5 — המשבצת הריקה של monday

**זו המלכודת החמורה ביותר שנתפסה עד כה, והיא שקטה לחלוטין.**

ל-monday יש בעמודת סטטוס משבצת "ריק" קבועה — **אינדקס 5**. תא בלי
בחירה מחזיר `value: null`, אבל `text` מחזיר את **שם התווית שיושבת
על 5**, אם יש שם אחת. וכל הקוד במאגר קורא `text`.

מה שקרה בפועל, תוך דקות מיצירת העמודה:

* עמודת "שיבוץ צה״ל — חיל" נוצרה עם 15 תוויות במפתחות 1..15, ו-
  **כל 35 החניכים הופיעו מיד כמשובצים ל"חיל האוויר"** — התווית
  שנפלה על 5. שישה מהם חניכים אמיתיים.
* אותה מלכודת ישבה כבר בלוח המיונים: "לא הגיע" על מפתח 5, כלומר
  **כל מיון בלי מצב היה נקרא "לא הגיע"** — טענה על חניך שהוא
  מעולם לא עשה.

**ולא רק בקריאה.** ניקוי תא סטטוס במחרוזת ריקה כותב `{"index":5}`
**במפורש** — כלומר "למחוק את הבחירה" **קובע** אותה. הניקוי הנכון
הוא `null`. הבדיקה שתפסה את זה היא "והשיבוץ באמת ריק" ב-
`tryout-test`, אחרי שהטענה הראשונה עברה על נתון שגוי.

### שלוש ההגנות, ושלושתן נחוצות

1. **ביצירה — מדלגים על 5.** `tools/seed-army.mjs` ו-
   `tools/seed-tryouts.mjs` בונים את המפתחות `1,2,3,4,6,7…`.
   זה מגן על לוחות **חדשים** בלבד.
2. **בקריאה — `value === null` הוא המבחן לריק, ולא `text === ""`.**
   `api/_student-rows.js` (`status()`) ו-`api/_tryouts.js`
   (`statusOf()`). זה מגן גם על עמודות שכבר קיימות ועל כאלה
   שהמכינה בנתה ביד. **כל קריאה חדשה של עמודת סטטוס חייבת את זה.**
3. **בכתיבה — `null` ולא `""`.**

### ⚠ ואי אפשר לפנות את המשבצת

נוסה: מחיקת התווית שעל 5 ויצירתה מחדש מחזירה אותה **בדיוק ל-5** —
monday נותנת תמיד את המפתח הפנוי הנמוך ביותר. ושליחת `id` חדש
נדחית ב-`For new labels no id should be provided`.

לכן `tools/name-slot5.mjs`: במקום לפנות, **קוראים לה בשמה**.
התווית שעל 5 מקבלת נוסח שאומר "אין כאן בחירה" ומושבתת (כדי שלא
תופיע בבורר), והתווית שישבה שם קודם נוצרת מחדש על מפתח פנוי —
**במצב שבו הייתה**, פעילה או מושבתת.

זה מה שנעשה בפועל: החילות קיבלו "טרם שובץ" על 5, והמיונים
"טרם דווח". מי שפותח את הלוח רואה עכשיו טענה נכונה.

### ⚠ תווית מושבתת חייבת לרדת מכל בורר

`update_status_column` דורס את כל הרשימה, ותווית שנעלמת ממנה
**מוחקת בשקט את הערך של כל שורה שיושבת עליה**. לכן הדרך היחידה
להוריד תווית בלי לאבד נתונים היא **להשבית**, וזה נפוץ.

וקורא שמתעלם מ-`deactivated_labels` מציע בדיוק את מה שהורידו:
זה תוקן ב-`corpsLabels` (`api/_tryouts.js`) וב-`branchLabels`
(`api/_alumni.js`), ששניהם קראו את `labels` בלבד.

⚠ **ההשבתה יושבת ב-`deactivated_labels`** (מערך מזהים) ולא בתוך
`labels_colors`. שורת ווידוא שקוראת מהמקום הלא-נכון מדפיסה תמיד
"לא קרה כלום" — וזה בדיוק הדיווח שמסתיר כישלון אמיתי בפעם הבאה.

### ⚠ ושמות הצבעים

`settings_str` מחזיר את שם הצבע ב-**`var_name`** ("green-shadow");
השדה `color` הוא **הקס** ("#00c875"). קריאה מהשדה הלא-נכון מחזירה
`undefined`, ואז `color` — שהוא **חובה** ב-API — אינו נשלח והקריאה
נופלת כולה ב-`BAD_USER_INPUT`.

הטבלה ב-`tools/monday-colors.json` (הועברה לתוך המאגר מ-scratchpad,
שהוא ב-gitignore). ⚠ **צבע שאין לו מיפוי — נכשלים ברעש ולא
מנחשים**: ניחוש על אחת משלוש התוויות הראשונות מפיל את כל הקריאה,
כולל את החלקים שכן היו נכונים (4כד).

### כלים שנוספו

| כלי | מה |
|---|---|
| `tools/seed-army.mjs` | שלושת שינויי הלוחות של "מיונים ושיבוצים" |
| `tools/name-slot5.mjs` | נותן שם כן למשבצת הריקה |
| `tools/deactivate-label.mjs` | משבית תווית, ומדווח כמה שורות עליה |
| `tools/monday-colors.json` | מיפוי `var_name` → enum, שנבנה בניסוי |

שלושתם **הרצה יבשה כברירת מחדל**, `--go` כדי לבצע, ומסרבים לרוץ
כשיש נתון אמיתי מאחורי מה שהם עומדים לשנות — לפי `value`, לא לפי
`text`.

---

## 5ח · הפרויקטים שלי — הגבול השני מול הצוות

`api/_projects.js` + `api/_project-items.js` + `src/Projects.jsx`,
ושלושה לוחות: הפרויקט, המשימות שלו, ותנועות התקציב שלו.

### ⚠⚠⚠ זו נקודת הקצה **השנייה** שבה `isManager` מבטל גישה

אחרי משימות בעלי התפקידים (4מה), וזה הנימוק זהה: פרויקט הוא
המקום שבו חניך **מנסה**. הוא מתכנן תקציב שאולי לא יסתדר, כותב
משימות שאולי לא יבוצעו, ומשנה מטרה באמצע. ברגע שהוא יודע
שמישהו קורא — הוא כותב אחרת, וזה כבר לא כלי עבודה אלא דוח.

* **הכלל נאכף בשורה אחת בראש ההנדלר**, לפני כל דבר אחר, ולא
  בסינון בכל מסלול. מסלול שייכתב מחר מוגן מעצמו.
* ⚠ **ההבטחה כתובה במסך** (`.pr-private`) ולא רק בקוד — חניך
  שאינו יודע שאיש אינו קורא ינהל את הפרויקט כאילו מסתכלים.
* ⚠ **עמודת הבעלים מזהה בלבד ולא שם**, כדי שהלוח ב-monday
  ייקרא כרשימת פרויקטים ולא כיומן של אדם.
* ⚠ **ומה שאי אפשר להסתיר**: הלוחות ב-monday, ולאיש צוות עם
  גישה ללוח יש גישה אליהם. אין טריק שסוגר את זה — מה שהמערכת
  מבטיחה הוא שהיא אינה בונה כלי לקרוא אותם.
* `project-test` נועל את שלושת הכיוונים, **עם מנהל מחובר
  באמת**: ג׳ר ריק מקבל 401, וטענת 403 הייתה נשארת ירוקה גם
  אילו הגבול נשבר.

### ⚠ פרויקט קבוצתי הוא מצב רגיל

`partners` מחזיק מזהי חניכים, וכל שותף רואה ועורך.

* ⚠ **השותפים מאומתים בשרת** מול `assignableStudents`. בלי זה
  אפשר היה לשתף מזהה שרירותי ואז לקרוא כל פרויקט במערכת דרך
  "אני שותף בו".
* ⚠ **רק מי שפתח משנה את רשימת השותפים ומוחק** — אחרת שותף
  אחד יכול להוציא את השאר.
* ⚠ **שם השותף נגזר מהמזהה ואינו נשמר** — שם שמור אינו
  מתעדכן כשחניך משנה שם.
* ⚠ **404 ולא 403** על פרויקט של מישהו אחר, ועל שורה ששייכת
  לפרויקט כזה. השייכות נבדקת **דרך הפרויקט ולא דרך השורה**:
  עמודת בעלים שנייה על המשימה יכולה לסתור את הראשונה.

### ⚠ מה שנגזר אינו נשמר

התקדמות, סכומים ויתרה מחושבים בשרת מהשורות עצמן.

* ⚠ **`pct` הוא `null` ולא 0 בלי משימות** — 0% נראה כמו
  "לא התקדמתי" בעוד שהאמת היא "אין מה למדוד" (4נ, 4ג).
* ⚠ **ריק אינו אפס**: תקציב שלא נקבע אינו אפס שקלים, ולכן
  "נותר" אינו מוצג כלל. תנועה בלי סכום **נספרת ומדווחת**
  (`noAmount`) ואינה מושמטת בשקט (4ט).
* ⚠ **הכנסה והוצאה הן שני מספרים**, לא אחד עם סימן.
* ⚠ **פרויקט עם תוכן אינו נמחק — הוא מארכב**, וההודעה אומרת
  כמה יש ומה לעשות במקום (4ק).

### ⚠ ומה שנוסף מסביב

* **`shared/cycles.js` ו-`TARGETS` ב-`_cycle.js`** — שלושת
  הלוחות שייכים למחזור. בלי השורות האלה מחזור חדש נוצר
  בלעדיהם, ותקלה כזו מתגלה רק בעוד שנה (4ל).
* **8 מתוך 12 פונקציות** — שלושת המסלולים נכנסו כ-`?action=`
  תחת `students.js`, ולא כקובץ נספר חדש.
* `.pr-pick` משתמשת ב-`.scroll-y` הקיימת: כלל `max-height`
  חדש בתוך `.rows` נבלע על ידי `.kx .rows{overflow:hidden}`
  (4ק), ו-`.kx .pr-chk` נושאת קידומת מלאה כי `.kx button`
  מאפסת רקע ומסגרת (4מח).

---

## ⚠⚠ `| head -N` על הרצת בדיקה הורג את הניקוי

קרה **פעם שנייה**. `head` סוגר את הצינור, התהליך מקבל SIGPIPE
ומת — ובלוק ה-`finally` שמוחק את שורות הבדיקה **אינו רץ**.
בפעם הזו נשארו שורות "בדיקה — " בשלושת לוחות הפרויקטים.

**הכלל: להריץ בדיקה עם `| tail -N`, ולעולם לא `| head -N`.**
כשצריך לראות דווקא את ההתחלה — להריץ לקובץ ואז לקרוא ממנו.

---

## 5ט · המותג — מקום אחד, ושתי צורות ללוגו

`src/brand.js`. הלוגו ישב ב**שלושה** מקומות: base64 בן 48KB
ב-`src/logo.js`, קובץ ב-`public/logo-mark.png`, ומחרוזת נתיב
מוקלדת בכל מסך. החלפת לוגו נגמרה בכך ששניים מהמסכים התעדכנו
והשניים האחרים לא. `src/logo.js` נמחק.

### ⚠⚠ `hasWordmark` קובע את כל הפריסה

הסמל קיים בשתי צורות: **סמל בלבד** (העיגול עם הדגל והשביל),
ו**לוקאפ מלא** שכולל בתוך התמונה גם את "במעלה הדרך · מכינת
ניר עוז".

* כשהתמונה כוללת את הכיתוב, המסך **אינו מוסיף אותו שוב** —
  אחרת שם המכינה מופיע פעמיים, אחת מצוירת ואחת בפונט ובגודל
  אחר. זה נראה כמו תקלה, לא כמו עיצוב.
* ⚠ **והמידה ברוחב ובתקרת גובה, לא בגובה קבוע.** עיגול הוא
  ריבוע; לוקאפ הוא גבוה וצר, וגובה קבוע היה מקטין אותו עד
  שהכיתוב שבתוכו מפסיק להיקרא.
* להחלפה: שומרים את הקובץ ב-`public/`, ומעדכנים `lockup`
  ו-`hasWordmark` — שתי שורות, וכל המסכים מתעדכנים.

⚠ **והלוח הלבן מתחת ללוגו במסך הכניסה אינו קישוט.** הסמל הוא
נייבי וחום על רקע שקוף, והוא יושב על תצלום כהה — בלעדיו
העיגול נבלע והשביל החום נעלם.

---

## ⚠⚠ קובץ חדש בזמן ש-vite dev רץ — המטמון מחזיק ישן

`src/brand.js` נוצר בזמן שהשרת רץ, וה-import שלו נפל
ב-`ReferenceError: BRAND is not defined` **אף שהקוד היה תקין
לחלוטין**. `vite build` עבר, והדפדפן החזיר מסך לבן.

הסיבה: `node_modules/.vite` החזיק חבילה שנבנתה לפני שהקובץ
היה קיים.

**הכלל: אחרי הוספת קובץ מקור חדש — לעצור את שרת הפיתוח,
למחוק `node_modules/.vite`, ולהפעיל מחדש.** ובלי זה, כל בדיקה
בדפדפן בודקת גרסה שאינה הקוד שכתוב.

⚠ וזו עוד הוכחה לאותו כלל: הבנייה אינה תופסת `ReferenceError`,
ורק פתיחה בדפדפן תופסת.

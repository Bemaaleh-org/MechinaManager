/* ============================================================
   מובילי השבוע — חשיפה בתחילת השבוע, ולא לפניה
   ------------------------------------------------------------
   ההחלטה (ראש המכינה, 23.9.2026): *"המובילשים זה כעקרון עניין
   שעושים אליו חשיפה בתחילת השבוע... אני רוצה שזה ישאר בסוד
   ולא יחשף לשאר החניכים עד 0:00 ביום שלמחרת תחילת השבוע."*

   ⚠⚠ **הסוד הוא מהחניכים האחרים, ולא מהמובילים עצמם.** מוביל
     שיגלה ביום ראשון בבוקר שהוא מוביל השבוע כבר איחר את
     ההכנה — זו בדיוק הסיבה שמסך ההובלה נפתח ל-`leadsAnyWeek`
     ולא ל-`isLeader` (5ב, 5יא). הכלל כאן אינו נוגע בזה.

   ⚠⚠ **"עד 0:00 ביום שלמחרת" הוא השוואת תאריכים, לא שעון.**
     `today > start` בתאריכי ישראל: ברגע שהתאריך עבר את יום
     הפתיחה, החשיפה קרתה. אין כאן חישוב שעה, אין אזור זמן
     ואין מה לשבור במעבר לשעון קיץ — וההרשאה עוברת בחצות
     מעצמה, בלי שאיש ידליק דגל (אותו דפוס של 5ב).

   ⚠ **תאריך הפתיחה הוא של התקופה המוצגת ולא של השורה.**
     שבועות חופפים מוצגים כתקופה אחת ("שבוע 3-4", ראו
     shared/week-span.js), ולתקופה אחת יש חשיפה אחת — בתחילתה.
     שתי חשיפות לתקופה שהמסך מציג כאחת היו נראות כמו באג.

   ⚠ **מה שנשאר גלוי בכוונה: שבועות שכבר עברו.** דף המובילויות
     מציג רק תקופות שנגמרו (`end < today`, ראו _leadership.js),
     והן ממילא אחרי החשיפה. הכלל כאן חל על ההווה והעתיד.
   ============================================================ */

/** יום אחד קדימה, בתאריכי ISO. ⚠ UTC בכוונה — אלה תאריכים
    ולא רגעים, ו-`Date` מקומי היה מזיז אותם ביום שלם. */
const nextIso = (iso) => {
  if (!/^\d{4}-\d{2}-\d{2}$/.test(String(iso || ""))) return "";
  const d = new Date(iso + "T00:00:00Z");
  d.setUTCDate(d.getUTCDate() + 1);
  return d.toISOString().slice(0, 10);
};

/** מתי התקופה נחשפת — 0:00 ביום שאחרי תחילתה. */
export const revealDate = (start) => nextIso(start);

/**
 * האם החשיפה כבר קרתה.
 * ⚠ תקופה בלי תאריך פתיחה נחשבת **חשופה**: זו שורה שבורה
 *   בלוח, והסתרה שקטה בגללה הייתה מוחקת מידע תפעולי אמיתי
 *   בלי שאיש ידע למה (עיקרון 6).
 */
export const leadersRevealed = (start, today) =>
  !start || !today || String(today) > String(start);

/**
 * האם *הקורא הזה* רשאי לראות את מובילי התקופה הזו.
 *
 * @param {object} o
 * @param {string} o.start   תחילת התקופה המוצגת (span start)
 * @param {string} o.today   תאריך ישראל, מכבד ?today=
 * @param {boolean} o.staff  איש צוות — רואה תמיד
 * @param {boolean} o.isLeader הקורא הוא אחד ממובילי התקופה
 * @param {boolean} o.assigns הקורא משבץ תורנויות בתקופה הזו
 *
 * ⚠⚠ **`assigns` הוא חריג תפעולי ולא פרצה.** מוביל שבוע פטור
 *   מתורנות, והשרת **חוסם** שיבוץ שלו (_chores-write.js). אב
 *   בית שלא יראה מי המובילים היה בוחר אחד מהם, מקבל 403 —
 *   ובהודעה עצמה את השם. כלומר ההסתרה לא הייתה מסתירה, רק
 *   מחליפה חיווי מראש בשגיאה בדיעבד (4יד). המסך אומר לו
 *   במפורש שהשמות טרם נחשפו.
 */
export const maySeeLeaders = ({ start, today, staff, isLeader, assigns } = {}) =>
  Boolean(staff) || Boolean(isLeader) || Boolean(assigns)
  || leadersRevealed(start, today);

/** מה שנאמר במקום השמות. ⚠ **נאמר ולא מושמט** — רשימה ריקה
    נקראת כמו "טרם שובצו" ושולחת לשאול את אב הבית (עיקרון 6). */
export const LEAD_SECRET_NOTE = "מובילי השבוע נחשפים בתחילתו";

/** אותה שורה, עם התאריך, למי שזה נוגע לו. */
export const leadSecretNote = (start) => {
  const at = revealDate(start);
  if (!at) return LEAD_SECRET_NOTE;
  const [, m, d] = at.split("-");
  return `${LEAD_SECRET_NOTE} — ייחשפו ב-${d}/${m}`;
};

/** למי שכן רואה, וזה עדיין אינו פומבי. */
export const LEAD_SECRET_STAFF = "טרם נחשף לשאר החניכים";

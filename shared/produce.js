/* ============================================================
   ירקות ופירות — כמה שוקלת יחידה
   ------------------------------------------------------------
   ⚠ **הערכה, לא מדידה.** עגבנייה שוקלת בין 80 ל-180 גרם, ומה
     שכתוב כאן הוא הממוצע. המספרים כאן נועדו לענות על "כמה
     ק״ג להזמין בשביל 35 איש", ולא לחשב עלות מדויקת.

     לכן כל מספר במסך מוצג עם **≈** ועם המילה "בערך". טבלה
     שנראית מדויקת ואינה, גרועה מטבלה שמצהירה על עצמה.

   ⚠ הטבלה כאן היא **ברירת מחדל בלבד**. פריט בלוח יכול לשאת
     משקל משלו בעמודה "ק״ג ליחידה", והוא גובר — עיקרון 1:
     מה שאפשר להגדיר בלוח, מוגדר בלוח. הטבלה קיימת כדי שלא
     יצטרכו למלא 60 שורות ביד.

   ⚠ ההתאמה לפי שם היא **חלקית ובשני הכיוונים**, כמו במצרכי
     המנות. "עגבניות שרי" תמצא את "עגבניות שרי" ולא את
     "עגבניות" — הרשימה ממוינת מהארוך לקצר כדי שההתאמה
     הספציפית תנצח.
   ============================================================ */

/** ק״ג ליחידה אחת, בערך */
export const PRODUCE_KG = {
  /* ---- ירקות ---- */
  "עגבנייה": 0.12,
  "עגבניות שרי": 0.012,
  "מלפפון": 0.11,
  "בצל": 0.15,
  "בצל ירוק": 0.02,
  "שום": 0.05,
  "גזר": 0.08,
  "תפוח אדמה": 0.17,
  "בטטה": 0.22,
  "פלפל": 0.16,
  "חציל": 0.35,
  "קישוא": 0.2,
  "כרוב": 1.2,
  "כרובית": 0.9,
  "ברוקולי": 0.4,
  "חסה": 0.4,
  "כרישה": 0.3,
  "סלרי": 0.6,
  "דלעת": 2.5,
  "דלורית": 0.9,
  "תירס": 0.3,
  "לימון": 0.1,
  "אבוקדו": 0.2,
  "פטרוזיליה": 0.06,
  "כוסברה": 0.06,
  "שמיר": 0.05,
  "נענע": 0.05,
  /* ---- פירות ---- */
  "תפוח": 0.18,
  "בננה": 0.12,
  "תפוז": 0.2,
  "קלמנטינה": 0.09,
  "אשכולית": 0.35,
  "אגס": 0.17,
  "אפרסק": 0.15,
  "נקטרינה": 0.14,
  "שזיף": 0.07,
  "משמש": 0.05,
  "אפרסמון": 0.2,
  "רימון": 0.3,
  "מנגו": 0.35,
  "אבטיח": 5,
  "מלון": 1.5,
  "אננס": 1.5,
  "ענבים": 0.5,
  "תות": 0.02,
  "קיווי": 0.09,
};

/* ============================================================
   נרמול עברי
   ------------------------------------------------------------
   ⚠ **אותיות סופיות מתחלפות ברבים.** "מלפפון" נגמר ב-ן סופית,
     ו"מלפפונים" מכיל נ רגילה — ולכן `includes` פשוט **אינו
     מוצא** אותו. זה תפס אותנו בבדיקה הראשונה, וזו מלכודת
     שתחזור בכל התאמת טקסט בעברית במערכת.

   ⚠ גם י כפולה: "עגבנייה" מול "עגבניה". שתי הצורות תקינות,
     ואנשים כותבים את שתיהן.
   ============================================================ */
const SOFIT = { "ך": "כ", "ם": "מ", "ן": "נ", "ף": "פ", "ץ": "צ" };

/**
 * מילה → שורש גס.
 * ⚠ הסדר קובע: קודם מסירים סיומת ריבוי מהמילה **המקורית**
 *   ("מלפפונים" → "מלפפונ"), ורק אז ממירים אות סופית
 *   ("מלפפון" → "מלפפונ"). הפוך, "מלפפונים" היה הופך
 *   ל-"מלפפונימ" והסיומת כבר לא הייתה מזוהה.
 */
function stem(w) {
  let t = String(w || "").trim().replace(/["׳״֑-ׇ]/g, "");
  t = t.replace(/(ים|ות|יות)$/, "");
  t = t.replace(/[ךםןףץ]/g, (c) => SOFIT[c]);
  t = t.replace(/יי/g, "י").replace(/[הי]$/, "").replace(/י$/, "");
  return t;
}

const stems = (t) => String(t || "").trim().split(/\s+/).map(stem).filter(Boolean);

/* ============================================================
   ⚠ התאמה **מילה־מילה על שורשים**, ולא includes על המחרוזת.
   ------------------------------------------------------------
   שתי דרכים שנוסו ונכשלו:

   1. `includes` פשוט — "מלפפונים" לא מצא את "מלפפון", כי ן
      סופית ו-נ רגילה הן תווים שונים.
   2. `includes` דו-כיווני — "עגבניות" מצא את "עגבניות שרי",
      כי הכיוון השני התאים. שרי שוקלת 12 גרם ועגבנייה 120,
      ופי עשרה בהזמנת ירקות הוא הפרש אמיתי.

   הכלל: מפתח מתאים אם **כל** מילותיו נמצאות בשאילתה. "עגבניות
   שרי" דורש גם "שר", ולכן אינו נתפס על ידי "עגבניות" לבדה.
   ============================================================ */
const KEYS = Object.keys(PRODUCE_KG)
  .map((k) => ({ k, w: stems(k) }))
  .sort((a, b) => b.w.length - a.w.length
    || b.w.join("").length - a.w.join("").length);

/**
 * שם פריט → ק״ג ליחידה, או null.
 * ⚠ null פירושו "לא ידוע" ולא אפס. פריט שאינו ברשימה אינו
 *   שוקל כלום — הוא פשוט לא נמדד ביחידות.
 */
export function kgPerUnit(name) {
  const raw = String(name || "").trim();
  if (!raw) return null;
  if (PRODUCE_KG[raw] != null) return PRODUCE_KG[raw];
  const q = stems(raw);
  if (!q.length) return null;
  const hit = KEYS.find((x) => x.w.every((kw) => q.some((qw) => qw === kw)));
  return hit ? PRODUCE_KG[hit.k] : null;
}

/** האם הפריט הוא ירק או פרי שאנחנו יודעים לשקול */
export const isProduce = (name) => kgPerUnit(name) != null;

/**
 * כמות ביחידות → ק״ג בערך.
 * ⚠ עיגול לשתי ספרות. 12 עגבניות הן 1.44 ק״ג, לא 1.4 —
 *   ובכמויות של מכינה ההפרש מצטבר.
 */
export function unitsToKg(units, name, override) {
  const per = override != null && override > 0 ? Number(override) : kgPerUnit(name);
  const n = Number(units);
  if (!per || !Number.isFinite(n) || n <= 0) return null;
  return Math.round(n * per * 100) / 100;
}

/** ק״ג → כמה יחידות, בערך */
export function kgToUnits(kg, name, override) {
  const per = override != null && override > 0 ? Number(override) : kgPerUnit(name);
  const n = Number(kg);
  if (!per || !Number.isFinite(n) || n <= 0) return null;
  return Math.round(n / per);
}

/* ============================================================
   מחיר
   ------------------------------------------------------------
   ⚠ המחיר הוא **סדר גודל**, לא מחיר חוזה. הוא נועד לענות
     "כמה בערך תעלה ההזמנה הזו", ולכן מוצג מעוגל ועם ≈.
   ============================================================ */

/** מחיר × כמות, או null אם חסר אחד מהם */
export function lineCost(price, qty) {
  const p = Number(price), q = Number(qty);
  if (!Number.isFinite(p) || p <= 0 || !Number.isFinite(q) || q <= 0) return null;
  return Math.round(p * q * 100) / 100;
}

/** הרשימה לתצוגה במסך ההמרה, ממוינת בעברית */
export const produceList = () =>
  Object.entries(PRODUCE_KG)
    .map(([name, kg]) => ({ name, kg, perKg: Math.round(1 / kg * 10) / 10 }))
    .sort((a, b) => a.name.localeCompare(b.name, "he"));

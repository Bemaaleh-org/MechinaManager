/* ============================================================
   מזהי לוח ההודעות לקבוצה — קובץ מחולל
   ------------------------------------------------------------
   ⚠ אין לערוך ידנית. נכתב מחדש במלואו על ידי
     tools/seed-group.mjs כשהוא יוצר את הלוח ב-monday.

   ⚠ **אובייקט ולא מחרוזת**, כמו כל שאר מזהי הלוחות במערכת:
     api/_cycle.js מחליף את המחזור הפעיל עם Object.assign על
     האובייקט הקיים, ומחרוזת מיוצאת הייתה נקבעת פעם אחת בטעינת
     המודול ולא ניתנת להחלפה בזמן ריצה (CLAUDE.md 4ל).

   ⚠ **הקבוצה עצמה כבר קיימת ואינה כאן.** מי משובץ למי, ומי
     המדריך — נשלפים מלוח ההגדרות ומלוח השיבוץ
     (shared/placements.js, api/_guides.js). הלוח היחיד שנוסף
     על ידי "הקבוצה שלי" הוא מקום להודעה שהמדריך מפרסם לקבוצתו,
     ולכן מזהה אחד בלבד.

   להקמה:  node --env-file=.env tools/seed-group.mjs
   ============================================================ */

export const GROUP_BOARDS = {
  messages: "",
};

export const GROUP_COLS = {
  messages: {
    /* מזהה הגדרת השיבוץ (לוח ההגדרות) — טקסט, לא לינק בין
       לוחות. אותו דפוס כמו עמודות student/placement בלוח
       השיבוץ עצמו (shared/placements-ids.js). */
    group: "",
    /* שם הקבוצה, לקריאות בלוח ב-monday בלבד — כמו studentName
       בלוח השיבוץ. */
    groupName: "",
    body: "",
    date: "",
    pinned: "",
    by: "",
    byId: "",
  },
};

/* ============================================================
   ⚠ **הלוח הזה אינו חוסם את המסך "הקבוצה שלי".**

   רוב המסך — מי הקבוצה, מי המדריך, מי החברים — נגזר מלוחות
   שכבר קיימים (הגדרות + שיבוץ), ואינו תלוי בלוח הזה בכלל.
   לכן api/_group.js אינו מחזיר 503 על GET כשהלוח הזה טרם
   הוקם: הוא מציג את הקבוצה כרגיל, ומחזיר רשימת הודעות ריקה
   ו-`canPost:false` בלבד. 503 שמוריד את כל המסך בגלל לוח
   עזר אחד הוא בדיוק הכשל שנתפס בעבר בטבלת ההמרות (4לו).

   הפעולות שכן דורשות את הלוח (פרסום, עריכה, מחיקה) מחזירות
   503 מפורש משלהן.
   ============================================================ */
export const groupReady = () => Boolean(GROUP_BOARDS.messages);

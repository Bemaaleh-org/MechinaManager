/* ============================================================
   תקלות ובעיות — תוויות וכללים
   ------------------------------------------------------------
   מעקב תקלות תחזוקה. גלוי למנהלים ולאב הבית בלבד — ההרשאה
   נאכפת בשרת (withAuth {house:true}).

   בניגוד לאירוע בטיחות, תקלה אינה רשומה רשמית: מותר למחוק
   שורה שנפתחה בטעות. תקלה שטופלה עדיף לסמן "טופלה" ולא
   למחוק — כך נשארת היסטוריית תחזוקה.
   ============================================================ */

import { FAULTS, FAULTS_BOARD, FAULTS_COLS } from "./faults-ids.js";

/* ⚠ FAULTS הוא אובייקט ({ board }) ולא מחרוזת, כדי שהחלפת
   מחזור תוכל להחליף את המזהה בזמן ריצה — ראו api/_cycle.js.
   FAULTS_BOARD נשאר לתאימות ואינו מתחלף. */
export { FAULTS, FAULTS_BOARD, FAULTS_COLS };

export const faultsReady = () => Boolean(FAULTS.board);

/** איפה התקלה */
export const FAULT_PLACE = [
  "כיתה", "חאן יונס", "מגורי בנים", "מגורי בנות", "משרדים", "חוץ",
  "מכולה", "מגורי צוות",
  /* ⚠ "אחר" אחרון — הוא סל ולא מקום, ורשימה שנגמרת בו נקראת
     נכון. בלעדיו תקלה במקום שאינו ברשימה נרשמה במקום שגוי. */
  "אחר",
];

/** מי מתקן */
export const FAULT_FIX = { pro: "בעל מקצוע", inhouse: "בתוך המכינה" };
export const FIXES = [FAULT_FIX.pro, FAULT_FIX.inhouse];

/** דחיפות */
export const FAULT_URGENCY = { urgent: "דחוף", normal: "רגיל" };
export const URGENCIES = [FAULT_URGENCY.urgent, FAULT_URGENCY.normal];

/* ============================================================
   ⚠⚠ **תקלה או שדרוג — שתי שאלות שונות לאותו לוח.**
   ------------------------------------------------------------
   הבקשה: *"בתקלות תוסיף אופציה לשידרוגים ותקרא
   לזה תקלות ושידרוגים, וגם שתיהיה בחירה האם זה
   תקלה או שדרוג."*

   ⚠ **לוח אחד ולא שניים.** אותו מקום, אותו אחראי,
     אותו מחזור חיים ואותן תמונות — זה הדפוס של לוח
     רשומות הצוות שמחזיק חמישה סוגים (5י). שני לוחות
     היו מתפצלים בתיקון הראשון.

   ⚠⚠ **וריק הוא "תקלה".** 40 השורות שכבר בלוח נכתבו
     בעולם שבו כל שורה היא תקלה, וברירת מחדל אחרת
     הייתה מסווגת את כולן מחדש למפרע — ולמחוק את המספר
     שההבחנה קיימת בשבילו.

   ⚠ וזה **מה זה**, ולא **כמה זה דחוף**: שדרוג יכול
     להיות דחוף (מזגן לפני הקיץ) ותקלה יכולה להמתין.
     שני שדות, ולא אחד שמנסה לומר את שניהם.
   ============================================================ */
/* ============================================================
   ⚠⚠ **סוג שלישי: "המלצה על ציוד"** (בקשת ראש המכינה, 22.9.2026)
   ------------------------------------------------------------
   *"יהיה אפשר לכתוב את שם הפריט ופירוט מדוע צריך אותו"*.

   ⚠ **ואין לו שדות משלו.** שם הפריט הוא שם השורה, והנימוק
     הוא `desc` — בדיוק שני השדות שכבר קיימים. עמודות נפרדות
     היו ריקות ב-40 השורות הקיימות ובכל תקלה עתידית, ומי
     שמסתכל בלוח היה רואה שתי עמודות שכמעט תמיד ריקות. מה
     שמשתנה הוא **הכיתוב במסך**, לא המבנה.

   ⚠ **ומיקום אינו חובה כאן.** "איפה" היא שאלה על תקלה; על
     המלצה לקנות מקרר היא לרוב חסרת משמעות, ודרישה למלא
     אותה מלמדת לבחור ערך שרירותי — וזה מזהם את הסינון של
     אב הבית. הדרישה נשארת בתוקף לתקלה ולשדרוג.
   ============================================================ */
export const FAULT_KIND = { fault: "תקלה", upgrade: "שדרוג", gear: "המלצה על ציוד" };
export const KINDS = [FAULT_KIND.fault, FAULT_KIND.upgrade, FAULT_KIND.gear];

/** האם הדיווח הוא המלצה על ציוד — משותף למסך ולשרת, כדי
    ששניהם יסכימו מה חובה ומה לא (4יד). */
export const isGear = (kind) => String(kind || "") === FAULT_KIND.gear;

/** ⚠ בלי העמודה המסך פשוט אינו מציג בורר — ולא נופל (עיקרון 6). */
export const faultKindReady = () => Boolean(FAULTS_COLS.kind);

/** מחזור החיים של תקלה */
export const FAULT_STATUS = { open: "פתוחה", working: "בטיפול", done: "טופלה" };
export const STATUSES = [FAULT_STATUS.open, FAULT_STATUS.working, FAULT_STATUS.done];

/* ============================================================
   מה חניך רואה על תקלה שהוא דיווח
   ------------------------------------------------------------
   ⚠ החלטה של המכינה: החניך רואה סטטוס, ולא עלויות או פרטי
     איש המקצוע. כמו כל נתון רגיש במערכת — הסינון בשרת ולא
     בתצוגה, ובמיפוי מפורש ולא בהשמטה, כדי שעמודה חדשה שתיווסף
     ללוח לא תדלוף מעצמה (עיקרון 4).
   ============================================================ */
export function toStudentFault(f, mine = true) {
  return {
    id: f.id,
    /* ⚠ **מי דיווח אינו יוצא לחניך — רק "שלי" או לא.** הרשימה
       פתוחה לכולם כדי שלא ידווחו על אותה תקלה פעמיים, ולשם כך
       די בכותרת, במקום ובסטטוס. שם המדווח היה הופך את הרשימה
       ליומן של מי דיווח על מה, וזו אותה הבטחה שבגללה אין שדה
       "מי ביצע" בתורנויות (עיקרון 5). לצוות השם כן יוצא —
       הוא במיפוי המלא, לא כאן. */
    mine: Boolean(mine),
    title: f.title,
    date: f.date,
    place: f.place,
    /* ⚠ גם לחניך: "תקלה" מול "שדרוג" אינו נתון רגיש —
       הוא מה שהוא עצמו בחר כשדיווח, ובלעדיו הרשימה
       המשותפת מערבבת שתי רשימות שונות. */
    kind: f.kind,
    urgency: f.urgency,
    status: f.status,
    desc: f.desc,
    hasPhoto: Boolean(f.photoUrl),
    photoUrl: f.photoUrl || null,
    /* ⚠ גם תמונת "אחרי" גלויה לחניך. הוא זה שדיווח, והתמונה
       שמראה שהתקלה תוקנה היא התשובה לדיווח שלו — בדיוק מה
       שגורם לאנשים להמשיך לדווח. */
    photoDoneUrl: f.photoDoneUrl || null,
    /* ⚠ שדות הדיווג שהחניך רשאי לתקן — `fix` נכלל כדי שהטופס
       יוכל להציג אותו כפי שנשמר. שדות הטיפול אינם כאן. */
    fix: f.fix || null,
    /* ⚠ נגזר בשרת כדי שהכפתור יידע מראש (4יד): עריכה עד
       שטופל, מחיקה רק כשעדיין פתוחה — ורק על מה שאני דיווחתי.
       ⚠ **הדגל אינו ההגנה.** השרת בודק בעלות שוב בכל PUT
         ו-DELETE; זה כאן כדי שהכפתור לא יופיע ויקבל 404. */
    canEdit: mine && f.status !== FAULT_STATUS.done,
    canDelete: mine && f.status === FAULT_STATUS.open,
  };
}

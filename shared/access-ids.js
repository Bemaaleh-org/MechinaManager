/* ============================================================
   הרשאות מותאמות — קובץ מחולל
   ------------------------------------------------------------
   ⚠ אין לערוך ידנית. נכתב על ידי tools/seed-access.mjs.
     להקמה:  npm run seed:access

   ⚠ **אובייקט ולא מחרוזת** — api/_cycle.js מחליף מזהים עם
     Object.assign (4ל). מזהה שמיוצא כמחרוזת נקבע פעם אחת
     בטעינת המודול ואי אפשר להחליף אותו.

   ⚠⚠ **מחוץ ל-CYCLE_BOARDS.** ההרשאות הן של הצוות ושל
     התפקידים, ולוח ההרשאות עצמו כבר מחוץ למחזור מאותו טעם
     (4ל). התאמה שתימחק בספטמבר הייתה פותחת בשקט מסכים
     שמישהו סגר בכוונה — וזה הכיוון המסוכן.

   ⚠⚠ **שורה כאן יכולה רק לצמצם.** ראו shared/access-rules.js:
     מנגנון שמעניק גישה הופך כל כלל אבטחה במאגר לניתן לעקיפה
     משורה בלוח.
   ============================================================ */

export const ACCESS_BOARDS = {
  "board": "5104334703"
};

export const ACCESS_COLS = {
  "subject": "text_mm78n5gg",
  "screen": "text_mm78pmh4",
  "level": "color_mm786hay",
  "note": "text_mm78kjfj",
  "by": "text_mm785km2"
};

/** ⚠ בלי הלוח המערכת עובדת **בלי שום התאמה** — לא נחסמת (עיקרון 6). */
export const accessReady = () =>
  Boolean(ACCESS_BOARDS.board && ACCESS_COLS.subject && ACCESS_COLS.level);

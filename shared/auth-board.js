/* ============================================================
   לוח המשתמשים וההרשאות — מזהים
   ------------------------------------------------------------
   ⚠ הקודים בלוח הזה נקראים בצד השרת בלבד. אין לייבא את הקובץ
     הזה מתוך src/ — הוא מוביל ישירות לנתונים שאסור שיגיעו
     לדפדפן.
   ============================================================ */

export const AUTH_BOARD = "5101814098";

export const AUTH_COLS = {
  kind: "color_mm61prrq", // סוג
  code: "text_mm61rp9k", // קוד
  active: "boolean_mm61s6k2", // פעיל
  /* ⚠ תפקיד בתוך הצוות. ריק = איש צוות רגיל: רואה הכול, אינו
     מכריע בבקשות יציאה. השדה חי בלוח בכוונה — ראש המכינה יוצא
     לחופשה, וסימון הסגנית כ"ראש מכינה" פותח לה את ההכרעה בלי
     דיפלוי ובלי שאיש ייתקע. */
  role: "color_mm6j2s60", // תפקיד במכינה
  /* ============================================================
     ⚠ **צפייה בלבד — ריק פירושו הרשאה מלאה.**

     הקוטביות ההפוכה היא כל העניין: כל שורה שקיימת היום נכתבה
     לפני שהעמודה נוספה, ותיבה ריקה שמשמעותה "צפייה בלבד"
     הייתה נועלת את **כל** הצוות בשקט — כולל את ראש המכינה,
     שהיה מגלה את זה רק כשינסה לאשר בקשת יציאה.

     ⚠ והאכיפה **בשרת ובמקום אחד** (`withAuth` ב-api/_session.js):
     כל בקשה שאינה GET נדחית. מסך שמסתיר כפתורים הוא הצעה;
     שער אחד שחוסם כתיבה הוא הבטחה. מסך שיוסיף כפתור מחר
     מוגן מעצמו.
     ============================================================ */
  viewOnly: "boolean_mm6pgkcg", // צפייה בלבד
};

/** תוויות עמודת "תפקיד במכינה" */
export const STAFF_ROLE = {
  head: "ראש מכינה", // מכריע סופית בבקשות יציאה
  guide: "מדריך",    // ממליץ על בקשות החניכים בקבוצה שלו
};

/** תוויות עמודת "סוג" */
export const KIND = {
  shared: "קוד משותף",
  manager: "מנהל",
  trainee: "חניך",
};

/* ============================================================
   נעילת התפקידים הקבועים
   ------------------------------------------------------------
   שינוי מי נושא תפקיד דורש קוד. הבקשה: *"תנעל אותם ויהיה
   אפשר לפתוח אותם רק עם קוד שמכניסים, בלי זה אי אפשר לשנות
   את מי שיש לו הרשאה בתפקיד."*

   ⚠⚠⚠ **הקוד יושב בעמודה משלו ולא בעמודת `code`.**

     עמודת `code` היא **סוד הכניסה** — `_signin.js` מחפש בה
     התאמה, וכל ערך שיושב בה הוא קוד שאפשר להיכנס איתו
     למערכת. קוד נעילה שהיה נכתב לשם היה הופך את 890890
     לדלת כניסה, וזו ההפך הגמור מהכוונה.

     לכן: עמודה נפרדת (`lockCode`), על שורה שעמודת ה-`code`
     שלה **ריקה** ו-`active` שלה **כבוי**. שני אלה יחד
     הופכים אותה לבלתי נגישה לכניסה: בלי סוד אין מה להתאים
     (`_signin.js` דורש `r.secret`), ושורה כבויה נדחית ממילא.

   ⚠ **בלוח ולא בקוד** — עיקרון 1 ועיקרון 2. ראש המכינה משנה
     את הקוד ב-monday בלי דיפלוי, ושום סוד אינו יושב במאגר.

   ⚠ **והאכיפה בשרת.** בדיקה בדפדפן היא נוחות תצוגה; מי
     שקורא ל-endpoint ישירות עוקף אותה (עיקרון 3).
   ============================================================ */

/** שם השורה שנושאת את הגדרות המערכת. ⚠ אינה משתמש. */
export const SETTINGS_ROW = "הגדרות המערכת";

/** ברירת המחדל שנזרעת בהקמה. משנים בלוח, לא כאן. */
export const ROLES_LOCK_DEFAULT = "890890";

/** ⚠ ריק עד שהעמודה נוצרת — `npm run seed:roles-lock` */
export const SETTING_COLS = {
  lockCode: "text_mm777gdv",
};

export const rolesLockReady = () => Boolean(SETTING_COLS.lockCode);

# המשך עבודה — מאיפה ממשיכים

> **המסמך הזה נועד למחשב אחר, לסשן אחר, ולקלוד שלא היה כאן.**
> קראו אותו ראשון. אחריו `CLAUDE.md` — שם יושבים העקרונות
> המחייבים, וכל אחד מהם נובע מבאג אמיתי או מהתחייבות שניתנה.

עדכון אחרון: **30 באוגוסט 2026**.

---

## 1 · דקה ראשונה — שלוש פקודות

```bash
git clone <repo> && cd MechinaKitchen && npm install
```

```bash
cp .env.example .env
```

ואז **למלא ב-`.env` את `MONDAY_TOKEN` ואת `SESSION_SECRET`** —
בלעדיהם שום דבר לא רץ. את הערכים מקבלים מאחים; הם קיימים גם
במשתני הסביבה של Vercel.

```bash
npm run dev
```

השרת עולה על `http://localhost:5173`.

---

## 2 · ⚠ אם אין דפדפן — וזה קרה כבר

באפליקציית קלוד במחשב השני **הכלים של הדפדפן לא נפתחו**, ואי
אפשר היה לראות את `localhost`. זה לא חוסם כלום: **כל הבדיקה
במאגר הזה היא ממילא ללא דפדפן.**

### מה שמחליף את הדפדפן, לפי סדר העדיפות

**א. חבילות הבדיקה — הכלי העיקרי.** 20 חבילות, 423 טענות, כולן
פונות לשרת ב-`fetch` ובודקות תשובות אמיתיות מול monday האמיתית.

```bash
node --env-file=.env tools/tests/run.mjs
```

```bash
node --env-file=.env tools/tests/run.mjs chores rights
```

**ב. `curl` לכל נקודת קצה.** צריך עוגייה — מתחברים פעם אחת
ושומרים אותה:

```bash
curl -s -c /tmp/c.txt -X POST "http://localhost:5173/api/auth?action=login" -H "Content-Type: application/json" -d '{"code":"<הקוד של דני>"}'
```

```bash
curl -s -b /tmp/c.txt "http://localhost:5173/api/chores?action=view&admin=1" | head -c 2000
```

**ג. `vite build` — תופס שגיאות תחביר.**

```bash
npx vite build
```

⚠ **והוא אינו תופס `ReferenceError`.** משתנה שנקרא בלי ייבוא
עובר את הבנייה ונופל רק בזמן ריצה — זה קרה כאן פעמיים. הדרך
היחידה לתפוס אותו היא להריץ את הבדיקות או להכות בנקודת הקצה.

**ד. הייצור.** `<meta name="build">` נושא את ה-SHA הקצר:

```bash
curl -s https://nirozmanager.vercel.app/ | grep -o 'name="build" content="[^"]*"'
```

⚠ **דיפלוי לוקח 40–90 שניות.** להמתין **על תנאי** ולא על זמן:

```bash
until curl -s https://nirozmanager.vercel.app/ | grep -q "$(git rev-parse --short HEAD)"; do sleep 8; done; echo deployed
```

### ואם בכל זאת רוצים דפדפן

אין צורך בכלי של קלוד — `http://localhost:5173` נפתח בכל
דפדפן במחשב. ⚠ **בחלון פרטי**: `localStorage` הסתיר כאן באג
במשך יומיים (עיקרון 6).

---

## 3 · איפה הכול נמצא

| | |
|---|---|
| **ייצור** | https://nirozmanager.vercel.app |
| **ענף** | `lessons`, ומשקפים אותו גם ל-`main` |
| **נתונים** | monday.com בלבד. אין מסד אחר |
| **מזהי לוחות** | `shared/*-ids.js` — חלקם קבצים מחוללים |
| **מפת הלוחות** | `מיפוי-לוחות.md` |
| **הקמת עמדה** | `הקמת-עמדה.md` |
| **העקרונות** | `CLAUDE.md` — **המסמך המחייב** |

```bash
git push origin lessons && git push origin lessons:main
```

---

## 4 · מה נבנה בסשן הזה, לפי הסדר

כל אחד מהם מתועד בהרחבה ב-`CLAUDE.md` בסעיף שלו.

| # | מה | סעיף |
|---|---|---|
| 1 | **ניהול צוותים** — ועדות וסדרות מנהלות משימות במסך משלהן | 4נ |
| 2 | **הצפה בתוך ההקשר** + קטגוריית "צוות מזדמן" | 4ס |
| 3 | **שלוש רמות הרשאה** — צפייה בלבד, כתיבה לבעל התחום | 4ע |
| 4 | **פרטי חניך** בפרופיל, ולוח נוכחות של חודש אחד | 4פ |
| 5 | **תורנויות** — גזרות, מטבח, מעקב וצ׳ק ליסט | 4צ |
| 6 | תיקוני התורנויות, עריכת צ׳ק ליסט, מחיקת צוותים | 4ק |
| 7 | לשוניות עם חצים, דפדוף שבועות, שעות בבקשה, נהלים | 4ר |
| 8 | **האפליקציה נערכת מתוך עצמה** | 4ש |

### חמישה באגים אמיתיים שנמצאו בדרך

1. **⚠⚠ עקיפת סיסמה.** `?action=login` נתן סשן מלא לכל מי
   שמחזיק ת.ז של חניך — **גם אחרי שבחר סיסמה**. הנקודה אינה
   נקראת מאף מסך, ולכן איש לא הבחין. (4ע)
2. **⚠⚠ מחיקה חוצת-לוחות.** `DELETE` של הצפה בדק רק
   `!isStudent`, ו-`deleteItem` שולחת `delete_item` **בלי
   `board_id`** — כלומר כל איש צוות יכול היה למחוק כל שורה
   בכל לוח. (4ס)
3. **מדריכים יכלו לערוך תקציב ושיעורים** — לא בהחלטה, אלא
   כי `isManager` הוא הדגל הרחב ביותר במערכת. (4ע)
4. **שני `ReferenceError` חיים** ש-`vite build` לא תפס. (4ס)
5. **`renameItem` בשלושה ארגומנטים** ונקראה בשניים — כל שינוי
   שם נפל ב-502 גנרי, והבדיקה הייתה ירוקה כי כל מסלול נחסם
   קודם לכן. (4ס)

---

## 5 · מצב הלוחות עכשיו

**7 מתוך 12 פונקציות Vercel תפוסות.** נותרו 5.
`kitchen · auth · students · attendance · lessons · container · chores`

⚠ **חריגה מ-12 שוברת את הדיפלוי של כל המערכת**, לא רק של
התכונה החדשה. לפני הוספת קובץ ל-`api/` — לספור.

לוחות שנוצרו בסשן הזה:

| לוח | לשם מה | מזהים |
|---|---|---|
| צוותים – משימות | משימות ועדה, **משותפות** | `shared/team-ids.js` |
| צוותים – אוצר מילים | סטטוסים ושלבים, ו"נחשב סגור" | " |
| תורניות – גזרות | חמש גזרות + "מטבח וחד״א" | `shared/chores-ids.js` |
| תורניות – שיבוץ | שורה לכל חניך·גזרה·תקופה | " |
| תורניות – התאמות | 1+ / 1- ידני, עם סיבה | " |
| תורניות – צ׳ק ליסט | 33 מטלות מקובץ הנהלים | " |
| תורניות – ביצוע | שורה = בוצע | " |
| טקסטים באפליקציה | כל נוסח שנערך מהמסך | " |

⚠ **סקריפטי ההקמה חייבים לרוץ פעם אחת בכל סביבה חדשה**,
והקבצים המחוללים חייבים להיכנס לקומיט:

```bash
node --env-file=.env tools/seed-chores.mjs
```

```bash
node --env-file=.env tools/seed-hada.mjs
```

---

## 6 · חשבונות לבדיקה

| מי | איך נכנסים | מה זה |
|---|---|---|
| **חניך בדיקה** | `bdika` / `mechina2026` | חמישה תפקידים, ניתן לשיבוץ, **אינו נספר בשום מונה** |
| דני לויט | קוד בלוח ההרשאות | ראש מכינה |
| אלון רצון | `12341234` | **צפייה בלבד** — כל בקשה שאינה GET נדחית |

⚠ **חשבון הבדיקה `demo` אינו נספר בשום מקום** (4לא), אבל
**כן ניתן לשיבוץ** דרך `assignableStudents()`. כל שימוש בה
שנוגע במונה, בממוצע או במכסה הוא באג.

⚠ **הסיסמה של חשבון הבדיקה יושבת בקוד הבדיקות** ולא בלוח —
מה שבלוח הוא גיבוב scrypt. זה חשבון שאינו של אדם ואינו נספר,
ולכן זה מקובל כאן ורק כאן.

---

## 7 · מה פתוח

### חסום באחים

* **Google Sheets** — הפרסרים בנויים ובדוקים (50 טענות), אין
  אימות. Google חוסמת מפתחות חשבון שירות ברמת הארגון. צריך
  `GOOGLE_API_KEY` ב-Vercel וב-`.env`, ושלושה קבצי אקסל בדרייב
  שיומרו ל-Google Sheets אמיתיים.

### ביקש ועוד לא נעשה

* **העלאת קבצים למשימות וסריקה שלהם.** דורש מפתח וסוד נוספים
  ב-`.env`, ואחים ביקש שנחליט על זה יחד.

### פתוח מזמן

* החלפת הלוגו.
* שתי מנות דוגמה (שניצל, אורז לבן) שנשארו בלוח המנות.
* PR מ-`lessons` ל-`main` שרועי מאשר.
* **לאפס את חשבון הבדיקה** כשאחים יסיים לסייר — כרגע הוא נושא
  חמישה תפקידים.

### שאלה פתוחה שנשאלה ולא נענתה

**"מובילי שבוע לא יכולים לעשות תורנות מטבח בכלל"** — מומש
כ"לא בשבוע שהם מובילים". אם הכוונה הייתה פטור לכל השנה, זו
שורה אחת ב-`api/_chores-write.js` — אבל זה היה סותר את
ההוגנות שכל טבלת המעקב קיימת בשבילה.

---

## 8 · ⚠ שבע מלכודות שתפסו כאן, ויתפסו שוב

1. **`.kx button` מאפסת `background` ו-`border`** בסגוליות
   (0,1,1). כל כלל על כפתור חייב `.kx` לפניו. תפסה את
   `.task-box`, את `.yr2-c`, את `.tm-task` ואת `.mv-c`.
2. **`.kx .rows{overflow:hidden}` היא (0,2,0)** ומנצחת מחלקה
   בודדת גם כשהיא מאוחרת יותר בקובץ. רשימה גוללת בתוך `.rows`
   חייבת `.kx .rows.X`. תפסה שלוש פעמים.
3. **אין בקטיקים ב-`src/styles.js`.** הקובץ הוא template
   literal אחד, ובקטיק בהערה סוגר אותו — ואז `vite build`
   **מדווח הצלחה** בעוד הדפדפן נשבר. הבדיקה:
   ``grep -c '`' src/styles.js`` חייב להחזיר **2**.
4. **מטמון השרת יושב בתהליך אחר מהבדיקה.** `invalidate()`
   בסקריפט אינו נוגע בו. להמתין **על תנאי** ולא על זמן,
   ולבדוק את **תוכן ההודעה** ולא רק את הסטטוס — שתי בדיקות
   כאן עברו מסיבה שגויה בגלל זה.
5. **ניקוי אחרי בדיקה לפי מזהה, ובכל לוח שנגעה בו.** שורה
   אמיתית כבר נמחקה כאן בטעות, ו-`quota-test` השאיר שלושה
   ימי נוכחות בלוח האמיתי כי ספר לוח אחד מתוך שניים.
6. **`Number(x)` על תוכן לא-מספרי נותן `NaN`**, ו-`NaN != null`
   הוא `true` אבל `length > NaN` תמיד `false` — האכיפה
   **מתבטלת בשקט**. תמיד `Number.isFinite`.
7. **סינון ששומט שורה בשקט.** `CATEGORIES.includes` הסתיר
   קטגוריה לא-מוכרת מכל מסך בלי שום שגיאה. כל סינון כזה חייב
   **לספור ולדווח** ב-`warnings`.

### ובנוסף, על הסביבה הזו

⚠ **`bash` כאן מעבד את תוכן הפקודה גם בתוך heredoc.** בקטיקים
מורצים כתחליף פקודה, ומרכאות בודדות לא-מאוזנות שוברות את
הפקודה. **לכתיבת קבצים עם עברית או בקטיקים — להשתמש בכלי
Write, או ב-Python עם `PYTHONUTF8=1`.** זה בזבז כאן זמן שלוש
פעמים.

⚠ **`python` כאן הוא cp1252 כברירת מחדל** — `PYTHONUTF8=1`
לפני כל סקריפט שנוגע בעברית, אחרת `UnicodeEncodeError`.

---

## 9 · תקציר לפני קומיט

1. עובדים ב-`lessons`.
2. `npx vite build` עובר.
3. ``grep -c '`' src/styles.js`` = 2.
4. `node --env-file=.env tools/tests/run.mjs` — הכול ירוק.
5. לא נוסף קובץ נספר ל-`api/` בלי לספור (7 מתוך 12).
6. כל נקודת קצה חדשה עטופה ב-`withAuth`.
7. נתון רגיש מסונן בשרת, **במיפוי מפורש ולא בהשמטה**.
8. קובץ מחולל (`*-ids.js`) נכנס לקומיט.
9. אין סוד בקוד, בהערה או בקומיט.
10. `git push origin lessons && git push origin lessons:main`,
    ואז לוודא את ה-SHA בייצור.

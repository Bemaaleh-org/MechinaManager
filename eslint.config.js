/* ============================================================
   בדיקה אחת בלבד: משתנה שנקרא ולא הוגדר
   ------------------------------------------------------------
     npm run check:undef

   ⚠⚠ **למה זה קיים, ולמה רק כלל אחד.**

   `vite build` **אינו תופס `ReferenceError`** — הוא בונה, עובר,
   והדפדפן מציג מסך לבן. זה קרה כאן ארבע פעמים:

     · `byCategory` ו-`CATEGORIES` בלי ייבוא
     · `reload={reload}` בעוד הטוען נקרא `load`
     · `<MI.flag />` על מפתח שאינו קיים (נתפס ב-check:icons)
     · `only` — שלוש שורות סינון שהודבקו לפונקציה הלא-נכונה,
       ומסך "רשימת קניות חדשה" נפל לבן ברגע שנפתח

   הרביעי הוא מה שהוליד את הקובץ הזה. הוא ישב בייצור, `vite build`
   דיווח הצלחה, ומי שנתקל בו ראה מסך לבן בלי שום הודעה.

   ⚠ **כלל אחד ולא חבילת lint.** מאגר בן 40 אלף שורות שנכתב
     בלי linter יציף אלפי אזהרות סגנון ביום שיוכנס אחד, ואז
     מפסיקים להסתכל על הפלט — כלומר גם על הכלל הזה. `no-undef`
     הוא היחיד כאן שמונע מסך לבן, ולכן הוא היחיד שנאכף.

   ⚠ **ובלי `no-unused-vars`** — הוא נכון ומועיל, והוא גם היחיד
     שהיה מדליק מאות שורות על קוד עובד. אם ירצו אותו: כאזהרה,
     ובקומיט נפרד.
   ============================================================ */
import globals from "globals";

export default [
  {
    files: ["src/**/*.{js,jsx}", "api/**/*.js", "shared/**/*.js", "tools/**/*.mjs"],
    languageOptions: {
      ecmaVersion: "latest",
      sourceType: "module",
      parserOptions: {
        /* ⚠ בלי זה כל קובץ jsx נכשל בפרסור, והבדיקה "עוברת"
           על אפס קבצים — כישלון שנראה בדיוק כמו הצלחה. */
        ecmaFeatures: { jsx: true },
      },
      globals: {
        ...globals.browser,
        ...globals.node,
        /* ⚠ `React` מוזרק על ידי התוסף ואינו מיובא בכל קובץ. */
        React: "readonly",
      },
    },
    linterOptions: { reportUnusedDisableDirectives: false },
    /* ⚠ תוסף מדומה. בקוד יש עשרות שורות
       `// eslint-disable-next-line react-hooks/exhaustive-deps`,
       ו-eslint **נכשל** על הפניה לכלל שאינו קיים. הפלאגין
       הזה רושם את השם ואינו עושה דבר — ההערות נשארות קריאות
       למי שיתקין את התוסף האמיתי, והבדיקה אינה מתמלאת רעש. */
    plugins: {
      "react-hooks": {
        rules: { "exhaustive-deps": { create: () => ({}) } },
      },
    },
    rules: {
      "no-undef": "error",
    },
  },
];

<div dir="rtl">

# הקמת עמדת עבודה חדשה

מדריך להקמת מחשב נוסף לעבודה על מערכת ניהול מכינת ניר עוז, ולעבודה
נכונה מכמה מקומות במקביל — בלי התנגשויות ובלי ענפים שבורים.

---

## ההבנה החשובה לפני הכול

הסנכרון עובר דרך שני צירים נפרדים:

```
קוד:     כל העמדות  ⇄  GitHub (ענף lessons)
נתונים:  כל העמדות  →  monday
```

**monday לא צריך שום סנכרון** — הוא ענן מרכזי. כל עמדה עם הטוקן רואה
בדיוק את אותם לוחות באותו רגע. הדבר היחיד שיכול להתפצל ולהתנגש הוא
**הקוד**, ולכן כללי העבודה בהמשך עוסקים רק בו.

---

## הקמת מחשב חדש

### 1. תוכנות

- **Git** — git-scm.com
- **Node.js** (גרסה 20 ומעלה) — nodejs.org
- **Claude Code** — לפי ההוראות של Anthropic

### 2. מפתח SSH — חדש לכל מחשב

⚠ לא מעתיקים מפתח ממחשב אחר. כל עמדה מקבלת מפתח משלה, וכך אפשר
לבטל גישה של מחשב אחד (נגנב, הוחלף) בלי לפגוע בשאר.

```bash
ssh-keygen -t ed25519 -C "achim-<שם-המחשב>" -f ~/.ssh/id_ed25519 -N ""
cat ~/.ssh/id_ed25519.pub
```

את השורה שהודפסה (מתחילה ב-`ssh-ed25519`) מדביקים בדפדפן:
**github.com/settings/ssh/new** → Title: שם המחשב → Add SSH key.

### 3. שכפול והתקנה

```bash
git clone git@github.com:Bemaaleh-org/MechinaManager.git
cd MechinaManager
npm install
git checkout lessons
git config user.name "Achim"
git config user.email "achim@bemaaleh.com"
```

### 4. קובץ הסודות

```bash
npm run setup
```

הסקריפט יוצר `.env`, מייצר `SESSION_SECRET` אקראי, ומדפיס מה עדיין
חסר. נשאר רק להדביק את הטוקן בשורה `MONDAY_TOKEN=` שבקובץ.

```
MONDAY_TOKEN=...
SESSION_SECRET=...    ← נוצר אוטומטית
```

⚠ **הערכים עוברים בערוץ פרטי בלבד** — מנהל סיסמאות, העברה פיזית,
או הודעה שנמחקת. לא מייל גלוי, לא קבוצה. הקובץ חסום ב-`.gitignore`
ולעולם לא נכנס לגיט. בלי הקובץ הקוד רץ אבל אין חיבור ל-monday —
מסך הכניסה יחזיר "ההתחברות נכשלה".

### 5. בדיקה שהכול עובד

```bash
npm run setup     # שוב, אחרי הדבקת הטוקן — מאמת חיבור וכל הלוחות
npm run dev
```

פותחים `http://localhost:5173` — אמור להופיע מסך הכניסה הכחול,
וכניסה עם קוד אמיתי אמורה להצליח. **תמיד בודקים בחלון פרטי.**

Claude Code קורא אוטומטית את `CLAUDE.md` שבשורש המאגר — שם כל
כללי הפרויקט (מגבלת 12 הפונקציות, monday כמקור אמת, הדפוסים).
זה מה שגורם לכל עמדה להתנהג אותו דבר.

---

## שלושת הכללים לעבודה מכמה מקומות

### 1. פותחים ב-pull, סוגרים ב-push

```bash
git pull          # תחילת כל סשן עבודה
# ... עבודה ...
git add -A && git commit -m "תיאור" && git push    # סוף הסשן
```

המאגר הוא נקודת האמת. עבודה שלא נדחפה **לא קיימת** עבור העמדות
האחרות — והיא זו שתתנגש אחר כך.

### 2. נושא אחד — מקום אחד

לא עובדים על אותה תכונה משני מקומות במקביל. זה קרה פעם אחת
(שני מימושים של תפריט הניווט, אחד מהמחשב ואחד מסשן ענן בטלפון)
והסתיים בהתנגשות בשלושה קבצים. התחלת משהו בטלפון? מסיימים
וממזגים לפני שממשיכים במחשב, ולהפך.

### 3. הכול מתמזג ל-lessons בלבד

- הענף `main` הוא הייצור. אליו מגיעים **רק** דרך Pull Request
  שרועי מאשר.
- סשני ענן מהטלפון יוצרים ענפי `claude/...` — תקין, אבל:
  ⚠ **ה-base של ה-PR שלהם חייב להיות `lessons`**, לא `main`
  (ברירת המחדל של גיטהאב היא main — לבדוק בכל פעם!).
- ענף שנפתח — ממזגים מהר. ענף שמחכה יומיים הוא התנגשות שמחכה
  לקרות.

---

## עבודה מהטלפון

| מה | איך |
|---|---|
| להשתמש באפליקציה | הכתובת באוויר, או "הוסף למסך הבית" (PWA) |
| לבדוק גרסה בפיתוח | `npm run dev -- --host` במחשב, ומהטלפון `http://<IP-המחשב>:5173` באותו Wi-Fi |
| לפתח | claude.ai/code → המאגר → ענף בסיס `lessons` → משימות קטנות וממוקדות |
| תיקון של שורה | אפליקציית GitHub → עריכת הקובץ → קומיט ל-lessons |

⚠ לסשן ענן אין את קובץ `.env`, ולכן הוא לא בודק מול monday.
בודקים את התוצאה בתצוגה המקדימה של Vercel (נבנית אוטומטית לכל
push) — שם הסודות מוגדרים.

---

## תזכורות קבועות

- `main` לא נוגעים. לעולם.
- כל בדיקה — בחלון פרטי.
- לפני שינוי בצד השרת: `node --env-file=.env tools/api-snapshot.mjs`
  לפני ואחרי, ומשווים.
- ספירת פונקציות לפני קובץ חדש ב-`api/`: המגבלה 12.
- מה שלא ברור — שואלים, לא מנחשים. במיוחד בכל מה שנוגע לעקרונות
  שב-`CLAUDE.md`.

</div>

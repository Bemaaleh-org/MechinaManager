/* ============================================================
   זריעת נהלי המטבח והחד״א
   ------------------------------------------------------------
   node --env-file=.env tools/seed-hada.mjs

   המקור: "נהלים חד_א.docx" שאחים צירף. שני חלקים:
     · מטלות — לצ׳ק ליסט, לפי יום
     · נהלים — לבלוקי טקסט נערכים

   ⚠ **המבנה חולץ מהעיצוב שבמסמך ולא נוחש.** כותרת אזור מסומנת
     שם ב-bold+underline, והמטלות תחתיה רגילות. שתי שורות
     במסמך מסומנות bold ובכל זאת הן מטלות ("לרוקן מגירות…"),
     ולכן החלוקה נבדקה ידנית ולא הוסקה אוטומטית — הסקה שגויה
     הייתה נראית סמכותית והייתה שגויה.

   ⚠ **וכל שורה כאן ניתנת לעריכה מהאפליקציה** על ידי אחראי
     המטבח. הזריעה היא נקודת פתיחה, לא מקור אמת.

   ⚠ הרצה חוזרת אינה מכפילה — מטלה נמצאת לפי שם ויום.
   ============================================================ */
import { gql } from "../api/_monday.js";
import { CHORE_BOARDS, CHORE_COLS } from "../shared/chores-ids.js";

const C = CHORE_COLS.checklist;
const T = CHORE_COLS.texts;

/* ============================================================
   שגרה יומית — חוזרת בכל יום
   ============================================================ */
const DAILY = [
  ["בוקר", [
    "טאטוא של הרצפה ומעבר עם מגבונים על השולחנות",
    "ניקיון כסאות ורגלי שולחנות",
    "ניקיון כללי אבק עם מגבונים",
  ]],
  ["סיום כל ארוחה", [
    "מעבר על השולחנות עם סבון וסמרטוט",
    "טאטוא רצפה",
    "מעבר על כל משטחי העבודה עם סקוטש וסבון",
    "סגירת מזון בקופסאות, פינוי אשפה",
    "ניקיון טוסטרים אם השתמשו בהם",
  ]],
  ["סיום יום", [
    "שטיפת רצפה כללית מטבח וחדר אוכל — קרצוף עם מטאטא כביש לפני שטיפה",
    "שטיפת תעלות ניקוז + ריקון ונקיון סלסלה",
    "שטיפת פחים עם צינור וסבון",
  ]],
];

/* ============================================================
   מטלות שבועיות — לפי יום
   ============================================================ */
const WEEKLY = {
  "א": [
    ["מחסן", [
      "ניקיון פנים וחוץ ארונות",
      "נקיון מקררים",
      "מעבר וניקיון על האביזרים השונים",
    ]],
    ["מטבח + שטיפת כלים", ["נקיון קירות סקוטש וסבון"]],
    ["מיון FiFo", ["ארונות, מדפים ומגירות"]],
  ],
  "ב": [
    /* ⚠ שלוש השורות הראשונות במסמך יושבות בלי כותרת אזור. */
    ["כללי", [
      "ניקיון גופי תאורה במטבח וחד״א",
      "ניקיון חלונות",
      "ניקיון כל הדלתות, חוץ ופנים",
    ]],
    ["מדפי אחסון", ["נקיון מדפים בסבון וסקוטש"]],
  ],
  "ג": [
    ["ספרייה בחדר אוכל", ["להוריד הכול, לעבור עם סבון ושטיפה, לסדר מחדש"]],
    /* ⚠ "לרוקן מגירות…" מסומן bold במסמך, והוא מטלה ולא כותרת.
       זו אי-עקביות בעיצוב המקור, לא מבנה. */
    ["מקרר חדר אוכל", ["לרוקן מגירות, סקוטש וסבון פנים וחוץ"]],
    ["מיון FiFo", ["ארונות, מדפים ומגירות"]],
  ],
  "ד": [
    ["ניקיון ציוד יסודי", ["תנור, אינדוקציות, טוסטר, מיקרוגל וכו׳"]],
    ["מטבח + שטיפת כלים", ["נקיון קירות סקוטש וסבון"]],
  ],
  "ה": [
    ["כללי", [
      "ניקוי ומילוי צנצנות תבלינים ומלחיות",
      "נקיון יסודי משטחי עבודה + קרשי חיתוך",
      "פינוי של קרטונים וארגזים",
    ]],
    ["מדפי אחסון", ["נקיון מדפים בסבון וסקוטש"]],
    ["מיון FiFo", ["ארונות, מדפים ומגירות"]],
  ],
  "ו": [
    ["סידור יסודי", ["שטיפת כל המקום"]],
    ["סידור מחלקות", ["ארונות ומדפים"]],
    ["מיון אוכל פתוח", ["סדר וארגון לפני שבת"]],
  ],
  /* ⚠ שבת נשארת ריקה בכוונה — במסמך כתוב שם "שבת שלום" בלבד,
     וזו ברכה ולא מטלה. יום בלי מטלות מוצג כיום בלי מטלות. */
  "ש": [],
};

/* ============================================================
   הנהלים — בלוקי טקסט
   ============================================================ */
const TEXTS = [
  ["hada.receiving", "קבלת סחורה ואחסון מזון", [
    "יש לוודא שחומרי הגלם תקינים",
    "יש לבצע FIFO",
    "יש לרשום על אוכל מוכן תאריך קבלה ושם המקבל (במידה ורוצים לברר יש עם מי)",
    "אחסון סחורה בארונות ומדפים שטופים ונקיים",
    "יש להפריד סחורה לפי מחלקות: ביצים, אוכל מעובד, קופסאות שימורים ומזון יבש, ירקות טריים (בקירור ובחוץ)",
    "חומרי ניקוי בארון נפרד",
    "רטבים פתוחים למקרר",
    "אין שום דבר על הרצפה — ארגזים, שקיות וכלים",
    "אין לאחסן אוכל בשקיות (דגש על ירקות ופירות)",
  ].map((x) => "• " + x).join("\n")],
  ["hada.hygiene", "היגיינה", [
    "בכל כיור סבוניה מלאה + סקוטש במצב טוב + סמרטוט + נייר ניגוב ידיים",
    "יש להקפיד על שטיפת ידיים מרובה לפני ובמהלך הכנה וטיפול במזון",
    "כניסה למטבח בבגדים ונעליים נקיים ושיער אסוף",
    "משטחי עבודה ונירוסטה תמיד נקיים ובלי חלודה",
    "קרשי חיתוך על מתקני ייבוש, ללא כתמים שחורים, נקיים ומטופלים, החלפה אחת לחודש",
    "בסיום שימוש במכשיר יש לנקות (מיקרו, תנור, טוסטר)",
    "לא להשאיר בחוץ אוכל לא מכוסה",
    "הקפדה על מניעת מזיקים כולל הדברה תקופתית",
    "הפרדה נגישה של סכו״ם",
  ].map((x, i) => (i + 1) + ". " + x).join("\n")],
];

/* ============ כתיבה ============ */
const existing = (await gql(
  `query($b:[ID!]){ boards(ids:$b){ items_page(limit:400){ items{ id name
       column_values(ids:["${C.day}"]){ text } } } } }`,
  { b: [CHORE_BOARDS.checklist] })).boards[0].items_page.items;
const key = (name, day) => name.trim() + "@" + day;
const seen = new Set(existing.map((i) => key(i.name, (i.column_values[0] || {}).text || "")));

let added = 0, skipped = 0;
const write = async (day, area, task, order) => {
  if (seen.has(key(task, day))) { skipped++; return; }
  await gql(
    `mutation($b:ID!,$n:String!,$v:JSON!){ create_item(board_id:$b,item_name:$n,column_values:$v,create_labels_if_missing:false){id} }`,
    { b: CHORE_BOARDS.checklist, n: task, v: JSON.stringify({
      [C.day]: { label: day }, [C.area]: area, [C.order]: order }) });
  seen.add(key(task, day));
  added++;
};

let n = 0;
for (const [area, tasks] of DAILY) for (const t of tasks) await write("כל יום", area, t, ++n);
for (const [day, groups] of Object.entries(WEEKLY)) {
  let m = 0;
  for (const [area, tasks] of groups) for (const t of tasks) await write(day, area, t, ++m);
}
console.log(`צ׳ק ליסט: נוספו ${added} · כבר היו ${skipped}`);

const haveTexts = (await gql(
  `query($b:[ID!]){ boards(ids:$b){ items_page(limit:200){ items{ id name } } } }`,
  { b: [CHORE_BOARDS.texts] })).boards[0].items_page.items;
for (const [name, title, body] of TEXTS) {
  if (haveTexts.some((i) => i.name.trim() === name)) { console.log("  קיים:", name); continue; }
  await gql(
    `mutation($b:ID!,$n:String!,$v:JSON!){ create_item(board_id:$b,item_name:$n,column_values:$v,create_labels_if_missing:false){id} }`,
    { b: CHORE_BOARDS.texts, n: name, v: JSON.stringify({ [T.title]: title, [T.body]: body }) });
  console.log("  נזרע:", name, "·", title);
}
console.log("\nבוצע.");

# מיפוי שדות — פרוטוטייפ ↔ monday

מסמך עבודה. נוצר לפני שלב 1. מתעד את המצב הקיים ואת השינויים המתוכננים.

## מצב נוכחי — 01/08/2026

### מה עובד מקצה לקצה מול monday

| זרימה | Endpoint | הערות |
|---|---|---|
| קריאת הקטלוג | `GET /api/catalog` | |
| קריאת התנועות | `GET /api/moves?months=6` | **המערכת רב-משתמשית** — מאומת בשני חלונות |
| דיווח יומי | `POST /api/moves` | קבלה / שימוש / פחת |
| ביטול דיווח | `POST /api/move-cancel` | סימון `בוטל`, לא מחיקה |
| ספירה שבועית | `POST /api/count` | כולל סימון תוקף |

בנוסף: פרויקט Vite ב-`src/` (המקור `mechina-kitchen.jsx` נשמר ללא שינוי); שכבת ביניים ב-`api/` בפורמט Vercel שרצה מקומית; הטוקן לא מגיע לדפדפן — מאומת מול `dist/`.

`lastCountAt` ("נספר השבוע") נגזר מהתנועות ב-monday ולא נשמר בנפרד.

### נשאר בחיבור

1. **רשימות קניות** — הזרימה המלאה: צבירת חוסרים, שליחה לאישור, אישור מנהל, קבלת סחורה. דורש את לוח הרשימות ולוח השורות, שעדיין לא נגענו בהם. החלק הכבד ביותר.
2. **דאשבורד ודוח תקופתי** — קריאה בלבד, קלים יחסית.

### אחרי החיבור

התחברות אישית והרשאות אמיתיות → דיפלוי ל-Vercel → בדיקת שטח → אוטומציות ופוש ערבי.

### פתוח

- גישה ל-Vercel — חוסמת דיפלוי בלבד.
- הטוקן אישי של שרה זרמטי — להחליף לחשבון ייעודי לפני עלייה לאוויר.
- כתיבת מלאי היא קרא-חשב-כתוב; שני תורנים במקביל על אותו מוצר עלולים לדרוס זה את זה.
- **ביטול דיווח פתוח כרגע לכל מי שרואה את המסך**, לא רק למנהל. הגבלה אמיתית תיכנס בשלב ההרשאות.
- שלוש תנועות דמו ישנות בלי תאריך — מסוננות מה-API (`skippedNoDate`), לא נמחקו.

**הרצה:** `npm run dev` (חייב לעבור דרך הסקריפט — הוא טוען את `.env` לשרת).

---

**החלטות שאושרו:**
1. שורות רשימת הקניות — לוח רביעי נפרד
2. מקור האמת למלאי — שדה "מלאי נוכחי" בקטלוג
3. כל שינוי נתונים ב-monday דורש אישור מפורש מראש

---

## לוח 1 — קטלוג מוצרים (5101009573)

מצב: 12 מוצרים, 11 עמודות. כל 12 השמות תואמים לפרוטוטייפ.

| שדה בפרוטוטייפ | עמודה ב-monday | column_id | סוג |
|---|---|---|---|
| `name` | Name | `name` | name |
| `cat` | קטגוריה | `color_mm5knrh5` | status |
| `unit` | יחידת מדידה | `color_mm5k92a6` | status |
| `tracking` | סוג מעקב | `color_mm5kppve` | status |
| `sup` | ספק | `color_mm5khc07` | status |
| `exp` | מעקב תוקף | `boolean_mm5kkcmv` | checkbox |
| `stock` | מלאי נוכחי | `numeric_mm5k9a93` | numbers |
| `min` | מינימום | `numeric_mm5kx89e` | numbers |
| `target` | יעד | `numeric_mm5kz9f5` | numbers |
| `price` | מחיר תקן ליחידה | `numeric_mm5k5j4z` | numbers |
| `stockStatus` | סטטוס מלאי | `color_mm5kvr1a` | status |
| `id` | מזהה הפריט ב-monday | — | (מובנה) |
| `expiryFlag` | סימון תוקף | `color_mm5mg6jn` | status ✅ |
| `pending` | ממתין לאישור | `boolean_mm5mwjw7` | checkbox ✅ |

### בוצע (שלב 1)
1. ✅ עמודה **"סימון תוקף"**: `מעל 3 ימים` (ירוק) / `פחות מ-3 ימים` (כתום)
2. ✅ עמודה **"ממתין לאישור"** (checkbox)
3. ✅ נוספו ל"קטגוריה": `לחם ומאפים`, `תבלינים ומשקאות`
4. ✅ הושבתו `Working on it` / `Done` / `Stuck` ב-5 עמודות הסטטוס

---

## לוח 2 — יומן תנועות (5101009681)

מצב: 3 תנועות דמו, 7 עמודות.

| שדה בפרוטוטייפ | עמודה ב-monday | column_id | סוג |
|---|---|---|---|
| `type` | סוג תנועה | `color_mm5kvcap` | status |
| `qty` | כמות | `numeric_mm5ky86h` | numbers |
| `reason` | סיבת פחת | `color_mm5ksxw1` | status |
| `user` | דווח על ידי | `text_mm5kshe` | text |
| — | שווי (₪) | `numeric_mm5kdzjh` | numbers |
| `pid` | מוצר (שם קריא) | `text_mm5kcy1z` | text |
| `pid` | מוצר (קישור) | `board_relation_mm5m5x2s` | board_relation ✅ |
| `ts` | תאריך ושעה | `date_mm5mhx77` | date ✅ |

### בוצע (שלב 1)
1. ✅ עמודה **"תאריך ושעה"**
2. ✅ עמודה **"מוצר (קישור)"** — נוצרה ידנית (monday חוסמת יצירת עמודות קישור ב-API), מקושרת ללוח הקטלוג
3. ✅ נוספו ל"סוג תנועה": `שימוש`, `ספירה`
4. ✅ נוספו ל"סיבת פחת": `עודף מבושל`, `נשפך / נפגם`, `אחר`
5. ✅ ברירות המחדל הושבתו

---

## לוח 3 — רשימות קניות (5101009698)

מצב: רשימה 1, 7 עמודות.

| שדה בפרוטוטייפ | עמודה ב-monday | column_id | סוג |
|---|---|---|---|
| `sup` | ספק | `color_mm5kmt6g` | status |
| `status` | סטטוס | `color_mm5k3ffc` | status |
| `createdBy` | נוצר על ידי | `text_mm5k5hkc` | text |
| `approvedBy` | אושר על ידי | `text_mm5kcah` | text |
| — | עלות משוערת (₪) | `numeric_mm5k42t7` | numbers |
| — | תאריך יעד קנייה | `date_mm5kjce5` | date |
| `items[]` | **חסר** | — | לוח 4 (שלב 2) |
| `approvedAt` | תאריך אישור | `date_mm5mexba` | date ✅ |
| `purchasedAt` | תאריך קנייה | `date_mm5mtgc3` | date ✅ |

### בוצע (שלב 1)
1. ✅ נוספו ל"סטטוס": `טיוטה`, `מאושר`, `נקנה`, `התפספס`
2. ✅ נוסף ל"ספק": `סיטונאי`; ו-`סופר - שבועי` שונה ל-`סופר`
3. ✅ עמודות **"תאריך אישור"** ו**"תאריך קנייה"**
4. ✅ ברירות המחדל הושבתו

---

## לוח 4 — שורות רשימת קניות (5101307336)

כל שורה = מוצר אחד בתוך רשימה אחת. נוצר בשלב 2, ב-workspace 7158838.

| שדה בפרוטוטייפ | עמודה | column_id | סוג |
|---|---|---|---|
| — | Name | `name` | name |
| `item.qty` | כמות מבוקשת | `numeric_mm5rhj8m` | numbers ✅ |
| `item.got` | כמות שהתקבלה | `numeric_mm5r6xbh` | numbers ✅ |
| `item.auto` | מקור (`אוטומטי` / `ידני`) | `color_mm5rsq8q` | status ✅ |
| `list.id` | קישור לרשימה | `board_relation_mm5rv6c` | board_relation → לוח 3 ✅ |
| `item.pid` | קישור למוצר | `board_relation_mm5rzhc6` | board_relation → קטלוג ✅ |

---

## איפה זה חי בקוד

מזהי העמודות שבמסמך הזה מיושמים ב-`shared/boards.js`, והתרגום עצמו ב-`shared/mapper.js`.
לאימות מול הלוחות בפועל: `node --env-file=.env shared/mapper.test.mjs` (קריאה בלבד).

⚠️ **אי-התאמת תווים**: הפרוטוטייפ משתמש בגרשיים עבריים (`ק״ג`, `חד״פ`, `יח׳`) ואילו ב-monday נכתבו גרשיים רגילים (`ק"ג`, `חד"פ`, `יח'`). התרגום מטופל במפורש ב-`LABELS` שב-`boards.js` — השוואת מחרוזות ישירה תיכשל בשקט.

## הערות

- **ברירות המחדל של monday** (`Working on it` / `Done` / `Stuck`) הושבתו ולא נמחקו — הן נעלמות מכל תפריט אך ניתנות להחזרה.
- **כל ערך בעמודת סטטוס חייב צבע ייחודי** — monday דוחה עדכון עם צבע כפול.
- **`update_status_column` דורס את כל הערכים** — חובה לשלוח את הרשימה המלאה, כשכל ערך קיים נושא את ה-`id` שלו, אחרת הוא נמחק.
- **עמודות `board_relation` לא ניתנות ליצירה ב-API** בשום גרסה. יצירה ידנית בלבד. **כתיבה** כן עובדת: `change_column_value` עם `{"item_ids":[<id>]}`, וניקוי עם `{"item_ids":[]}`.
- **קריאת עמודת קישור**: השדות `text` ו-`value` מחזירים `null` תמיד. חובה להשתמש ב-`... on BoardRelationValue { linked_item_ids linked_items { id name } }`.
- **מחיר תקן** יושב על הקטלוג. שכבת הביניים חייבת לסנן אותו בבקשות של חניך.
- **סימון תוקף** רלוונטי רק למוצרים ש"מעקב תוקף" מסומן בהם.
- **סטטוס מלאי** נגזר מ-`stock < min`. נכתב יחד עם המלאי, לא מחושב בנפרד — אוטומציית החוסר בנויה עליו.

/* ============================================================
   פרמטר הבדיקה ?date= — שער יחיד בצד השרת
   ------------------------------------------------------------
   ההגבלה חייבת לשבת כאן ולא בדפדפן: כל בדיקה בצד הלקוח ניתנת
   לעקיפה על ידי קריאה ישירה ל-endpoint. השרת הוא זה שמחליט
   אם התאריך שנשלח נלקח בחשבון בכלל.

   מותר: הרצה מקומית בלבד.
     • אין VERCEL בסביבה                  → פיתוח מקומי (vite)
     • VERCEL_ENV === "development"       → vercel dev מקומי

   אסור: כל דיפלוי — production וגם preview. שם הפרמטר מתעלם
   לחלוטין, וכאילו לא נשלח: בלי שגיאה, בלי חיווי, בלי testMode.
   התאריך תמיד היום האמיתי לפי שעון ישראל.

   ⚠ באוויר לא מבצעים אפילו אימות פורמט. תאריך פסול לא יחזיר
     400 אלא פשוט יתעלם — אחרת ההבדל בין הסביבות היה ניתן
     לזיהוי מבחוץ.
   ============================================================ */

export const overrideAllowed = () =>
  !process.env.VERCEL || process.env.VERCEL_ENV === "development";

/**
 * מחזיר Date לפי הפרמטר, או null כשאין פרמטר / כשההגבלה פעילה.
 * זורק רק בפיתוח מקומי, כשהפורמט פסול.
 */
export function parseTestDate(raw) {
  if (!overrideAllowed()) return null; // באוויר: כאילו לא נשלח
  if (!raw) return null;

  if (!/^\d{4}-\d{2}-\d{2}$/.test(raw)) {
    throw new Error("תאריך בדיקה לא תקין. הפורמט: YYYY-MM-DD");
  }
  const at = new Date(`${raw}T12:00:00Z`);
  if (Number.isNaN(at.getTime())) throw new Error("תאריך בדיקה לא תקין");
  // JavaScript מגלגל בשקט תאריך שאינו קיים (31 בפברואר → 3 במרץ)
  if (at.toISOString().slice(0, 10) !== raw) {
    throw new Error("תאריך בדיקה לא תקין — היום הזה לא קיים");
  }
  return at;
}

/** true רק כשהפרמטר באמת נלקח בחשבון — מזין את חיווי מצב הבדיקה */
export const isTestMode = (at) => Boolean(at);

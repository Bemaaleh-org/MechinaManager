/* ============================================================
   מי רשאי לנהל את מה שבאחריות ועדת קבוצה ותוכן
   ------------------------------------------------------------
   ⚠ **הלוגיקה עברה ל-`api/_flag-team.js`.** היא הייתה זהה כמעט
     לחלוטין ל-`mayRecruit`, ושונה מ-`maySeeAll` שב-`_tryouts.js`
     בכך שהאחרון פתח ליו״ר בלבד — הפרש שלא היה החלטה. ראו
     ההסבר המלא שם.

   הקובץ נשאר כי חמישה מודולים מייבאים ממנו, והשם אומר את
   התחום. ⚠ אין להחזיר לכאן לוגיקה: מימוש שני הוא בדיוק מה
   שיצר את ההפרש (4מד).
   ============================================================ */

import { mayFlagged, flagHint } from "./_flag-team.js";

/** @returns {{ok:boolean, why:string|null, setup?:boolean, teams:Array}} */
export const mayContent = (session) => mayFlagged(session, "content");

/** ההודעה למי שנחסם — אומרת מי כן רשאי, ולא "אין הרשאה". */
export const contentHint = (may) => flagHint(may, "content");
